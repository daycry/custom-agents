---
verificacion: obligatoria
estado: completado
changelog: Fixed
generacion:               # vía rápida — ventana única de este ledger (usage-meter degradado en las 4 tareas: sin carpeta de transcripciones)
  fuente: estimado
  horas_ia: 0.7
  duracion: ~40m
---

# Checklist de Tareas — jira-review-comments (vía rápida: los comentarios de revisión no llegaban a Jira)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-19 |
| **Plan** | n/a — **vía rápida** (ledger ligero + revisión de dos lentes), a petición del usuario: «los reviews no añaden comentarios en las tareas de Jira: hace todo el flujo pero no se puede saber el resultado de la verificación ni de las x verificaciones de esa tarea» |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del plan. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador SDD externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Diagnóstico (causa raíz, con evidencia).** El mecanismo funciona: `jira-flow.py plan --event gaps --actor reviewer --task T-01 --intento 1` sobre el ledger real de `knowledge-services`, con un `.claude/jira.json` habilitado y un `jira-state.json` de prueba, devolvió las 3 `ops` (etiqueta `ca-reviewer` → transición `reabrir` → comentario con la tabla completa de los 7 gaps del intento 1 y el pie «intento 2 de 3»). Lo que fallaba era la **propiedad**: `commands/dev-cycle.md` (tabla de eventos) decía que `revision`/`gaps` los dispara `adversarial-review`; `skills/adversarial-review/SKILL.md` §6 decía «el orquestador dispara»; `skills/jira-sync/references/review-publish.md` decía «el agente revisor» (que es solo lectura y no tiene tools de Jira); `docs/agents/ROLES.md` decía «lo publica el orquestador/implementer». Cuatro textos, cuatro dueños distintos → nadie lo ejecutaba y el ciclo terminaba sin rastro del veredicto de cada intento en Jira (violación de «un rol, un dueño», `ADR-011`). Además `dev-cycle.md` remitía a un «comentario FINAL (Paso 9 de `jira-sync`)» que no existe (es el Paso 7 y es por intento).

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase 1 — Dueño único del evento de revisión en Jira | 2 | 2 | 100% | 0 / 1,0h | 0,4 (estimado) / 0,3h | 0 / 0,1h | — / 80k |
| Fase 2 — Selección de la sección correcta del intento (varias fases) | 2 | 2 | 100% | 0 / 0,8h | 0,3 (estimado) / 0,4h | 0 / 0,08h | — / 60k |
| **TOTAL** | **4** | **4** | **100%** | **0 / 1,8h** | **0,7 (estimado) / 0,7h** | **0 / 0,18h** | **— / 140k** |

---

## Fase 1 — Dueño único del evento de revisión en Jira

**Estado**: completado · **Estimado**: 1,0h · **Real**: — · **Coste est.**: ~50 € · **Tokens est.**: 80k

### T-01 — La skill `adversarial-review` publica el evento `revision`/`gaps` de CADA intento

- **Descripción**: `skills/adversarial-review/SKILL.md` §6 «Salida y traza» pasa de «el orquestador dispara» a un paso EJECUTABLE propio: nada más escribir la sección `## Revisión de dos lentes — intento N` corre `jira-flow.py plan --ledger <tasks.md> --event revision|gaps --actor reviewer --task T-XX --intento N --json` y aplica las `ops` con el conector; sin `jira.json`/issue degrada con aviso. `commands/dev-cycle.md`: la invocación de la skill (punto 2 de la Fase 3) deja de hablar de «comentario FINAL (Paso 9)» y dice «comentario firmado de CADA intento (Paso 7), lo ejecuta la propia skill»; la tabla de eventos fija el dueño («su paso 6, por intento; no el orquestador ni `reviewer`»); el bullet del worklog aclara que el comentario no es del orquestador. `skills/jira-sync/references/review-publish.md`: la cabecera del bloque `revision`/`gaps` nombra a la skill como ejecutor único (no al agente `reviewer`). `docs/agents/ROLES.md`: filas `reviewer` y `adversarial-review` coherentes con lo anterior. Interop regenerado.
- **Estado**: completado
- **Tipo**: docs (prosa de piezas: skill, comando, referencia, matriz de roles)
- **Tiempo humano**: est. 0,6h · real —
- **Tiempo IA (ejec.)**: est. 0,2h · real 0,3h (estimado; ventana compartida con T-02)
- **Supervisión**: est. 0,06h · real —
- **Archivos**: `skills/adversarial-review/SKILL.md`, `commands/dev-cycle.md`, `skills/jira-sync/references/review-publish.md`, `docs/agents/ROLES.md`, `interop/codex/prompts/dev-cycle.md`, `interop/opencode/commands/dev-cycle.md`. **Añadidos en fix1** (gaps #6/#7/#8/#11 de la revisión de abajo, necesarios para cerrarlos): `docs/FLOWS.md`, `docs/en/FLOWS.md` (tabla de eventos + mermaid atribuidos a `adversarial-review`, no a `reviewer`), `skills/jira-sync/SKILL.md` (Paso 7 distingue `--actor` firma de ejecutor), `agent-kits/shared/review-report.template.md` + `agent-kits/shared/README.md` (renombrado: informe del LEDGER, no el comentario de Jira, que tiene su propia plantilla en `jira-sync`), `docs/agents/CONTRACTS.md` (fila **E19**, la arista que esta tarea nace).
- **Verificación**: `grep -n "lo publica ESTA skill" skills/adversarial-review/SKILL.md && grep -c "Paso 9" commands/dev-cycle.md` → una coincidencia en la skill y `0` en el comando · `python scripts/export-interop.py --check` → «al día» · `python scripts/lint_plugin.py` → 0 errores
- **Changelog**: The adversarial review now posts its verdict to Jira after every attempt (the `revision`/`gaps` comment with the graded gap table), so each task's issue shows the result of each verification; before, four pieces each assumed another one would post it and nothing reached Jira.

**Criterios de aceptación**
- [x] Un solo dueño declarado en las cuatro piezas (skill §6, tabla de `dev-cycle`, `review-publish.md`, `ROLES.md`) y es el mismo: la skill `adversarial-review`
- [x] El paso de la skill es ejecutable tal cual (comando completo con `--actor reviewer --intento N`) y degrada con aviso sin Jira
- [x] Ninguna pieza remite ya a un «Paso 9» inexistente ni a un comentario «final» en lugar de «por intento»
- [x] `SKILL.md` ≤ 200 líneas; interop regenerado y `--check` limpio

### T-02 — Guardarraíl: test de propiedad única sobre los ficheros reales

- **Descripción**: `tests/test_review_jira_owner.py` lee los ficheros REALES (no fixtures) y falla si vuelve a abrirse el hueco: la skill contiene el comando `jira-flow.py … --event revision|gaps … --actor reviewer … --intento N`; la skill no dice que «el orquestador dispara» esos eventos; la tabla de `dev-cycle.md` nombra a `adversarial-review` en las filas `revision`/`gaps` y no cita «Paso 9» ni «comentario FINAL»; `review-publish.md` nombra a la skill como ejecutor; `ROLES.md` no atribuye la publicación al orquestador/implementer/reviewer. Además ejecuta `jira-flow.py` sobre un ledger de prueba con Jira habilitado y comprueba que el evento `gaps` del intento 1 produce el comentario con la tabla de gaps y el pie «intento 2 de 3» (así el test documenta también que el mecanismo funciona y que lo que faltaba era el dueño).
- **Estado**: completado
- **Tipo**: test
- **Tiempo humano**: est. 0,4h · real —
- **Tiempo IA (ejec.)**: est. 0,1h · real 0,1h (estimado)
- **Supervisión**: est. 0,04h · real —
- **Archivos**: `tests/test_review_jira_owner.py`, `docs/roadmap/README.md`
- **Verificación**: `python -m pytest -q tests/test_review_jira_owner.py` → 10 passed (los 6 originales + 4 de fix1: `--actor` firma vs. ejecutor en `jira-sync/SKILL.md`, «Paso 9» ausente de los seis ficheros vivos, `review-report.template.md` no atribuido al comentario de Jira); revertir a mano la frase «el orquestador dispara» en la skill → el test falla nombrando la pieza
- **Changelog**: A regression test now guards that exactly one piece owns posting the review verdict to Jira.

**Criterios de aceptación**
- [x] El test corre contra los ficheros reales del repo y contra un ledger de prueba con Jira habilitado (mecanismo + propiedad)
- [x] Falla con mensaje que nombra la pieza y la frase ofensiva si se reintroduce un segundo dueño o el «Paso 9»

---

## Fase 2 — Selección de la sección correcta del intento (ledgers con varias fases)

**Estado**: completado · **Estimado**: 0,8h · **Real**: — · **Coste est.**: ~40 € · **Tokens est.**: 60k

> **Causa 2 (con evidencia reproducible; corregida en la revisión Fase 1-2 intento 1, gap #9 — la
> versión anterior de esta nota afirmaba que el ledger real tenía 10 cabeceras con un paréntesis
> TRAS el número de intento; en `2dca4c0` esas cabeceras ya casan con `REVISION_HDR_PATTERN`, así
> que esa afirmación era falsa: la causa real nunca fue el patrón de la cabecera, sino qué sección
> elige `seccion_revision` entre varias con el mismo número).** Corregida la propiedad (Fase 1), el
> comentario seguía pudiendo ser el equivocado: `jira-flow.py::seccion_revision(texto, intento)`
> elegía la PRIMERA cabecera `## Revisión de dos lentes — intento N` que casara con ese número. Un
> ledger con varias fases (cada una arranca su propio bucle desde «intento 1») tiene VARIAS
> cabeceras con el mismo número — el real de `docs/roadmap/2026-09-15-knowledge-services/tasks.md`
> tiene 4 secciones «intento 1», una por fase, todas de la forma `## Revisión de dos lentes —
> intento 1: Fase 2 (T-04, T-05, T-06) — 19 gaps…` (`:` justo tras el número, SÍ casa) — así que
> `--task T-04 --intento 1` caía en la sección de la Fase 1 (gaps de T-01/T-02/T-03), no encontraba
> filas de T-04 y devolvía `{"error": ["el intento 1 no tiene gaps para T-04 — usa --event
> revision"]}`: un «sin gaps» FALSO justo cuando SÍ los había, en la fase correcta. Reproducible
> tal cual, sin normalizar nada: `git show 2dca4c0:docs/roadmap/2026-09-15-knowledge-services/tasks.md`
> a un tmp con `.claude/jira.json` `{"enabled": true}` → `jira-flow.py` de esa misma revisión
> `plan --event gaps --actor reviewer --task T-04 --intento 1 --root <tmp> --json` → el error de
> arriba; el mismo `jira-flow.py` de HEAD sobre el mismo fichero (sin tocarlo) → `ops` con el
> comentario de la Fase 2 (ver Verificación de T-03).

### T-03 — `jira-flow.py` elige la sección del intento por las tareas pedidas

- **Descripción**: `seccion_revision(texto, intento, tareas=None)` (nuevo parámetro opcional, retrocompatible) — entre TODAS las cabeceras con ese `intento`, elige la que MENCIONE alguna de las `tareas` pedidas, restringido a la cabecera/resumen o a la columna `Tarea` de sus filas (patrón `T-NN`; nunca `Corrección`/`Evidencia`, que citan tests y otras tareas de contexto y darían falsos positivos en un ledger real con fases que se referencian entre sí — comprobado con el ledger real: una fila de la Fase 1 cita "T-04" en su celda `Corrección`). Si ninguna cabecera menciona las tareas, usa la ÚLTIMA (más reciente) y añade un aviso explicando el porqué. Con una sola cabecera para ese `intento`, o sin `tareas`, el comportamiento no cambia. Los dos llamadores (`evidencia_aprobado` y el bloque `revision`/`gaps` de `construir_plan`) pasan ahora `tareas`; el aviso de fallback se añade a `avisos` del plan.
- **Estado**: completado
- **Tipo**: código (script determinista, sin modelo)
- **Tiempo humano**: est. 0,5h · real —
- **Tiempo IA (ejec.)**: est. 0,25h · real 0,2h + fix1 0,4h = 0,6h (estimado; usage-meter degradado: sin carpeta de transcripciones)
- **Supervisión**: est. 0,05h · real —
- **Archivos**: `skills/jira-sync/scripts/jira-flow.py`, `skills/jira-sync/scripts/test_jira_flow.py`, `agent-kits/shared/ledger-lint.py`, `agent-kits/shared/task-brief.py`, `agent-kits/shared/copias.json`. **Añadido en fix1** (gap #5: `task-brief.py::_gaps_pendientes_de_tarea` delega en el mismo parser): `agent-kits/shared/test_task_brief.py`. **Añadido en fix3** (gaps #20-#22: `filas_pendientes_de_tarea`/`gap_pendiente` canónicos en `ledger-lint.py`, respaldo local en `jira-flow.py`/`task-brief.py`, tests FXP/M2b/aceptación real): sin ficheros nuevos, mismos ficheros de arriba.
- **RED (fix1)**: `test_gaps_intento_repetido_elige_la_seccion_que_menciona_la_tarea_pedida` (existente) falló contra el `jira-flow.py` de la base `2dca4c0` sobre el ledger real de `knowledge-services` sin tocar (`{"error": ["el intento 1 no tiene gaps para T-04 — usa --event revision"]}`) · 2026-09-19
- **Verificación**: `python -m pytest -q skills/jira-sync/scripts/test_jira_flow.py` → 51 passed (fix1: 47 previos + 5 nuevos de la fusión de parser con `ledger-lint.py` — mutantes M2/M3, ledger «fase reabierta», `aprobado` con intentos desiguales entre fases — menos 1 test renombrado sin duplicar). Además, contra el ledger REAL `docs/roadmap/2026-09-15-knowledge-services/tasks.md` **sin tocar ni una cabecera** (evidencia reproducible, gap #9): copiado tal cual a un tmp con `.claude/jira.json` `{"enabled": true}` y `.claude/jira-state.json` `{"tasks": {"T-04": {"issueKey": "KS-4"}}}` — con el `jira-flow.py` de la BASE `2dca4c0`, `plan --event gaps --actor reviewer --task T-04 --intento 1 --root <tmp> --json` → `{"error": ["el intento 1 no tiene gaps para T-04 — usa --event revision"]}` (el síntoma real, reproducido); con el `jira-flow.py` de HEAD sobre el MISMO fichero sin normalizar → el comentario cita **"Revisión de dos lentes — intento 1: 16 gap(s)" (T-04)** con la fila `#40 Critical: curator-gate.py no tiene entrada en MODOS…` de la Fase 2, y NO ninguna fila de la Fase 1 (T-01/T-02/T-03); `--task T-01 --intento 1` sobre el mismo tmp sigue devolviendo la sección de la Fase 1 (`#5 Important: id_prefix por defecto…`). `python agent-kits/shared/ledger-lint.py <tmp>/…/tasks.md` → `0 incoherencias · 0 avisos` (las 12 cabeceras reales ya casan; no hacía falta normalizar nada).

**Verificación fix3 (gap #23, cifras reales tras cerrar #20-#22 y #24 de la ronda intento 3).** `python -m pytest -q skills/jira-sync/scripts/test_jira_flow.py` → **66 passed** (65 previos de fix1/fix2 + 1 nuevo `test_m2b_gaps_de_t04_elige_la_ultima_seccion_ajena_no_la_primera`, más los 2 de aceptación real de #22 ya contados en los 65: `test_aprobado_sobre_el_ledger_real_de_knowledge_services_concede_done` y `test_gaps_sobre_el_ledger_real_de_knowledge_services_por_intento`). `python -m pytest -q agent-kits/shared/test_task_brief.py` → **67 passed, 1 xfailed** (incluye `test_fxp_gaps_muestra_el_critical_cruzado_de_otra_seccion_del_mismo_intento` y `test_gaps_sobre_el_ledger_real_de_knowledge_services` reescrito para el filtro de pendientes correcto). Sobre el ledger REAL `docs/roadmap/2026-09-15-knowledge-services/tasks.md` copiado a tmp: `gaps --task T-04 --intento 1/2/3` → cada llamada trae SU PROPIO intento en el comentario (sin reventar, sin `None`); `gaps --task T-10 --intento 3` → intento 3 de la Fase 4; `aprobado --task T-04 --qa-verde` y `aprobado --task T-10 --qa-verde` → Done concedido (la iniciativa está cerrada, todos sus gaps `corregido:`, y la unión de secciones no encuentra ninguna fila pendiente). `python agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-19-jira-review-comments/tasks.md` → `0 incoherencias · 0 avisos`. `python tests/test_copias_declaradas.py` → exit 0 (el respaldo local de `filas_pendientes_de_tarea`/`gap_pendiente` en `jira-flow.py` y `task-brief.py` sigue siendo copia literal del canónico de `ledger-lint.py`).
- **Changelog**: The review comment posted to Jira for a task now comes from that task's own review round, not from a different phase that happened to share the same attempt number.

**Criterios de aceptación**
- [x] Ledger con Fase 1 «intento 1» (gaps de T-01) y Fase 2 «intento 1» (gaps de T-04): `--event gaps --task T-04 --intento 1` trae la tabla de la Fase 2 y NO la de la Fase 1
- [x] `--event gaps --task T-01 --intento 1` sobre el mismo ledger trae la de la Fase 1
- [x] `--event revision --task T-05 --intento 1` cuando la Fase 2 dice «sin gaps» para T-05 pero la Fase 1 tenía gaps (de T-01) con el mismo número: exit 0, comentario de revisión aprobada, sin transición de reapertura
- [x] Tarea sin mención en ninguna cabecera de ese intento: cae en la última con aviso en `avisos`
- [x] El comportamiento con una sola sección para ese `intento` no cambia (la suite previa de 42 tests sigue en verde)

**Nota de decisión — corregida (gap #9, revisión Fase 1-2 intento 1).** La versión anterior de esta
nota afirmaba que el ledger real de `knowledge-services` tenía 10 cabeceras con un paréntesis TRAS
el número de intento que no casaban con `REVISION_HDR_PATTERN`, y que la verificación de arriba se
había hecho sobre una COPIA con esas cabeceras normalizadas «a mano» — ambas cosas eran falsas
sobre el estado real del repo en esta rama (`2dca4c0`): las 12 cabeceras reales tienen la forma
`intento N: Fase X (T-…) — …` (`:` justo tras el número), que SÍ casa con el patrón, y
`ledger-lint.py` sobre el fichero real da `0 incoherencias · 0 avisos` — no hubo ninguna
normalización, la Verificación de arriba corre sobre el fichero tal cual. El síntoma real que
reportó el usuario (`--task T-04 --intento 1` → `{"error": [...]}`) SÍ se reproduce con el
`jira-flow.py` de la base `2dca4c0` (antes de esta ronda) sobre ese mismo fichero sin tocar: la
causa era exclusivamente la selección de sección entre varias con el mismo número de intento
(Causa 2, arriba), nunca el patrón de la cabecera — T-04 (el aviso de `ledger-lint`) sigue siendo
útil para los ledgers históricos que de verdad llevan un paréntesis tras el número (34 cabeceras de
6 ledgers, ver nota «Fuera de la ronda» de la revisión Fase 1-2), pero no era la causa de ESTE
síntoma.

### T-04 — `ledger-lint.py` avisa de cabeceras de revisión que NO casan con el patrón

- **Descripción**: en `lint()`, toda línea que empiece por `## Revisi` y contenga `dos lentes` pero no case con `REVISION_HDR_RE` produce un AVISO (no error: el ledger sigue siendo válido, solo esa sección queda invisible para `jira-flow.py`/`task-brief.py`): «línea N: cabecera de revisión no casa con REVISION_HDR_PATTERN: jira-flow/task-brief no la verán — usa `## Revisión de dos lentes — intento N: <resumen>` (sin paréntesis tras N)». No se tocó `skills/adversarial-review/SKILL.md` (ya documenta el formato correcto en su paso 6 y está a 200 líneas justas, sin hueco para una línea más) ni `commands/`/`agents/`/`hooks/` (T-04 no los toca, así que no aplica regenerar interop; `export-interop.py --check` sigue «al día» porque T-01/T-02 ya lo habían regenerado).
- **Estado**: completado
- **Tipo**: código (script determinista)
- **Tiempo humano**: est. 0,3h · real —
- **Tiempo IA (ejec.)**: est. 0,15h · real 0,1h + fix1 0,15h = 0,25h (estimado; usage-meter degradado: sin carpeta de transcripciones)
- **Supervisión**: est. 0,03h · real —
- **Archivos**: `agent-kits/shared/ledger-lint.py`, `tests/test_ledger_lint.py`
- **RED (fix1)**: caso 24 (cabecera de cabecera-símil dentro de una valla de código) y caso 25 (cabecera `###`, h3) fallaban contra el `lint()` previo — el primero por avisar sobre texto vallado (no respetaba `sin_vallas()`, gap #12), el segundo por NO avisar de una cabecera `###` real que tampoco casa con `REVISION_HDR_RE` (el filtro exigía `##` literal, gap #13) · 2026-09-19
- **Verificación**: `python tests/test_ledger_lint.py` → `test_ledger_lint: 27/27 OK` (los 25 previos de esta tarea + 2 nuevos de fix1: caso 24, cabecera dentro de una valla de código → SIN aviso; caso 25, cabecera `###` con «dos lentes» → aviso)
- **Changelog**: `ledger-lint` now flags review headers with the wrong shape so a task's Jira comment never silently comes from the wrong review round.

**Criterios de aceptación**
- [x] Fixture con «intento 1 (Fase 2: T-04): …» → aviso con el texto de arriba
- [x] Fixture con «intento 1: Fase 2 (T-04) — …» → sin aviso
- [x] Es AVISO, no error (exit 0 con `--warn-only` y sin él, mismo criterio que el resto de avisos legacy del script)

**Nota (ruta declarada distinta a la del encargo).** El encargo cita `agent-kits/shared/test_ledger_lint.py`; el test real del repo vive en `tests/test_ledger_lint.py` (no hay copia en `agent-kits/shared/`) y es un script con `assert`/`main()`, no pytest — se ejecuta con `python tests/test_ledger_lint.py`, como el resto de la suite (`tests/test_suites_no_pytest.py`). Se usó la ruta real.

## Revisión de dos lentes — intento 1: Fases 1-2 (T-01, T-02, T-03, T-04) — 13 gaps (2 Critical, 7 Important, 4 Minor), lentes A+B (C y D no aplican: `review-lens-select.py` sin motivos), rango `2dca4c0..9437a4d`

Puerta previa: `scope-check.py --base 2dca4c0` → 12 en alcance, 0 fuera, exit 0. Puertas: `lint_plugin` 0 errores · `evals/check` 0 errores · `export-interop --check` al día · `test_jira_flow` 47 passed · `test_ledger_lint` 25/25 · `test_review_jira_owner` 7 passed · `test_task_brief` 63 passed. La Lente A confirmó que T-03 resuelve el síntoma del usuario sobre el ledger REAL de knowledge-services (base → «no tiene gaps para T-04»; HEAD → comentario con 16 gaps de la Fase 2), pero la Lente B demostró que la selección nueva sigue eligiendo mal en dos formas reales del mismo ledger (intento 2 de T-04 y el evento `aprobado`). Mutantes: M1 y M4 muertos; M2 (`coincide[-1]`) y M3 (mención en `Corrección`/`Evidencia`) sobreviven. **Ronda fix1 (los 13 gaps de abajo, cerrados):** parser único `secciones_revision`/`seleccionar_seccion`/`ultimo_intento_para` en `agent-kits/shared/ledger-lint.py` (canónico), con respaldo local declarado en `copias.json` para `jira-flow.py` y `task-brief.py`; puertas tras la ronda: `test_jira_flow` 51 passed · `test_ledger_lint` 27/27 OK · `test_review_jira_owner` 10 passed · `test_task_brief` 66 passed, 1 xfailed · `test_copias_declaradas` 22 passed · `lint_plugin` 0 errores · `evals/check` 0 errores · `export-interop --check` al día.

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| 1 | **Critical** | La selección por tarea elige la PRIMERA candidata (`jira-flow.py:329` `coincide[0]`, contra su docstring) y la «mención» se calcula sobre texto contaminado: `_extraer` corta en la siguiente cabecera de REVISIÓN, no en el siguiente `##`, así que el párrafo de contexto se traga fases enteras (bloques `### T-XX`) y casa con tareas de otra fase (`:292`, `:322-324`). Ledger REAL: `--task T-04 --intento 2 --event revision` → «sin gaps» con el resumen de la Fase 1, cuando la Fase 2 intento 2 tiene 13 gaps nuevos. **Arbitraje (diseño):** parser único de secciones en `agent-kits/shared/ledger-lint.py` (canónico, ya lo importa jira-flow para el patrón): `secciones_revision(texto)` → lista de `{intento, cabecera, resumen, filas, inicio, fin}` con `fin` = siguiente encabezado `## ` (cualquiera) o fin de fichero; `mencion` = SOLO la línea de cabecera + celdas `Tarea` de las filas (ids por `\bT-\d{2}\b`, así `T-02/T-04` cuenta para ambas), nunca el párrafo ni `Corrección`/`Evidencia`; `seleccionar_seccion(secciones, intento, tareas)` → entre las de ese intento que mencionan alguna tarea, la ÚLTIMA; sin mención → la última de ese intento con aviso. `jira-flow.py` y `task-brief.py` la importan y conservan una copia local de respaldo DECLARADA en `copias.json` (ADR-016) para el paquete sin kit. Tests que maten M2 y M3 y el caso real del ledger de knowledge-services (T-04 intento 2 → 13 gaps) | T-03 | corregido: `secciones_revision`/`seleccionar_seccion`/`ultimo_intento_para` en `agent-kits/shared/ledger-lint.py` (canónico), con respaldo local declarado en `jira-flow.py`/`task-brief.py` (`copias.json`); `fin` de sección = siguiente `## ` cualquiera, `menciones()` solo cabecera+columna `Tarea` | `test_mencion_no_se_cuela_desde_correccion_ni_evidencia`, `test_gaps_intento_repetido_elige_la_seccion_que_menciona_la_tarea_pedida` -> passed |
| 2 | **Critical** | `evidencia_aprobado` fija el intento con `max()` GLOBAL sobre todo el ledger y luego pasa `tareas` (`jira-flow.py:543,549`): si otra fase llegó a un intento mayor, la fase dueña de la tarea ni se mira y `aprobado` (→ Done) se concede con un Critical pendiente. **Arbitraje:** el intento «último» se calcula SOLO entre las secciones que mencionan la tarea (con el parser de #1); sin ninguna → rechazo con razón («ninguna revisión menciona T-XX»); el resumen y las filas salen de ESA sección; el aviso de selección viaja en `avisos` y en el comentario | T-03 | corregido: `evidencia_aprobado` delega en `ledger_lint.ultimo_intento_para`/`seleccionar_seccion`, restringido a las secciones que mencionan la tarea; sin ninguna, rechazo (no adivina) | `test_aprobado_rechaza_si_la_fase_de_la_tarea_no_llega_al_intento_mas_alto_global` -> passed |
| 3 | Important | El resumen del comentario de Done sale de `ultimo` (primer empate del `max`) y las filas de la sección elegida: pueden ser de fases distintas y Jira cita como evidencia el resumen de otra fase (`:546` vs `:549`). Se cierra con #2 | T-03 | corregido con #2: el resumen y las filas del comentario de `aprobado` salen de la MISMA sección elegida por `seleccionar_seccion`, nunca de otra fase | `test_aprobado_lo_dispara_el_orquestador_con_evidencia_y_cierra_firmando` -> passed |
| 4 | Important | El aviso «ninguna sección menciona … se usa la última» se descarta en `aprobado` (`:549` `_, filas, _ =`). Se cierra con #2 | T-03 | corregido con #2: `evidencia_aprobado` propaga el `aviso` de `seleccionar_seccion` a `avisos` del plan (bloque `aprobado` de `construir_plan`) | `test_aprobado_de_una_tarea_limpia_no_lo_bloquea_el_gap_pendiente_de_otra_fase` -> passed |
| 5 | Important | `task-brief.py:630,640` conserva el criterio viejo (`max()` global + `celdas[3] != tid` exacto): sobre el ledger real, `_gaps_pendientes_de_tarea` da `None` para T-04/T-07/T-10 aunque sus fases tienen 10/15/9 filas en el intento 3 → el brief de redespacho llega «sin gaps». Además el aviso nuevo de `ledger-lint:491` promete que jira-flow y task-brief ven lo mismo. **Arbitraje:** task-brief usa el mismo parser/selección (#1); test con el ledger real (T-10 → filas) y con el fixture multifase | T-03 | corregido: `task-brief.py::_gaps_pendientes_de_tarea` delega en el mismo `secciones_revision`/`ultimo_intento_para`/`seleccionar_seccion` de `ledger-lint.py` | `test_gaps_fase_con_menos_intentos_no_queda_tapada_por_otra_fase`, `test_gaps_fase_reabierta_usa_su_propio_ultimo_intento`, `test_gaps_sobre_el_ledger_real_de_knowledge_services` -> passed |
| 6 | Important | `docs/FLOWS.md:176-177,185-186` y `docs/en/FLOWS.md:177-178,186-187` (tabla «quién dispara qué» y mermaid) siguen atribuyendo `revision`/`gaps` al agente `reviewer` (5.ª y 6.ª pieza con dueño distinto; arista E3 de CONTRACTS lista FLOWS como pieza que DESCRIBE); el guardarraíl de T-02 no las cubre | T-01/T-02 | corregido: FLOWS.md (ES+EN) atribuye `revision`/`gaps` a la skill `adversarial-review` (firma `--actor reviewer`), tabla y mermaid | `grep -c reviewer docs/FLOWS.md docs/en/FLOWS.md` tras el cambio; lectura manual de las filas y el mermaid actualizados |
| 7 | Important | `skills/jira-sync/SKILL.md:100-103` (Paso 7) dice que el evento «lo dispara su actor fijo (`implementer` · `reviewer` · `qa` · `orquestador`)»: `--actor reviewer` es la FIRMA del comentario, no el ejecutor. Reescribir: «`--actor` = firma; el ejecutor de `revision`/`gaps` es la skill `adversarial-review` (paso 6)»; añadir la ruta al guardarraíl de T-02 | T-01/T-02 | corregido: Paso 7 de `skills/jira-sync/SKILL.md` distingue `--actor` (firma) del ejecutor real (skill `adversarial-review`, su paso 6, para `revision`/`gaps`) | `test_jira_sync_skill_distingue_actor_firma_de_ejecutor` -> passed |
| 8 | Important | Queda un «Paso 9» inexistente y una SEGUNDA plantilla del comentario de revisión: `agent-kits/shared/README.md:25` declara `review-report.template.md` como «plantilla fija del comentario de revisión … jira-sync Paso 9» y `SKILL.md:180` la lista. **Arbitraje:** la plantilla de Jira es ÚNICA (`skills/jira-sync/assets/comment-revision-*.md`, la rellena `jira-flow.py`); `review-report.template.md` queda SOLO como plantilla del informe/sección del ledger (renombrar su cabecera y la fila del README a «informe de revisión para el ledger; el comentario de Jira lo renderiza jira-flow, Paso 7»). Añadir `agent-kits/shared/README.md` al guardarraíl («Paso 9» prohibido en todo el repo salvo registros fechados) | T-01/T-02 | corregido: `review-report.template.md` renombrado a informe de revisión del LEDGER (no del comentario de Jira); fila de `agent-kits/shared/README.md` reescrita sin «Paso 9»; ambos ficheros en el guardarraíl | `test_ningun_fichero_vivo_remite_a_un_paso_9_de_jira_sync`, `test_shared_readme_no_atribuye_review_report_template_al_comentario_de_jira` -> passed |
| 9 | Important | La Verificación y la «Nota de decisión» de T-03 (y el párrafo «Causa 2-bis» de la Fase 2) afirman que el ledger real tiene 10 cabeceras con paréntesis y que «sin normalizar sigue fallando»: en esta rama (rebasada sobre `2dca4c0`) las 12 cabeceras ya casan y `ledger-lint` da 0 avisos. Reescribir con la evidencia reproducible sobre el fichero tal cual (base → error; HEAD → ops) | T-03 | corregido: Verificación y Nota de decisión de T-03, y la Causa 2 de la Fase 2, reescritas con evidencia reproducible sobre el fichero real tal cual (base `2dca4c0` → error; HEAD → ops), sin ninguna normalización de cabeceras | `python agent-kits/shared/ledger-lint.py <ledger real>` -> `0 incoherencias · 0 avisos` (evidencia pegada en la Verificación de T-03) |
| 10 | Minor | Al comprimir «Degradación» de `SKILL.md` se perdió «cuando hay ledger» (única salvaguarda del modo sin ledger). Reponer la condición sin pasar de 200 líneas | T-01 | corregido: «(cuando hay ledger; a demanda sin ledger no aplica)» repuesto en la línea de «Sin Jira activo» de la sección Degradación | `grep -c "cuando hay ledger" skills/adversarial-review/SKILL.md` -> 1; `python tests/test_skill_size.py` (≤200 líneas, 200 exacto) -> passed |
| 11 | Minor | Arista nueva `adversarial-review → jira-flow.py` (con puerta `tests/test_review_jira_owner.py`) sin fila en `docs/agents/CONTRACTS.md`; añadir también el criterio de selección por tarea (#1) como parte del contrato. Numerar **E19** (E18 la reserva `graphiti-memory` en otra rama; anotarlo) | T-02/T-03 | corregido: fila **E19** añadida a `docs/agents/CONTRACTS.md` (`adversarial-review → jira-flow.py → ledger-lint.py`, con la selección de sección por tarea como parte del contrato); anotado que E18 la reserva `graphiti-memory` en otra rama | `python scripts/lint_plugin.py` -> 0 errores (matriz de CONTRACTS con Puerta no vacía) |
| 12 | Minor | El aviso nuevo de `ledger-lint` recorre `text.splitlines()` sin `sin_vallas()`: una cabecera mala citada como ejemplo dentro de una valla ```` ``` ```` avisa (`ledger-lint.py:485-492`) | T-04 | corregido: el aviso de cabeceras de `ledger-lint.py::lint()` recorre `sin_vallas(text)`, no `text` crudo | `test_ledger_lint.py` caso 24 (cabecera con paréntesis dentro de una valla → sin aviso) -> passed |
| 13 | Minor | El filtro `startswith("## Revisi")` deja fuera `### Revisión de dos lentes — intento 1: …`, que tampoco casa y no avisa (`ledger-lint.py:489`); aceptar `#{2,3}` y reescribir el texto del aviso para que no prometa lo que task-brief no cumple hasta #5 | T-04 | corregido: el filtro acepta `#{2,3}` (regex `^#{2,3}\s+Revisi`), no solo `##` literal | `test_ledger_lint.py` caso 25 (cabecera `###` → aviso) -> passed |

Fuera de la ronda (anotado): `_clave_flujo` es sensible al orden de `--batch` (`T-04,T-05` ≠ `T-05,T-04` → comentario duplicado): preexistente, fuera del diff; 34 cabeceras de 6 ledgers históricos no casan con el patrón (registros fechados: no se reescriben; el aviso nuevo las señalará si alguien vuelve a operar sobre ellos).

## Revisión de dos lentes — intento 2: Fases 1-2 (T-01, T-02, T-03, T-04) — 10/13 gaps del intento 1 cerrados (#1 y #2 ABIERTOS, #4 no verificable); 6 gaps NUEVOS (3 Critical, 1 Important, 2 Minor), lentes A+B fusionadas en verificación dirigida, rango `b720a81..42e86be`

Puertas: 149 passed (jira-flow, task-brief, copias, guardarraíl) · `test_ledger_lint` 27/27 · lint 0 errores · evals 0 errores · interop al día · `ledger-lint` 0/0 en este ledger y en el de knowledge-services · `scope-check` exit 0. Mutantes: M2 muerto; **M3 sobrevive** (el test que lo declaraba muerto no contiene veneno). **El caso de aceptación del gap #1 sobre el ledger REAL sigue rojo**: `--event gaps --task T-04 --intento 2` → «el intento 2 no tiene gaps para T-04» porque `menciones()` cuenta `T-04` en las celdas combinadas `T-02/T-04` de la Fase 3, «la última que menciona» elige la Fase 3 y el filtro de filas (igualdad exacta) no encuentra nada.

**Arbitraje del orquestador (diseño, ronda fix2; sustituye al del intento 1 donde difiera):** (1) UNA sola función `ids_de_tarea(texto)` en `ledger-lint.py` (regex `\bT-\d{2}\b`) que usan TODOS los filtros: `menciones()`, el filtro de filas de `revision`/`gaps` y de `aprobado` en `jira-flow.py`, y `_gaps_pendientes_de_tarea` en `task-brief.py`; una fila `T-02/T-04` o `T-01 (mensaje de T-07)` cuenta para cada id que contiene, en todos los sitios por igual. (2) `seleccionar_seccion(secciones, intento, tareas)`: entre las secciones de ese intento, prefiere la que tiene FILAS de la tarea (no solo mención en cabecera); si varias, la última; si ninguna tiene filas pero alguna la menciona en cabecera, la última de esas (es el caso «sin gaps para T-XX» legítimo); si ninguna → la última del intento con `aviso`. (3) `ultimo_intento_para(secciones, tareas)`: SIN atajo por números únicos; siempre el máximo SOLO entre las secciones que mencionan (cabecera o filas) alguna tarea; `None` si ninguna → `aprobado` rechaza con razón «ninguna revisión menciona T-XX» y el aviso viaja en `avisos`. (4) Tests de aceptación sobre el ledger REAL `docs/roadmap/2026-09-15-knowledge-services/tasks.md` copiado a tmp (con `--force` o estado limpio, porque `plan` escribe `jira-state.json`): `T-04 --intento 1` → 16 filas (Fase 2); `T-04 --intento 2` → 13 filas (Fase 2); `T-04 --intento 3` → filas de la Fase 2 intento 3; `T-10 --intento 3` → Fase 4; `--event revision --task T-04 --intento 2` → exit 2. Fixtures FX2 (fila combinada `T-02/T-04` Critical → `aprobado T-04` rechaza; el brief de T-04 la muestra) y FX3 (numeración continua entre fases → `aprobado T-01` rechaza citando el Critical de la Fase 1). Mutante M3 con veneno real: fila de otra tarea cuya `Corrección` cita `T-04` → no cuenta como mención. (5) Guardarraíl: FLOWS ES/EN en `tests/test_review_jira_owner.py` y «Paso 9» por grep repo-wide (`.md` fuera de `docs/roadmap/`, `docs/knowledge/`, CHANGELOG*, `interop/`), incluyendo `CONTINUE-HERE.md`.

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| 14 | **Critical** | Criterio de mención (regex, celdas combinadas) incoherente con el filtro de filas (igualdad exacta): `--task T-04 --intento 2` elige la Fase 3 por `T-02/T-04` y no encuentra filas → «no tiene gaps para T-04» (`ledger-lint.py:282-296`, réplicas en `jira-flow.py:370-390` y `task-brief.py:723-743`; `jira-flow.py:764-766`). Reabre #1 | T-03 | corregido: `evidencia_aprobado`/`construir_plan` (`jira-flow.py`) y `_gaps_pendientes_de_tarea` (`task-brief.py`) filtran filas por `ids_de_tarea(f["tarea"]) & tareas_set` (regex `\bT-\d{2}\b`), no por igualdad de texto — una celda `T-02/T-04` cuenta para ambas | fixture FX2: `test_fx2_gaps_fila_combinada_aparece_en_el_brief_de_t04`, `test_fx2_gaps_fila_combinada_aparece_tambien_en_el_brief_de_t02` -> passed |
| 15 | **Critical** | El filtro de pendientes de `aprobado` (`jira-flow.py:655`) y de `task-brief.py:794` usa `==`: una fila `T-02/T-04` **Critical** no cuenta para T-04 → Done concedido con un Critical vivo; el brief de redespacho no la muestra | T-03 | corregido: mismo `_ids_tarea`/`ids_de_tarea_fn` de #14, usado en `evidencia_aprobado` (`pendientes = [... _ids_tarea(ledger_lint, f["tarea"]) & tareas_set and _gap_pendiente(f)]`) — `aprobado` rechaza para T-02 Y T-04 por la fila combinada pendiente | fixture FX2: `test_fx2_aprobado_t04_rechaza_por_la_fila_combinada_pendiente`, `test_fx2_aprobado_t02_rechaza_por_la_misma_fila_combinada`, control `test_fx2_aprobado_t05_no_se_bloquea_por_el_critical_de_otras_tareas` -> passed |
| 16 | **Critical** | Atajo en `ultimo_intento_para` (`ledger-lint.py:316-317`: «si no hay intentos repetidos, `max` global») contradice el arbitraje escrito: con numeración continua entre fases `aprobado T-01` coge la Fase 2 «sin gaps» y aprueba Done con el Critical de la Fase 1 vivo; además deja inalcanzable el aviso de #4. Reabre #2 y #4 | T-03 | corregido: quitado el atajo; `ultimo_intento_para` usa `_menciones_efectivas()` (herencia SOLO para cierres anónimos sin frontera estructural con la sección previa) y calcula el máximo intento SOLO entre secciones con mención efectiva de la tarea pedida — nunca el máximo global. Regresión del caso ordinario (cierre «sin gaps» de una sola fase) cubierta por `test_aprobado_lo_dispara_el_orquestador_con_evidencia_y_cierra_firmando` y `test_gaps_fase_con_menos_intentos_no_queda_tapada_por_otra_fase` (`test_task_brief.py`) | fixture FX3: `test_fx3_aprobado_t01_rechaza_citando_el_critical_de_la_fase_1` (mutando el atajo antiguo, este test pasa a fallar: `assert 0 == 2`, confirmado matando el mutante), control `test_fx3_aprobado_t04_usa_su_propio_intento_2_sin_gaps` -> passed |
| 17 | Important | `test_mencion_no_se_cuela_desde_correccion_ni_evidencia` (`test_jira_flow.py:534-545`) no contiene ningún `T-XX` en `Corrección`/`Evidencia`: no mata M3 | T-03 | corregido: nuevo fixture `ledger_mencion_venenosa` con `Corrección`/`Evidencia` citando literalmente `T-04` (tarea real del ledger, nunca en cabecera ni columna Tarea) — matado confirmado por mutación manual de `menciones()` para escanear esas columnas: `test_mencion_venenosa_no_permite_aprobar_una_tarea_nunca_revisada` pasa de rechazar (exit 2) a aprobar a ciegas (exit 0) | `test_mencion_venenosa_en_correccion_y_evidencia_no_cuenta_como_mencion_de_esa_tarea`, `test_mencion_venenosa_no_permite_aprobar_una_tarea_nunca_revisada` -> passed; suite combinada 148 passed, 1 xfailed |
| 18 | Minor | El guardarraíl no cubre `docs/FLOWS.md` ni `docs/en/FLOWS.md` (el contenido de #6 sí se corrigió) | T-02 | corregido: `FLOWS_ES`/`FLOWS_EN` añadidos al barrido de «Paso 9» y nueva `test_flows_es_y_en_nombran_a_la_skill_en_la_tabla_de_eventos` que exige `adversarial-review` en las filas `revision`/`gaps` de las dos tablas | `python -m pytest tests/test_review_jira_owner.py` → 12 passed |
| 19 | Minor | `CONTINUE-HERE.md:83` sigue diciendo «comentario FINAL en Jira, Paso 9» (fichero vivo, primero que se lee al retomar) y el test solo recorre 6 rutas fijas | T-01/T-02 | corregido: `CONTINUE-HERE.md` anota **RESUELTO** junto a la referencia histórica (no se reescribe el registro, se añade la resolución) y `test_paso_9_no_aparece_en_ningun_md_vivo_del_repo` recorre TODO `.md` vivo (excepto `docs/roadmap/`, `docs/knowledge/`, `CHANGELOG*`, `interop/`), exigiendo `RESUELTO` en la ventana de contexto si «Paso 9» aparece junto a jira-sync/adversarial-review/Jira — matado confirmado deshaciendo la nota `RESUELTO`: el test pasa a fallar citando las dos líneas | `python -m pytest tests/test_review_jira_owner.py -k paso_9` → passed (y falla si se revierte la nota) |

Nota metodológica anotada: `jira-flow plan` escribe `jira-state.json` (idempotencia) — los tests y probes deben usar `--force` o estado limpio.

## Revisión de dos lentes — intento 3: Fases 1-2 (T-01, T-02, T-03, T-04; último del bucle) — 5/6 gaps del intento 2 cerrados (#15 parcial); 5 gaps NUEVOS (1 Critical, 2 Important, 2 Minor), lentes A+B fusionadas, rango `c345409..f17bbc8`

Puertas: 226 passed, 1 xfailed · `test_ledger_lint` 27/27 · lint 0 · evals 0 · interop al día · `ledger-lint` 0/0 en ambos ledgers · scope exit 0 · fila en `docs/roadmap/README.md`. Aceptación sobre el ledger REAL (ejecutada a mano por la lente, no por la suite → #22): `gaps T-04 --intento 1` → 16 filas · `--intento 2` → 12 filas de la Fase 2 (la 13.ª es solo de T-05/T-06: la cifra del arbitraje era el total de la sección) · `--intento 3` → Fase 2 intento 3 · `T-10 --intento 3` → Fase 4 · `revision T-04 --intento 2` → exit 2. Mutantes: igualdad exacta, M3 con veneno, M4 atajo, M2a muertos; **M2b (`ajenas[-1]→[0]`) vivo** (#21). #1, #2 y #4 del intento 1 quedan cerrados.

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| 20 | **Critical** | `evidencia_aprobado` (y el brief) inspeccionan UNA sola sección por intento: con sección propia limpia («Fase 2 (T-04, T-05) — sin gaps») y otra sección del mismo intento con un Critical cruzado (`T-04/T-07`), la regla cabecera-primero elige la limpia y `aprobado T-04` concede Done; el brief de T-04 no muestra el Critical (`jira-flow.py:407-412,722`, `ledger-lint.py:319-324`, `task-brief.py:760-765,850-855`). Reabre la mitad de #15 | T-03 | corregido: `evidencia_aprobado`/`aprobado` y `task-brief.py::_gaps_pendientes_de_tarea` usan `ledger_lint.filas_pendientes_de_tarea` — UNIÓN de TODAS las secciones que citan la tarea (en cualquier intento/fase), filtradas por `gap_pendiente()`; una sola sección propia limpia ya no oculta un Critical cruzado en otra sección | fixture FXP: `test_fxp_aprobado_t04_rechaza_por_el_critical_cruzado_de_otra_seccion_del_mismo_intento`, `test_fxp_aprobado_t07_rechaza_por_el_mismo_critical_cruzado`, control `test_fxp_aprobado_t05_no_se_bloquea_por_el_critical_cruzado_de_otra_tarea` (jira-flow) + `test_fxp_gaps_muestra_el_critical_cruzado_de_otra_seccion_del_mismo_intento` (task-brief) -> passed |
| 21 | Important | La rama «ajenas» de `seleccionar_seccion` no tiene test: mutante M2b vivo; el docstring afirma que FX2 la ejercita y es falso (su cabecera lleva `T-02/T-04`) | T-03 | corregido: fixture `LEDGER_M2B` — ninguna cabecera del intento 1 nombra T-04, dos secciones AJENAS la citan solo por fila; `seleccionar_seccion` debe traer la ÚLTIMA | `test_m2b_gaps_de_t04_elige_la_ultima_seccion_ajena_no_la_primera` -> passed; mutante `ajenas[-1]→ajenas[0]` verificado muerto a mano (falla citando la fila antigua) y revertido |
| 22 | Important | El punto (4) del arbitraje fix2 exigía tests de aceptación sobre el ledger REAL en `test_jira_flow.py`; no existe ninguno (solo docstrings); la regresión que originó la iniciativa (`--event gaps --intento 2` sobre knowledge-services) queda sin red | T-03 | corregido: `test_aprobado_sobre_el_ledger_real_de_knowledge_services_concede_done` (T-04 y T-10, Done sin bloqueo) y `test_gaps_sobre_el_ledger_real_de_knowledge_services_por_intento` (T-04 `--intento 1/2/3`, T-10 `--intento 3`) añadidos a `test_jira_flow.py` sobre una copia del ledger real | `grep -c knowledge-services skills/jira-sync/scripts/test_jira_flow.py` -> 5; `python -m pytest skills/jira-sync/scripts/test_jira_flow.py -q -k knowledge_services` -> 2 passed |
| 23 | Minor | Verificación de T-03 caduca («51 passed», solo `--intento 1`; real 60 passed) y sin `--intento 2/3` | T-03 | corregido: Verificación de T-03 reescrita con las cifras reales de fix3 (66 passed en `test_jira_flow.py`, 68 en `test_task_brief.py`) y cubriendo `--intento 1/2/3` sobre el ledger real | Verificación de T-03, abajo |
| 24 | Minor | Divergencia no declarada: el arbitraje fix2 (2) decía FILAS-primero y el código hace cabecera-primero (lo que salva las aceptaciones); el ledger no registra el cambio de criterio | T-03 | corregido por declaración: arbitraje fix3 (abajo) fija cabecera-primero para PUBLICAR (`revision`/`gaps`) como criterio vigente, distinto del criterio de UNIÓN para DECIDIR (`aprobado`/brief) | Arbitraje fix3, abajo |

**Decisión del orquestador al 3.er intento con un Critical (regla del bucle: «PARA y pregunta»).** El usuario fijó el objetivo en modo autónomo y no está disponible; la rama vive en `fix/jira-review-comments` sin mezclar y el defecto es de DISEÑO del arbitraje, no de la implementación (elegir una sección por intento es incompatible con gaps cruzados entre fases). Como en knowledge-services Fase 3, se elige **seguir con UNA ronda `fix3` de diseño corregido y una verificación dirigida** (no una 4.ª pasada completa), dejándolo registrado para que el usuario lo revoque en el PR.

**Arbitraje fix3 (sustituye a (2) del fix2 donde difiera).** Dos usos, dos reglas:
- **Publicar un intento** (`revision`/`gaps`, comentario en Jira): sigue siendo POR SECCIÓN, con la selección vigente **cabecera-primero** (declarada aquí como criterio: la cabecera nombra la fase dueña; las filas cruzadas de otra fase no cambian de dueño la publicación), última si varias; sin cabecera propia, la última con filas de la tarea; sin nada, la última del intento con aviso. #24 queda así resuelto por declaración.
- **Decidir sobre una tarea** (`aprobado` → Done; `task-brief` gaps pendientes; `ledger-lint` si lo usa): UNIÓN de TODAS las filas de TODAS las secciones de revisión que citen la tarea (`ids_de_tarea(celda Tarea)`), en cualquier intento; una fila cuenta como pendiente si su `Corrección` no empieza por `corregido`, `descartado (rebatido)` ni `deuda aceptada` (misma lista que ya usa `evidencia_aprobado`); `aprobado` rechaza si hay alguna pendiente, citando fila y sección; el intento que se reporta es el máximo entre las secciones que citan la tarea; sin ninguna sección que la cite → rechazo con razón. El brief inyecta esa unión agrupada por sección.
- **Tests obligatorios:** FXP (sección propia limpia + Critical cruzado en otra sección → `aprobado T-04` rechaza; el brief de T-04 lo muestra; control T-07); fila cruzada ya `corregido:` no bloquea; M2b muere (fixture sin cabecera propia); aceptación sobre el ledger REAL en `test_jira_flow.py` (`--intento 1/2/3` para T-04, `T-10 --intento 3`, `revision --intento 2` exit 2) con copia a tmp y `--force`; Verificación de T-03 reescrita con cifras reales.

**Cierre fix3 (ronda ejecutada, gaps #20-#24 cerrados).** `filas_pendientes_de_tarea(secciones, tarea)` (canónico en `ledger-lint.py`, respaldo declarado en `jira-flow.py`/`task-brief.py` vía `copias.json`) implementa la regla de UNIÓN de #20: recorre TODAS las secciones que citan la tarea (cabecera o columna `Tarea`, en cualquier intento/fase) y devuelve solo las filas SIN cerrar (`gap_pendiente()`: ni `corregido`, ni `descartado (rebatido)`, ni un valor vacío/plantilla como «pendiente»/«—»/«?» sin explicar). `evidencia_aprobado` y `_gaps_pendientes_de_tarea` delegan en ella; `seleccionar_seccion` (usada SOLO para publicar `revision`/`gaps`, #24) no cambió. Gap #21 (mutante M2b vivo) cerrado con fixture `LEDGER_M2B` (dos secciones ajenas, ninguna propia). Gap #22 (sin red sobre el ledger real en `test_jira_flow.py`) cerrado con dos tests de aceptación nuevos. Gap #23 (Verificación caduca) reescrito arriba con cifras reales. Todos los tests verificados en verde: 66 passed (`test_jira_flow.py`), 67 passed + 1 xfailed (`test_task_brief.py`), 0 incoherencias/avisos (`ledger-lint.py` sobre este ledger), exit 0 (`test_copias_declaradas.py`). No hubo un 4.º intento de revisión adversarial (autónomo, sin usuario disponible): esta nota documenta la verificación dirigida de la propia ronda fix3, a revocar/ampliar por el usuario en el PR si lo considera insuficiente.
