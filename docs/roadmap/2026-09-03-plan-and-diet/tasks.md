---
tasks: plan-and-diet
descripcion: Cerrar tres brechas frente al plugin superpowers con la vía rápida — (1) token-diet de las 3 skills grandes (`jira-sync`, `confluence-publish`, `cybersecurity`) con progressive disclosure a `references/` y CERO pérdida de contenido; (2) campo `Verificación` obligatorio por tarea en la plantilla del planner, validado por `ledger-lint.py` cuando el ledger declara `verificacion: obligatoria` e inyectado por `task-brief.py`; (3) CI headless opcional (`headless.yml`) que corre un subconjunto de evals y comprueba los hooks del plugin en una sesión real de `claude -p` — más doc, ADR-008 y cierre.
estado: completado        # borrador | en-progreso | completado | cancelado
creado: 2026-09-03
actualizado: 2026-09-03
via: rapida               # vía rápida de /dev-cycle: sin spec/evaluación/plan; conserva revisión de dos lentes + suites
verificacion: obligatoria # cada `### T-XX` lleva `- **Verificación**:`; ledger-lint lo exige (exit ≠ 0 si falta) — dogfooding de T-02
generacion:
  fuente: estimado        # usage-meter no disponible en este entorno (sandbox cloud)
---

# Checklist de Tareas — plan-and-diet (vía rápida)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-03 |
| **Plan** | n/a — **vía rápida** (sin spec/evaluación/plan; ledger ligero + revisión de dos lentes + verificación) |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del cambio. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Decisión del usuario (2026-09-03).** Tras `activation-reliability`, quedan tres patrones de superpowers que este plugin aún no aplica: SKILL.md cortos con detalle bajo demanda (aquí 3 skills pasan de 289/432/976 líneas), «verification steps» explícitos por tarea (aquí los criterios de aceptación no siempre dicen QUÉ comando ejecutar) y una comprobación de los hooks en sesión real (hoy es una lista manual en `docs/observability.md`). Se cierran en una iniciativa de vía rápida: T-01 dieta, T-02 verificación, T-03 CI headless, T-04 doc/ADR. `CHANGELOG*.md` lo toca el orquestador.

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase única — plan y dieta | 4 | 4 | 100% | 7,8 / 8,0h | 0,41 / 0,42h | 0,09 / 0,09h | 165k / 170k |
| **TOTAL** | **4** | **4** | **100%** | **7,8 / 8,0h** | **0,41 / 0,42h** | **0,09 / 0,09h** | **165k / 170k** |

---

## Fase única — plan y dieta

**Estado**: completado · **Estimado**: 8,0h · **Real**: 7,8h (estimado) · **Coste est.**: ≈415 € · **Tokens est.**: 170k

### T-01 — Token-diet de las 3 skills grandes (progressive disclosure)

- **Descripción**: `skills/jira-sync/SKILL.md` (289 líneas), `skills/confluence-publish/SKILL.md` (432) y `skills/cybersecurity/SKILL.md` (976) se inyectan COMPLETOS en el contexto cada vez que se invocan; el detalle (plantillas de comentario, casuística del tope de jornada, tablas de campos de config, política normativa de publicación, los 8 prompts de especialistas, catálogos OWASP/CWE/ATT&CK, estrategia de codebase grande) solo hace falta en un paso concreto. Se aplica el patrón superpowers: el `SKILL.md` queda en ≤ 200 líneas con frontmatter, propósito, disparadores, el flujo de pasos (título + 1-2 líneas), guardrails/invariantes, «qué NO hace» y una **tabla de referencias** con la instrucción explícita «lee X solo cuando llegues al paso Y»; cada bloque movido reaparece **literal** en `skills/<skill>/references/<tema>.md` (`cybersecurity` ya tiene `references/`: se reutiliza sin duplicar). Los nombres de paso que citan otras piezas (`Paso 7`, `Paso 9`, `Paso 0-ter` de `jira-sync`; «qué sube y qué no», «Publicar» de `confluence-publish`) conservan su numeración y título. Nuevo test `tests/test_skill_size.py` (ningún `SKILL.md` > 250 líneas, umbral duro) y aviso del linter a partir de 200 (`SKILL_WARN_LINES`). `docs/CONVENTIONS.md` (+EN) gana la regla de skills cortas; `skills/plugin-dev/SKILL.md` Paso 2 la cita. Script de «cero pérdida» (`tests/test_skill_size.py --diet-check <skill> <git-ref>`, reutilizable): todo párrafo de > 80 caracteres del `SKILL.md` original está, literal, en el nuevo `SKILL.md` o en `references/`.
- **Estado**: completado
- **Tiempo humano**: est. 3,0h · real 3,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,16h · real 0,16h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/jira-sync/SKILL.md`, `skills/jira-sync/references/` (nuevo), `skills/confluence-publish/SKILL.md`, `skills/confluence-publish/references/` (nuevo), `skills/cybersecurity/SKILL.md`, `skills/cybersecurity/references/agent-prompts.md` (nuevo), `skills/cybersecurity/references/recon-and-scope.md` (nuevo), `skills/cybersecurity/references/large-codebase.md` (nuevo), `skills/cybersecurity/references/aggregation-and-report.md` (nuevo), `skills/confluence-pull/SKILL.md` (puntero a la referencia), `tests/test_skill_size.py` (nuevo), `scripts/lint_plugin.py`, `tests/test_lint_plugin.py`, `docs/CONVENTIONS.md`, `docs/en/CONVENTIONS.md`, `skills/plugin-dev/SKILL.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `149 / 196 / 167` · `test_skill_size: 12/12 OK` · `0 párrafos perdidos` ×3 · `evals/check … 0 errores` · anclajes `Paso 0-ter`/`7`/`8`/`9` y `Publicar`/`Estado de sincronización`/`Qué sube y qué no` presentes): `wc -l skills/jira-sync/SKILL.md skills/confluence-publish/SKILL.md skills/cybersecurity/SKILL.md` → las tres ≤ 200 líneas · `python3 tests/test_skill_size.py` → `test_skill_size: N/N OK` · `for s in jira-sync confluence-publish cybersecurity; do python3 tests/test_skill_size.py --diet-check $s 12beca3; done` → `0 párrafos perdidos` en las tres · `python3 evals/check.py` → exit 0 · `grep -rn "Paso 7\|Paso 9" agents commands skills/adversarial-review` → los pasos citados existen con el mismo número en `skills/jira-sync/SKILL.md`.

**Criterios de aceptación**
- [x] `wc -l` de los tres `SKILL.md` ≤ 200 líneas; el resto de `SKILL.md` no cambia de tamaño salvo los editados a propósito y declarados en `Archivos` (`plugin-dev` 136→141 por el Paso 2; `confluence-pull` 137→138 por el puntero a la referencia); `tests/test_skill_size.py` pasa (umbral duro 250) y `scripts/lint_plugin.py` avisa a partir de 200 líneas (caso 21 en `tests/test_lint_plugin.py`: skill sintética de 201 líneas → aviso `SKILL.md de 201 líneas (> 200)`; de 150 → sin aviso). **Medido:** `jira-sync` 289→149 · `confluence-publish` 432→196 · `cybersecurity` 976→167 líneas; `test_skill_size: 12/12 OK`; `test_lint_plugin: 21/21 OK`; el linter avisa ahora de `adversarial-review` (203 líneas) — aviso legítimo, siguiente candidata a dieta (fuera de alcance).
- [x] Cero pérdida: `tests/test_skill_size.py --diet-check <skill> 12beca3` → `0 párrafos perdidos` para las tres skills (todo párrafo > 80 caracteres del original, normalizado por espacios, aparece literal en el `SKILL.md` nuevo o en `references/*.md`). **Medido:** 141 / 181 / 110 párrafos comprobados, 0 perdidos en cada una.
- [x] Cada `SKILL.md` conserva frontmatter idéntico (`git diff 12beca3 -- <SKILL.md>` no toca las líneas del frontmatter), y `python3 evals/check.py` → exit 0 (los `trigger: literal` siguen casando con la description). **Medido:** frontmatter byte-idéntico (md5 de las primeras 15/15/26 líneas) en las tres; `evals/check: 31 ficheros · 96 casos · 0 errores`.
- [x] Cada `SKILL.md` tiene una sección `## Referencias (lectura bajo demanda)` (`## References (read on demand)` en `cybersecurity`, skill en inglés) con una fila por fichero de `references/` que indica en qué paso se lee; ninguna referencia nueva queda sin fila; `references/` no contiene ficheros duplicados entre sí (cybersecurity reutiliza los suyos).
- [x] Los anclajes citados desde fuera se mantienen: `skills/jira-sync/SKILL.md` conserva los headings `Paso 0-ter`, `Paso 7`, `Paso 8`, `Paso 9` y `Config .claude/jira.json`; `skills/confluence-publish/SKILL.md` conserva `Publicar`, `Estado de sincronización`, `Qué sube y qué no`; `skills/confluence-pull/SKILL.md` apunta a la referencia con la tabla completa.
- [x] `docs/CONVENTIONS.md` y `docs/en/CONVENTIONS.md` tienen la regla «SKILL.md corto (≤ 200 líneas, umbral duro 250) + `references/` bajo demanda con tabla de referencias»; `skills/plugin-dev/SKILL.md` Paso 2 la cita en la parte de **Skill**.

### T-02 — Campo `Verificación` obligatorio por tarea

- **Descripción**: hoy una tarea puede estar «bien definida» sin decir qué comando la demuestra. Se añade a cada `### T-XX` el campo `- **Verificación**: \`<comando>\` → <resultado esperado>` (uno o varios; para prosa: «lectura: <qué comprobar>»). Plantilla `agent-kits/planner/templates/tasks.md` (frontmatter con `verificacion: obligatoria` + campo en cada bloque de tarea) y la variante ligera de la vía rápida descrita en `commands/dev-cycle.md` Fase 0-bis. `agents/planner.md` P3 + DoD lo exigen («sin verificación ejecutable la tarea no está bien definida»). `ledger-lint.py`: si el frontmatter lleva `verificacion: obligatoria`, una tarea sin `Verificación` es incoherencia dura (exit 1); sin la clave, solo aviso (los 20 ledgers previos no la llevan → salida idéntica). `task-brief.py` incluye el campo en el brief («al terminar ejecuta la verificación y pega la salida»). `agents/implementer.md` P3 + DoD: «ejecuta la Verificación de la tarea y pega la salida en el ledger» (una línea). `skills/adversarial-review/SKILL.md` Lente A: comprueba que la verificación declarada se ejecutó (evidencia en el ledger). Este ledger la lleva (dogfooding).
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 1,8h (estimado)
- **Tiempo IA (ejec.)**: est. 0,10h · real 0,09h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `agent-kits/planner/templates/tasks.md`, `agents/planner.md`, `commands/dev-cycle.md`, `agent-kits/shared/ledger-lint.py`, `tests/test_ledger_lint.py`, `agent-kits/shared/task-brief.py`, `agent-kits/shared/test_task_brief.py`, `agents/implementer.md`, `skills/adversarial-review/SKILL.md`, `docs/roadmap/2026-09-03-plan-and-diet/tasks.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `test_ledger_lint: 14/14 OK` · `19 passed` en `test_task_brief.py` · `cmp` de las salidas old/new sobre los 20 ledgers previos → `IDENTICAL` · este ledger `0 incoherencias · 0 avisos`; sin la línea de T-04 → `❌ T-04: sin campo **Verificación** (el ledger declara verificacion: obligatoria)` exit 1): `python3 tests/test_ledger_lint.py` → `test_ledger_lint: 14/14 OK` · `python3 -m pytest -q agent-kits/shared/test_task_brief.py` → passed con el test nuevo `test_brief_incluye_verificacion` · `for f in docs/roadmap/*/tasks.md; do python3 agent-kits/shared/ledger-lint.py "$f"; done` → salida byte-idéntica a la de `git show 12beca3:agent-kits/shared/ledger-lint.py` en los 20 ledgers previos · `python3 agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-03-plan-and-diet/tasks.md` → exit 0, y borrar una línea `Verificación` de este ledger → exit 1 con `sin campo **Verificación**`.

**Criterios de aceptación**
- [x] `tests/test_ledger_lint.py` gana 3 casos: (12) frontmatter `verificacion: obligatoria` + tarea sin `Verificación` → exit 1 y mensaje `T-XX: sin campo **Verificación** (el ledger declara verificacion: obligatoria)`; (13) mismo ledger sin la clave → exit 0 con aviso `⚠️  T-XX: sin campo **Verificación**`; (14) con la clave y todas las tareas con el campo → exit 0 sin ese aviso. Salida `test_ledger_lint: 14/14 OK`. **Medido:** 14/14 OK (el caso 12 cubre también la variante `- **Verificación** (ejecutada …): …`).
- [x] Salida de `ledger-lint.py` byte-idéntica a la versión `12beca3` en los 20 ledgers previos (`docs/roadmap/*/tasks.md` excepto este) — `diff` estricto de las dos salidas concatenadas. **Decisión de diseño:** el aviso «sin clave» se emite solo cuando el ledger ya USA el campo en alguna tarea (adopción parcial: `T-XX: sin campo **Verificación** (otras tareas lo declaran)`) o cuando el frontmatter lleva `verificacion:` con un valor distinto de `obligatoria`; un ledger legacy sin clave y sin ningún `Verificación` calla — así los 20 previos no cambian ni una línea y la regla «sin la clave, solo aviso» se cumple donde tiene sentido. **Medido:** `cmp old.txt new.txt` → idénticos (20 ledgers, `git show 12beca3:agent-kits/shared/ledger-lint.py` vs actual).
- [x] `task-brief.py`: el brief de una tarea con `Verificación` incluye la sección `## Verificación (ejecútala al terminar y pega la salida)` con el comando literal; sin el campo, incluye una nota «(la tarea no declara Verificación: propón una en tu informe)». Tests `test_brief_incluye_verificacion` y `test_brief_sin_verificacion_avisa` en `agent-kits/shared/test_task_brief.py`. **Medido:** 19 passed (17 + 2); el contrato `DONE` exige «Verificación ejecutada con su salida real pegada».
- [x] `agent-kits/planner/templates/tasks.md` lleva `verificacion: obligatoria` en el frontmatter y `- **Verificación**:` en los 4 bloques de tarea; `agents/planner.md` P3 y DoD la exigen; `commands/dev-cycle.md` Fase 0-bis dice que el `tasks.md` ligero también la lleva.
- [x] `agents/implementer.md` P3 y DoD contienen la línea de ejecutar la Verificación y pegar la salida; `skills/adversarial-review/SKILL.md` Lente A comprueba (5) que la Verificación declarada se ejecutó; `tests/test_rationalization_tables.py` sigue en verde (la tabla no cambia de sitio). **Medido:** 6 tests OK; `adversarial-review/SKILL.md` sigue en 203 líneas (el punto (5) se añadió en la misma línea de la Lente A).
- [x] Este ledger: `verificacion: obligatoria` en el frontmatter, campo en las 4 tareas, `ledger-lint` exit 0.

### T-03 — CI headless opcional (hooks reales + evals)

- **Descripción**: la CI actual (`ci.yml`) no ejecuta `claude`: los evals de activación corren solo en local y la comprobación «los hooks se registran y se muestran en una sesión real» es la lista manual de `docs/observability.md`. Nuevo workflow `headless.yml.MANUAL-COPY` (+ copia en `.github/workflows/headless.yml`, vigilada por `tests/test_ci_manual_copy.py` como `ci.yml`): `workflow_dispatch` + `schedule` semanal (lunes 06:00 UTC); el secret `ANTHROPIC_API_KEY` se pasa a `env` del job y cada paso que lo necesita lleva `if: env.ANTHROPIC_API_KEY != ''` (patrón oficial de GitHub: los secrets no pueden ir en `if:` directamente — docs.github.com «Using secrets in conditional statements», verificado 2026-09-03); sin secret el job termina en verde con un aviso. Con secret: instala Claude Code (`npm install -g @anthropic-ai/claude-code`), (a) `python3 evals/run.py --bare --max-turns 4 --target skill:quick-implement --target command:setup` (subconjunto barato; `--bare` exige `ANTHROPIC_API_KEY` — headless.md verificado 2026-09-03; `run.py` gana `--target` repetible), (b) comprobación de hooks reales: `claude -p --bare --plugin-dir . --output-format stream-json --verbose --permission-mode acceptEdits --max-turns 6 "Edita docs/roadmap/2026-01-01-demo/tasks.md marcando T-01 completado"` sobre una copia del fixture `evals/fixtures/project`; **evidencia = fichero-testigo** `.claude/.progress-last` escrito por `hooks/progress-line.sh` (la doc de stream-json solo documenta eventos `hook_*` para `SessionStart`/`Setup`; no garantiza que el `systemMessage` de un `PostToolUse` aparezca en la salida) y, además, el `system/init` de la salida debe listar el plugin en `plugins` (contrato documentado: «Fail CI when a plugin doesn't load»). Sube `evals/reports/*.json` como artifact. `evals/README.md` (ES+EN) y `docs/observability.md` (+EN) documentan que este job cierra la verificación manual. `scripts/lint_plugin.py` ya generaliza el aviso de copia manual a `*.yml.MANUAL-COPY` (verificado; se añade caso de test con `headless.yml`).
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 2,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,10h · real 0,10h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `headless.yml.MANUAL-COPY` (nuevo), `.github/workflows/headless.yml` (nuevo), `tests/test_ci_manual_copy.py`, `evals/run.py`, `evals/test_evals.py`, `evals/README.md`, `docs/observability.md`, `docs/en/observability.md`, `tests/test_lint_plugin.py`
- **Verificación** (ejecutada 2026-09-03 — salida: `cmp` sin salida · `test_ci_manual_copy: 3/3 OK` (7.472 bytes idénticos) · YAML parseado con PyYAML: `on` = `workflow_dispatch`+`schedule`, `env.ANTHROPIC_API_KEY` a nivel de job, 8 `if:` sobre `env.` y ninguno sobre `secrets.` · dry-run → 7 comandos · `23 passed` en `pytest -q evals` · el script del paso de hooks se ejecutó en local con un `claude` falso en los dos escenarios (bare con testigo → OK; bare sin testigo → reintento sin --bare → OK con `::warning::`)): `cmp headless.yml.MANUAL-COPY .github/workflows/headless.yml` → sin salida (idénticos) · `python3 tests/test_ci_manual_copy.py` → `test_ci_manual_copy: 3/3 OK` · `python3 -c "import yaml,sys; yaml.safe_load(open('headless.yml.MANUAL-COPY'))"` (o `python3 tests/test_ci_manual_copy.py` que parsea el YAML si PyYAML está) → sin error · `python3 evals/run.py --dry-run --target skill:quick-implement --target command:setup | grep -c "claude -p"` → 7 (4 + 3 casos) · `grep -n "if: env.ANTHROPIC_API_KEY != ''" headless.yml.MANUAL-COPY` → ≥ 3 pasos · `python3 -m pytest -q evals` → passed.

**Criterios de aceptación**
- [x] `headless.yml.MANUAL-COPY` existe con `on: workflow_dispatch` + `schedule` (cron semanal), `env: ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}` a nivel de job, un paso «Sin secret → aviso» con `if: env.ANTHROPIC_API_KEY == ''`, y los pasos de instalación de Claude Code, evals (`--bare --max-turns 4 --target skill:quick-implement --target command:setup`), hooks reales (fichero-testigo `.claude/.progress-last` + `plugins` en `system/init`) y `actions/upload-artifact@v4` de `evals/reports/*.json` con `if: env.ANTHROPIC_API_KEY != ''`; ningún `if:` referencia `secrets.` directamente (`grep -n "if:.*secrets\." headless.yml.MANUAL-COPY` vacío). **Medido:** grep vacío (exit 1); pasos con `if: env.ANTHROPIC_API_KEY != ''` = 7 + `always() && …` en el upload.
- [x] `.github/workflows/headless.yml` es byte-idéntico (`cmp`), `tests/test_ci_manual_copy.py` incluye `headless.yml` en `COPIAS` (3/3 OK) y `lint_plugin.py` no avisa «difieren».
- [x] `evals/run.py --target` acepta varios valores (`action="append"`); `--dry-run` con los dos targets imprime exactamente los casos de esos dos ficheros; test `test_run_target_repetible` en `evals/test_evals.py`; el resto de la suite de evals sigue en verde. **Medido:** 7 comandos con los dos targets (4 + 3), 4 con uno solo, 96 sin filtro; `23 passed`.
- [x] `evals/README.md` (ES y EN) y `docs/observability.md` + `docs/en/observability.md` explican el job: qué corre, cómo se activa (secret), por qué la evidencia de hooks es el fichero-testigo y no `systemMessage` en stream-json, y que la lista manual sigue valiendo para instalaciones locales.
- [x] `tests/test_lint_plugin.py` caso 22: `headless.yml.MANUAL-COPY` distinto de `.github/workflows/headless.yml` → aviso con `cp headless.yml.MANUAL-COPY .github/workflows/headless.yml` (el aviso ya está generalizado a `*.yml.MANUAL-COPY`; el test lo fija). **Medido:** `test_lint_plugin: 22/22 OK`.

### T-04 — Doc, memoria y cierre

- **Descripción**: `docs/README.md` + `docs/en/README.md` (filas de `jira-sync`/`confluence-publish`/`cybersecurity` mencionan `references/`; fila `headless.yml`), `CLAUDE.md` (regla «Skills cortas» y verificación por tarea en «Determinismo»; `headless.yml.MANUAL-COPY` en el árbol), `agent-kits/shared/README.md` (filas de `ledger-lint.py` y `task-brief.py` con el campo `Verificación`), fila de la iniciativa en `docs/roadmap/README.md`. ADR-008 `propuesta` («SKILL.md ≤ 200 líneas + `references/` bajo demanda»: cierra la alternativa «todo en el SKILL.md» y afecta a 3+ piezas → cruza el umbral de `knowledge-write.md`; siguiente NNN libre = 008) + fila en `docs/knowledge/README.md`.
- **Estado**: completado
- **Tiempo humano**: est. 1,0h · real 1,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,06h · real 0,06h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `docs/README.md`, `docs/en/README.md`, `CLAUDE.md`, `agent-kits/shared/README.md`, `docs/roadmap/README.md`, `docs/knowledge/adr/ADR-008-skill-md-corto-con-references-bajo-demanda.md` (nuevo), `docs/knowledge/README.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `1` · `1` · `1` · `grep references/` → 6 líneas en los 3 ficheros · `lint_plugin: 8 agentes · 0 errores · 4 avisos` · `248 passed` · `scope-check … 47 fichero(s) … fuera de alcance (0)` exit 0): `ls docs/knowledge/adr/ | grep -c ADR-008` → 1 · `grep -c "ADR-008" docs/knowledge/README.md` → ≥ 1 · `grep -c "plan-and-diet" docs/roadmap/README.md` → ≥ 1 · `grep -n "references/" docs/README.md docs/en/README.md CLAUDE.md | wc -l` → ≥ 3 · `python3 scripts/lint_plugin.py` → `0 errores` · `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → todo passed · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-plan-and-diet --base 12beca3` → exit 0.

**Criterios de aceptación**
- [x] `docs/knowledge/adr/ADR-008-skill-md-corto-con-references-bajo-demanda.md` existe con el frontmatter de la plantilla (`id: ADR-008`, `estado: propuesta`, `iniciativa: plan-and-diet`), contexto/decisión/alternativas descartadas («todo en SKILL.md»; «umbral solo en el linter sin test duro»)/consecuencias, y fila en `docs/knowledge/README.md`.
- [x] `docs/README.md` y `docs/en/README.md`: las filas de las tres skills citan `references/`; hay fila/mención de `headless.yml.MANUAL-COPY`; `CLAUDE.md` tiene la regla de skills cortas (≤ 200 líneas + references) y menciona la `Verificación` por tarea y el job headless.
- [x] `agent-kits/shared/README.md`: `ledger-lint.py` menciona `verificacion: obligatoria`; `task-brief.py` menciona la sección de Verificación del brief.
- [x] `docs/roadmap/README.md` tiene la fila `2026-09-03 plan-and-diet`.
- [x] DoD global: `pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` verde; `lint_plugin.py` 0 errores (avisos listados en el cierre); `evals/check.py` exit 0; `ledger-lint` exit 0 aquí; `scope-check --base 12beca3` exit 0; `cmp` de las dos copias manuales de `ci.yml` y `headless.yml`. **Medido (cierre):** `248 passed` (229 previos + 19 nuevos: 15 `test_skill_size`, 2 `test_task_brief`, 1 `test_evals`, 1 parametrizado `test_ci_manual_copy`); suites-script de `tests/` OK (`test_ledger_lint 14/14`, `test_lint_plugin 22/22`, `test_ci_manual_copy 3/3`, `test_skill_size 12/12`); `lint_plugin` 0 errores · 4 avisos = 3 previos (`retro`/`roadmap-status`/`setup` genéricos) + 1 nuevo legítimo (`adversarial-review` 203 líneas > 200); `evals/check` 0 errores; `ledger-lint` 0/0 aquí y salida byte-idéntica (`cmp`) en los 20 previos frente a `12beca3`; `scope-check --base 12beca3` 47 ficheros en alcance, 0 fuera; `cmp` de `ci.yml` y `headless.yml` sin salida.

---

**Verificación de cierre (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → 248 passed · `for t in tests/test_*.py; do python3 "$t"; done` → todas OK · `lint_plugin` 0 errores · 4 avisos (3 previos + `adversarial-review` 203 líneas) · `evals/check.py` exit 0 · `ledger-lint` 0/0 aquí y `cmp` idéntico en los 20 ledgers previos (old vs new) · `scope-check --base 12beca3` exit 0 (47 en alcance, 0 fuera) · `cmp ci.yml`/`headless.yml` idénticos · `wc -l`: 289→149 · 432→196 · 976→167 · `--diet-check` 0 párrafos perdidos ×3. Pendiente de la **revisión de dos lentes** (la lanza el orquestador). `CHANGELOG*.md` sin tocar (lo hace el orquestador).

## Revisión de dos lentes — intento 1: 0 Critical · 2 Important · 3 Minor (todos verificados y corregidos en `T-fix1`)

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| 1 | Important | `ledger-lint.py` vs `task-brief.py`: `- **Verificación**:` + sub-lista multilínea → el lint decía «sin campo» (exit 1 con `obligatoria`, mensaje engañoso) y task-brief inyectaba solo el primer ítem (`\s*` del regex saltaba el salto de línea) | T-02 | **Reproducido** (ledger de juguete con 2 ítems: lint `❌ T-01: sin campo`, brief con 1 ítem). Parser único `parse_verificacion()` en `ledger-lint.py` (importable; task-brief lo importa por ruta con fallback local): acepta en línea (` · `), sub-lista (`  - ítem` indentados, consumidos sin comerse los criterios) y mezcla; el lint distingue `campo **Verificación** VACÍO` de `sin campo`; task-brief inyecta TODOS los ítems (un bullet por ítem). Plantilla del planner documenta las dos formas (T-02 en sub-lista) | `test_ledger_lint` caso 15 (15/15 OK: sub-lista → 0 incoherencias, `checked == 2`; vacío → error «VACÍO») · `test_brief_verificacion_sublista_y_ejecutada` (20 passed) |
| 2 | Important | `task-brief.py`: `- **Verificación** (ejecutada … — salida: …): <cmd>` cortaba en el primer `:` (dentro del paréntesis) → inyectaba la salida grabada como comando | T-02 | **Reproducido** con `plan-and-diet T-03` (brief empezaba por `` `cmp` sin salida · `test_ci_manual_copy: 3/3 OK` ``). `VERIF_RE` consume el paréntesis balanceado (un nivel) ANTES de `:`; `verificacion_ejecutada` sale aparte; el brief añade «Verificación ya ejecutada antes (<fecha>): **re-ejecútala**» | mismo test (caso real: comando `cmp a.yml b.yml` inyectado, salida grabada ausente, nota de re-ejecución); `task-brief … T-03` ahora lista los 6 comandos |
| 3 | Minor | `confluence-publish/references/config-and-policy.md:85` «(ver bloques bash arriba)» colgante | T-01 | Frase añadida al final del bullet apuntando a `references/publish-and-sync.md` («Publicar» paso 0 y «Staging»); el texto original se conserva literal (cero pérdida) | `--diet-check confluence-publish 12beca3` → 0 párrafos perdidos |
| 4 | Minor | Ledger T-01: «el resto de SKILL.md no cambia de tamaño» era falso (`plugin-dev` 136→141, `confluence-pull` 137→138) | T-01 | Criterio reescrito: «salvo los editados a propósito y declarados en `Archivos`» con las cifras | criterio T-01 |
| 5 | Minor | `headless.yml.MANUAL-COPY`: skip sin secret frágil (un paso nuevo sin `if:` correría sin secret) | T-03 | Paso `gate` con `outputs.skip` (job `outputs.skip`), `id:` en todos los pasos y paso final «Guardia del skip» (`always() && steps.gate.outputs.skip == 'true'`) que FALLA si algún `steps.<id>.outcome != 'skipped'`; comentario de cabecera con la regla para pasos nuevos; copia `.github/workflows/headless.yml` sincronizada | guardia simulada en local: todo `skipped` → rc 0; un `success` → `::error::` rc 1 · `cmp` idéntico · `test_ci_manual_copy: 3/3 OK` · 0 `if:` con `secrets.` |

**Rebatidos:** ninguno (los 5 reproducidos o confirmados). **Verificación tras `T-fix1`:** `pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → 249 passed · suites-script OK (`test_ledger_lint 15/15`, `test_lint_plugin 22/22`, `test_ci_manual_copy 3/3`, `test_skill_size 12/12`) · `lint_plugin` 0 errores · 4 avisos (los mismos) · `evals/check` 0 errores · `--diet-check` 0 perdidos ×3 · 20 ledgers previos `cmp` idénticos (`12beca3` vs actual) · `scope-check --base 12beca3` 0 fuera · este ledger 0/0.

## Revisión de dos lentes — intento 2: sin gaps (re-verificación del orquestador: sub-lista de Verificación aceptada por lint y task-brief, «VACÍO» ≠ «sin campo», brief de T-03 inyecta los comandos y no la salida; 249 tests). ADR-008 promovida a `aceptada (validada: revisión de dos lentes, 2026-09-03, intento 2)`.
