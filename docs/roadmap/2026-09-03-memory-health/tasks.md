---
tasks: memory-health
changelog: Added        # override de la heurística de changelog-sync (la descripción menciona «retira», pero la iniciativa AÑADE piezas)
descripcion: Memoria de sesión y salud del código — (1) `session-journal`: memoria EPISÓDICA sin MCP — hook `SessionEnd` que deja una entrada determinista en `docs/knowledge/journal/AAAA-MM-DD-<slug>.md` (fecha, iniciativa activa, ficheros tocados, tareas que cambiaron de estado, marcadores del meter) con idempotencia por `session_id`, y reinyección de la última entrada al arrancar/retomar (`session-context.sh`, ≤ 25 líneas); (2) skill `code-health` con `code-health.py` (duplicados por shingles, tamaño/anidamiento/funciones largas, hotspots por git log, TODO/FIXME con antigüedad; MD + JSON + `--baseline`); (3) skill `dependency-upgrade` con `deps-inventory.py` (manifiestos + lockfiles + `outdated` oficial si la herramienta está en PATH; clasificación patch/minor/major; spec de iniciativa «upgrade-…» para evaluator/planner); (4) integración, doc bilingüe, evals, ADR-010.
estado: completado        # borrador | en-progreso | completado | cancelado
creado: 2026-09-03
actualizado: 2026-09-03
via: rapida               # vía rápida de /dev-cycle: sin spec/evaluación/plan; conserva revisión de dos lentes + suites
verificacion: obligatoria # cada `### T-XX` lleva `- **Verificación**:`; ledger-lint lo exige (exit ≠ 0 si falta)
generacion:
  fuente: estimado        # usage-meter no disponible en este entorno (sandbox cloud)
---

# Checklist de Tareas — memory-health (vía rápida)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-03 |
| **Plan** | n/a — **vía rápida** (sin spec/evaluación/plan; ledger ligero + revisión de dos lentes + verificación) |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del cambio. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Decisión del usuario (2026-09-03).** Tres huecos frente a la práctica de los mejores plugins: el plugin no tenía **memoria episódica** entre sesiones (la memoria técnica `docs/knowledge/` es curada — ADR/gotchas/lecciones — y el contexto de retoma del roadmap solo dice *qué tarea* está abierta, no *qué pasó* en la última sesión); no había ninguna medida **determinista de salud del código** (duplicados, tamaño, hotspots, TODO envejecidos) con la que `evaluator` ajuste el riesgo de una spec ni `planner` abra deuda; y no había un flujo de **actualización de dependencias** que inventaríe, clasifique el salto y redacte la spec para presupuestar — sin pisar a `nemesis` (vulnerabilidades). Se cierra en una iniciativa de vía rápida: T-01 `session-journal`, T-02 skill `code-health`, T-03 skill `dependency-upgrade`, T-04 integración/doc/memoria. `CHANGELOG*.md` lo toca el orquestador. **Contratos verificados hoy** (`https://code.claude.com/docs/en/hooks.md` y `hooks-guide.md`, 2026-09-03): `SessionEnd` recibe `session_id`, `transcript_path`, `cwd`, `hook_event_name`, `reason` ∈ `clear|resume|logout|prompt_input_exit|other` (también matcher), **no puede bloquear** («Output and exit code are ignored, except `terminalSequence`») y **todos sus hooks comparten un presupuesto de 1,5 s** que sube hasta el `timeout` por hook (máx. 60 s) → `hooks.json` declara `timeout: 20`. Los hooks `prompt`/`agent` devuelven solo `{"ok", "reason"[, "impossible"]}` (decisión), no texto para escribir a disco, y su salida en `SessionEnd` se ignora → el **resumen por IA opt-in NO se implementa** (honestidad > feature): el borrador es determinista.

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase única — memoria y salud | 4 | 4 | 100% | 9,5 / 10,0h | 0,48 / 0,50h | 0,13 / 0,13h | 235k / 240k |
| **TOTAL** | **4** | **4** | **100%** | **9,5 / 10,0h** | **0,48 / 0,50h** | **0,13 / 0,13h** | **235k / 240k** |

---

## Fase única — memoria y salud

**Estado**: completado · **Estimado**: 10,0h · **Real**: 9,5h (estimado) · **Coste est.**: ≈520 € · **Tokens est.**: 240k

### T-01 — `session-journal`: memoria episódica sin MCP

- **Descripción**: (a) `agent-kits/shared/journal.py` determinista con 4 subcomandos: `draft` (borrador de la entrada SIN modelo: `fecha`, `iniciativa` = primera activa de `progress-report.py active` sobre `docs/roadmap/`, `ficheros_tocados` = top 10 de `git status --porcelain` + `git diff --name-only HEAD`, `tareas_cambiadas` = tareas cuyo `- **Estado**:` difiere entre el `tasks.md` de trabajo y `git show HEAD:tasks.md`, `marcadores_cerrados` = marcadores de `usage-state.json` cerrados hoy, `resumen` = primer prompt del usuario de `transcript_path` si es legible (best-effort, JSONL no oficial; si no, «Sesión sobre <iniciativa>»), `decisiones`/`pendientes` vacías salvo `--enrich <json>`), `write` (escribe `docs/knowledge/journal/AAAA-MM-DD-<slug>.md` con frontmatter `fecha · session_id · iniciativa · resumen · decisiones · pendientes · ficheros_tocados · tareas_cambiadas · fuente: hook|manual`; **idempotente por `session_id`**: si ya existe una entrada con ese id la ACTUALIZA en sitio, si no crea con sufijo `-2`, `-3` cuando colisiona el nombre; regenera el índice), `latest [--n 2] [--max-lines 25]` (última entrada, o las 2 últimas si son de días distintos, compactadas a ≤ N líneas para el contexto de sesión) e `index` (regenera `docs/knowledge/journal/README.md`: tabla fecha · iniciativa · resumen · fuente). Exit 0 siempre en `draft`/`write`/`latest`/`index` salvo error de uso (2); sin git → `ficheros_tocados`/`tareas_cambiadas` vacíos con aviso; sin roadmap → `iniciativa: n/a`. (b) Hook `hooks/session-journal.sh` registrado en `hooks/hooks.json` `SessionEnd` (sin matcher: también `clear`, porque un `/clear` cierra una unidad de trabajo; `timeout: 20` para levantar el presupuesto compartido de 1,5 s), stdin `session_id`/`cwd`/`reason`; sin `python3` → nada; desactivable con `dev.json` `sesion.journal: false`; nunca exit ≠ 0. (c) `hooks/session-context.sh` inyecta `journal.py latest --n 2 --max-lines 25` SOLO en `startup|resume` (no en `compact`: el journal no cambia dentro de la sesión). (d) **Resumen por IA opt-in NO implementado**: la doc oficial (2026-09-03) dice que los hooks `prompt`/`agent` devuelven solo una decisión `ok/reason` y que en `SessionEnd` la salida se ignora — se documenta la limitación en `claude-code-contracts.md` §1 (fila `SessionEnd` + hooks `prompt`/`agent`) y en `docs/observability.md`. (e) Normativa: `docs/knowledge/README.md` enlaza `journal/`; `knowledge-write.md` (journal = memoria de SESIÓN cronológica no curada vs ADR/gotchas/lecciones curadas; una decisión del journal que cruce el umbral se PROMUEVE a ADR, no se queda ahí); `knowledge-check.md` (`evaluator`/`planner`/`architect` abren la última entrada del journal SOLO si `iniciativa` coincide con la suya); regla 10 de CONVENTIONS (+EN) y regla 9 (`sesion.journal`); política Confluence: `docs/knowledge/journal/**` EXCLUIDO (bitácora, no decisión) en `confluence.example.json`, `config-and-policy.md`, `SKILL.md`, fixture `tests/fixtures/confluence-scope/` + aserto en `test_confluence_scope.py`; `/retro` lee el journal de la iniciativa como fuente de «causas de desviación» (una línea). Tests: `agent-kits/shared/test_journal.py` (≥ 10) + 3 casos en `tests/test_hooks_shell.py` (`SessionEnd` escribe; mismo `session_id` actualiza sin duplicar; `SessionStart resume` incluye el journal ≤ 25 líneas; `compact` no).
- **Estado**: completado
- **Tiempo humano**: est. 3,5h · real 3,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,18h · real 0,17h (estimado)
- **Supervisión**: est. 0,04h · real 0,04h (estimado)
- **Archivos**: `agent-kits/shared/journal.py` (nuevo; T-fix1: guarda «rastro del plugin», slug `sesion`, nota de versionado en el índice), `agent-kits/shared/test_journal.py` (nuevo; T-fix1: +2), `agent-kits/shared/README.md`, `hooks/session-journal.sh` (nuevo), `hooks/hooks.json`, `hooks/session-context.sh`, `tests/test_hooks_shell.py`, `docs/knowledge/README.md`, `docs/knowledge/journal/README.md` (nuevo), `agent-kits/shared/knowledge-write.md`, `agent-kits/shared/knowledge-check.md`, `docs/CONVENTIONS.md`, `docs/en/CONVENTIONS.md`, `docs/observability.md`, `docs/en/observability.md`, `skills/confluence-publish/assets/confluence.example.json`, `skills/confluence-publish/references/config-and-policy.md`, `skills/confluence-publish/SKILL.md`, `tests/fixtures/confluence-scope/confluence.json`, `tests/fixtures/confluence-scope/docs/knowledge/journal/2026-01-01-demo.md` (nuevo), `tests/test_confluence_scope.py`, `commands/retro.md`, `agents/evaluator.md`, `agents/planner.md`, `agents/architect.md`, `skills/plugin-dev/references/claude-code-contracts.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `15 passed` (test_journal) · hook simulado sobre proyecto tmp con git: 1.ª ejecución `rc 0 '' ''` + `docs/knowledge/journal/2026-09-03-demo.md` con `session_id: "s1"`, `iniciativa: demo`, `ficheros_tocados` = el `tasks.md` modificado (la propia bitácora se excluye) · 2.ª ejecución mismo `session_id` → `['README.md', '2026-09-03-demo.md']` (una entrada, actualizada) · `SessionStart resume` → `additionalContext` 3.966 caracteres con bloque `Journal de sesión …` de 3 líneas (≤ 25) · `compact` → 3.695 caracteres sin `Journal` · `test_hooks_shell: 29 passed` (26 + 3) · `test_confluence_scope: 25/25 OK` (journal `[excluido: docs/knowledge/journal/**]`, fuera del staging) · `lint_plugin: 10 agentes · 0 errores · 3 avisos` (los 3 previos) · grep journal → knowledge-check 3 · knowledge-write 7 · CONVENTIONS 11 · CONVENTIONS EN 11 · retro 1 · suite completa `339 passed` (321 + 18); TDD n/a en prosa; `RED:` no registrado — los 15 tests nacieron con el script en la misma pasada y pasaron a la primera, sin evidencia de rojo): `python3 -m pytest -q agent-kits/shared/test_journal.py` → ≥ 10 passed · `echo '{"hook_event_name":"SessionEnd","session_id":"s1","reason":"other","cwd":"<proyecto-tmp>"}' | bash hooks/session-journal.sh` → exit 0 y existe `docs/knowledge/journal/<hoy>-*.md` con `session_id: s1` · segunda ejecución con el mismo `session_id` → sigue habiendo UNA entrada con `s1` (actualizada, no duplicada) · `echo '{"hook_event_name":"SessionStart","source":"resume"}' | bash hooks/session-context.sh` → `additionalContext` contiene `Journal` y el bloque del journal tiene ≤ 25 líneas · `source: compact` → sin bloque `Journal` · `python3 -m pytest -q tests/test_hooks_shell.py` → passed (+3) · `python3 tests/test_confluence_scope.py` → OK con `docs/knowledge/journal/2026-01-01-demo.md` excluido · `python3 scripts/lint_plugin.py` → 0 errores (hook existe y es ejecutable) · `grep -c "journal" agent-kits/shared/knowledge-check.md agent-kits/shared/knowledge-write.md docs/CONVENTIONS.md docs/en/CONVENTIONS.md commands/retro.md` → ≥ 1 cada uno.

**Criterios de aceptación**
- [x] `journal.py draft` sobre un proyecto temporal con git, un ledger activo y un `tasks.md` modificado devuelve JSON con `iniciativa`, `ficheros_tocados` (≤ 10) y `tareas_cambiadas` (la tarea cuyo estado cambió); sin git → listas vacías + aviso stderr, exit 0.
- [x] `journal.py write` es idempotente por `session_id` (segunda llamada actualiza el mismo fichero; distinta sesión el mismo día con la misma iniciativa → fichero `-2`) y regenera `journal/README.md` con una fila por entrada.
- [x] `journal.py latest --n 2 --max-lines 25` devuelve ≤ 25 líneas; con dos entradas del mismo día devuelve solo la última; con dos días distintos, ambas; sin carpeta → sin salida, exit 0.
- [x] `hooks.json` registra `SessionEnd` con `timeout` explícito (presupuesto compartido de 1,5 s documentado); `session-journal.sh` escribe la entrada y es exit 0 con stdin vacío, sin `python3` y con `sesion.journal: false` (sin escribir).
- [x] `session-context.sh` inyecta el journal en `startup|resume` y NO en `compact`; total bajo el tope de 9.500 caracteres.
- [x] Normativa: regla 10 (+EN) distingue journal (sesión, cronológico, no curado, excluido de Confluence) de ADR/gotchas/lecciones; `knowledge-check.md` limita la lectura del journal a la misma iniciativa; `confluence-scope.py --status` sobre la fixture lista `docs/knowledge/journal/**` como excluido; `claude-code-contracts.md` registra el contrato de `SessionEnd` y la limitación de `prompt`/`agent` con URL + fecha.

### T-02 — Skill `code-health`

- **Descripción**: `skills/code-health/SKILL.md` (≤ 200 líneas: propósito, disparadores, 4 medidas, cómo leer el informe, guardrails, «qué NO hace» — no refactoriza, no es seguridad —, tabla de referencias) + `skills/code-health/scripts/code-health.py <ruta> [--json] [--min-lines 6] [--langs py,js,…] [--window 8] [--exclude-tests] [--baseline <json>] [--since 90]`: informe determinista y AGNÓSTICO de lenguaje con 4 medidas — (1) **duplicados** por shingles de tokens normalizados (ventana `--window` líneas no vacías, identificadores/literales normalizados, comentarios fuera) entre ficheros de código (extensiones por defecto `py js ts tsx jsx php go java rb cs kt rs`; excluye `vendor/ node_modules/ dist/ build/ .git/ __pycache__/` siempre y `tests/`/`test_*`/`*.spec.*` con `--exclude-tests`), con pares `fichero:línea ↔ fichero:línea` y % de líneas duplicadas; (2) **tamaño y complejidad aproximada** por fichero (líneas, profundidad máxima de anidamiento por indentación o llaves) y funciones `> --min-lines` × 5 líneas (heurística por regex por lenguaje: `def`/`function`/`func`/`fn`/`public … (`; documentada como aproximación); (3) **hotspots**: ficheros más cambiados en `git log --since <N> days --name-only` cruzados con tamaño (los grandes Y que cambian mucho: `cambios × log(líneas)`); (4) **TODO/FIXME/HACK/XXX** con antigüedad en días por `git blame -L` (si git). Salida MD legible (cabecera con totales, 4 secciones, top 10 por sección) + `--json` (mismo contenido); `--baseline <json>` compara con un informe anterior (mejora ↓ / empeora ↑ por medida). Exit 0 siempre salvo error de uso (2: ruta inexistente / baseline ilegible). Sin git → omite (3) y (4) con aviso en el propio informe. Tests (≥ 10) en `skills/code-health/scripts/test_code_health.py` con fixture multi-lenguaje generada en tmp (py + js + php: un bloque duplicado, una función larga, un TODO). Integración: `agents/evaluator.md` P2 lo ejecuta opt-in (si el usuario lo pide o si el repo tiene > 200 ficheros de código) para ajustar complejidad/riesgo de la spec (una línea + referencia a la skill); `agents/planner.md` P3 puede abrir tareas de deuda a partir del informe (una línea); `commands/roadmap-brief.md` puede incluir el resumen (una fila opcional). Doc: fila ES/EN en `docs/README.md` + `CLAUDE.md`.
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,4h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,12h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/code-health/SKILL.md` (nuevo), `skills/code-health/scripts/code-health.py` (nuevo), `skills/code-health/scripts/test_code_health.py` (nuevo), `agents/evaluator.md`, `agents/planner.md`, `commands/roadmap-brief.md`, `evals/cases/skill-code-health.json` (nuevo — adelantado para que `evals/check.py` siga en exit 0 en este commit)
- **Verificación** (ejecutada 2026-09-03 — salida: `14 passed` · `RED: test_funcion_larga_detectada_y_corta_no falló con 36 == 35 (contaba la `}` de cierre), test_profundidad_python_ignora_docstrings falló con 1 == 2 (el docstring rompía el opener), test_hotspots_y_antiguedad falló con 4 == 3 (HACK dentro de una cadena) · 2026-09-03` → corregidos en el script y pasan · sobre ESTE repo `--exclude-tests`: `24 ficheros · 6839 líneas de código · duplicado 3.5 % (24 bloques) · funciones largas 56 (> 30 líneas) · anidamiento máx. 6 · TODO/FIXME/HACK 7 (el más viejo: 23 días) · hotspots 23 ficheros cambiados en 90 días`; top duplicado `scope-check.py:46 ↔ review-lens-select.py:73` (25 líneas) y `evals/check.py:105 ↔ scripts/lint_plugin.py:351` (15); funciones más largas `lint_plugin.py lint()` 108 · `ledger-lint.py parse_ledger()` 107 · `evals/check.py check()` 98; hotspot nº 1 `build_dashboard.py` (8 cambios/90 d, 610 líneas, 74.0) · `--baseline` → `grep -c baseline` = 1 · ruta inexistente → exit 2 · `99 skills/code-health/SKILL.md` · `lint_plugin: 0 errores` · `evals/check: 35 piezas · 112 casos · 0 errores` · `test_skill_size: 14/14 OK`): `python3 -m pytest -q skills/code-health/scripts` → ≥ 10 passed · `python3 skills/code-health/scripts/code-health.py . --exclude-tests` → informe MD con las 4 secciones y exit 0 (salida resumida pegada aquí) · `python3 skills/code-health/scripts/code-health.py . --json > /tmp/ch.json && python3 skills/code-health/scripts/code-health.py . --baseline /tmp/ch.json | grep -c "baseline"` → ≥ 1 · `python3 skills/code-health/scripts/code-health.py /no/existe; echo $?` → 2 · `wc -l skills/code-health/SKILL.md` → ≤ 200 · `python3 scripts/lint_plugin.py` → 0 errores · `python3 evals/check.py` → exit 0.

**Criterios de aceptación**
- [x] Las 4 medidas se calculan sobre la fixture multi-lenguaje: el bloque duplicado py↔py aparece como par con `fichero:línea`; la función larga aparece en «funciones largas»; el TODO aparece con antigüedad (o «sin git» si la fixture no está en git).
- [x] Sin git en la ruta: secciones 3 y 4 omitidas con aviso en el informe; exit 0.
- [x] `--json` y MD llevan el mismo contenido; `--baseline` marca mejora/empeora por medida; ruta inexistente → exit 2.
- [x] `SKILL.md` ≤ 200 líneas con «qué NO hace» (no refactoriza → `quick-implement`; no seguridad → `nemesis`/`cybersecurity`); `evaluator`/`planner`/`roadmap-brief` lo citan en una línea cada uno.

### T-03 — Skill `dependency-upgrade`

- **Descripción**: `skills/dependency-upgrade/SKILL.md` (≤ 200 líneas) + `skills/dependency-upgrade/scripts/deps-inventory.py <ruta> [--json] [--no-outdated] [--timeout 60]`: detecta manifiestos (`package.json`, `composer.json`, `requirements*.txt`, `pyproject.toml`, `go.mod`, `Gemfile`, `*.csproj`) y sus lockfiles (`package-lock.json`/`yarn.lock`/`pnpm-lock.yaml`, `composer.lock`, `poetry.lock`/`uv.lock`, `go.sum`, `Gemfile.lock`, `packages.lock.json`); lista dependencias con versión declarada y, cuando el lockfile es parseable (`package-lock.json`, `composer.lock`), la bloqueada; si la herramienta del ecosistema está en PATH ejecuta su `outdated` oficial (`npm outdated --json`, `composer outdated --format=json`, `pip list --outdated --format=json`, `go list -u -m -json all`) con timeout y lo parsea; sin red/herramienta → inventario sin «latest» y aviso explícito (**nunca inventa versiones**). Clasifica cada salto declarado→latest como `patch`/`minor`/`major` (semver; no comparable → `desconocido`) y marca «major = breaking probable → leer changelog upstream». Salida MD (tabla por ecosistema) + `--json`. Exit 0 siempre salvo uso (2). Tests (≥ 8) en `skills/dependency-upgrade/scripts/test_deps_inventory.py` con fixtures de manifiestos en tmp y `outdated` MOCKEADO (inyección del ejecutor de subprocess). La skill: (1) inventario con el script, (2) para cada `major` propone leer el changelog/`UPGRADING` upstream (WebFetch si está disponible; si no, lo anota como pendiente) y registrar breaking changes, (3) redacta `docs/roadmap/<fecha>-upgrade-<paquete|lote>/spec.md` con la plantilla del `evaluator` (alcance, riesgos, criterios: suites verdes + smoke) para que `evaluator` la presupueste y `planner` la planifique — **no actualiza nada por su cuenta**. Delimita frente a `nemesis`/`cybersecurity` (vulnerabilidades y CVEs → allí; aquí, versiones y breaking changes) con enlace. Doc: fila ES/EN en `docs/README.md` + `CLAUDE.md`.
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 1,9h (estimado)
- **Tiempo IA (ejec.)**: est. 0,10h · real 0,10h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/dependency-upgrade/SKILL.md` (nuevo), `skills/dependency-upgrade/scripts/deps-inventory.py` (nuevo), `skills/dependency-upgrade/scripts/test_deps_inventory.py` (nuevo; T-fix1: +1), `skills/dependency-upgrade/references/upgrade-notes.md` (nuevo), `evals/cases/skill-dependency-upgrade.json` (nuevo — adelantado por el check de evals)
- **Verificación** (ejecutada 2026-09-03 — salida: `10 passed` · `RED: test_declaradas_por_ecosistema falló con '*' == '13.0.1' (regex lazy del csproj no capturaba Version) · 2026-09-03` → corregido (parseo por etiqueta) y pasa; el otro rojo inicial (17 vs 20 dependencias) era un error de conteo del propio test · sobre ESTE repo — corrige la expectativa del ledger: SÍ hay manifiestos — `--no-outdated`: `2 manifiesto(s) · 4 dependencias · sin latest 4` (`agent-kits/qa/runner/package.json` → `@playwright/test ^1.47.0`; `skills/to-pdf/scripts/package.json` → `mammoth ^1.8.0`, `markdown-it ^14.1.0`, `puppeteer ^23.0.0`; ambos «sin lockfile»), exit 0 · con `npm` en PATH y red vía proxy (`--timeout 15`): `major 2 · minor 2` — `markdown-it ^14.1.0 → 15.0.1 major ⚠️ breaking probable`, `puppeteer ^23.0.0 → 25.9.0 major ⚠️`, `@playwright/test → 1.62.1 minor`, `mammoth → 1.12.2 minor` (deuda real del repo, candidata a spec «upgrade-to-pdf») · ruta inexistente → exit 2 · `99 skills/dependency-upgrade/SKILL.md` · `grep -c nemesis SKILL.md` → 4 · `evals/check: 36 piezas · 116 casos · 0 errores` · `lint_plugin: 0 errores` · `test_skill_size: 15/15 OK`): `python3 -m pytest -q skills/dependency-upgrade/scripts` → ≥ 8 passed · `python3 skills/dependency-upgrade/scripts/deps-inventory.py . --no-outdated` → informe MD con los `package.json` del repo y `latest = —`, exit 0 · sobre un proyecto tmp con `package.json` y `requirements.txt` sin `npm`/`pip` en PATH → tabla con versiones declaradas, `latest` = `—` y aviso «sin herramienta», exit 0 · `python3 skills/dependency-upgrade/scripts/deps-inventory.py /no/existe; echo $?` → 2 · `wc -l skills/dependency-upgrade/SKILL.md` → ≤ 200 · `grep -c "nemesis" skills/dependency-upgrade/SKILL.md` → ≥ 1 · `python3 evals/check.py` → exit 0.

**Criterios de aceptación**
- [x] Detecta los 7 tipos de manifiesto y sus lockfiles en la fixture; lista declarada + bloqueada donde el lockfile es parseable.
- [x] Con `outdated` mockeado clasifica `patch`/`minor`/`major` correctamente y marca los `major` como «breaking probable»; sin herramienta → `latest: —` + aviso; nunca una versión inventada.
- [x] MD y JSON coherentes; ruta inexistente → exit 2; `--no-outdated` no ejecuta ninguna herramienta.
- [x] `SKILL.md` ≤ 200 líneas: 3 pasos, plantilla de spec del evaluator citada por ruta, «qué NO hace» (no actualiza, no CVEs → `nemesis`).

### T-04 — Integración, doc, memoria y cierre

- **Descripción**: `docs/README.md` + `docs/en/README.md` (filas `code-health`, `dependency-upgrade`; sección de memoria con `journal/`), `README.md`/`README.es.md` (badges `skills-15`; filas «What you get»/«Qué obtienes»: journal de sesión, salud del código, actualización de dependencias; lista de skills), `.claude-plugin/plugin.json` + `marketplace.json` (descriptions sincronizadas con las 2 skills), `CLAUDE.md` (tabla de skills, regla de memoria técnica con el journal, árbol con `SessionEnd`), `agent-kits/shared/README.md` (`journal.py`), `docs/FLOWS.md` + EN (journal en SessionStart/SessionEnd; `code-health` en la evaluación), fila en `docs/roadmap/README.md`; `evals/cases/skill-code-health.json` (T-02), `skill-dependency-upgrade.json` (T-03) con negativos vecinos («refactoriza este fichero» → `quick-implement`; «busca vulnerabilidades en las dependencias» → `nemesis`) y el negativo vecino del journal («guarda esto en la memoria técnica» → ADR vía `knowledge-write`, no journal) en `evals/cases/command-retro.json` — el journal no es pieza evaluable (`journal.py` lo dispara un hook, no una description), y `/retro` es la pieza que lo lee, así que el caso vive ahí; ADR-010 «journal como memoria de sesión determinista, IA opt-in diferida» (`estado: propuesta`, fila en `docs/knowledge/README.md`); `ci.yml.MANUAL-COPY` + `.github/workflows/ci.yml` + `CONTRIBUTING.md` con las dos carpetas nuevas de tests; `skill-index.py` ≤ 45 líneas / 3.500 caracteres con 36 piezas (ajusta el recorte si hace falta). Cierre: DoD global, ledger a `completado`.
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 1,9h (estimado)
- **Tiempo IA (ejec.)**: est. 0,09h · real 0,09h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `docs/README.md`, `docs/en/README.md`, `README.md`, `README.es.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CLAUDE.md`, `agent-kits/shared/README.md`, `docs/FLOWS.md`, `docs/en/FLOWS.md`, `docs/roadmap/README.md`, `evals/cases/skill-code-health.json` (T-02), `evals/cases/skill-dependency-upgrade.json` (T-03), `evals/cases/command-retro.json`, `docs/knowledge/adr/ADR-010-journal-memoria-de-sesion-determinista.md` (nuevo), `docs/knowledge/README.md`, `ci.yml.MANUAL-COPY`, `.github/workflows/ci.yml`, `CONTRIBUTING.md`, `skills/plugin-dev/SKILL.md` (comando de suites con `skills/*/scripts`), `docs/roadmap/2026-09-03-memory-health/tasks.md` (`agent-kits/shared/skill-index.py` NO hizo falta tocarlo: 42 líneas / 3.498 caracteres con 36 piezas)
- **Verificación** (ejecutada 2026-09-03 — salida: `365 passed` (321 + 44: 15 journal, 3 hooks-shell, 14 code-health, 10 deps-inventory, 2 skill-size) · 22 suites-script de `tests/` sin fallo · `test_manifests: 4/4 OK (15 skills · 11 comandos · 10 agentes)` · `test_readme_badges: 6 badge(s) verificados OK` · `test_skill_size: 15/15 OK` · `lint_plugin: 10 agentes · 0 errores · 3 avisos` (los 3 previos: retro/roadmap-status/setup genéricos) · `evals/check: 36 ficheros · 117 casos (72 positivos, 45 negativos) · 36 piezas · 0 errores` · `ledger-lint: 0 incoherencias · 0 avisos` · `scope-check --base 514abf2: 53 fichero(s) en alcance · fuera de alcance (0)` exit 0 · `skill-index --no-cache` → `42 3498` (≤ 45 / ≤ 3.500 sin tocar el recorte) · `diff ci.yml.MANUAL-COPY .github/workflows/ci.yml` vacío + `test_ci_manual_copy: 7/7 OK` · `ls docs/knowledge/adr | grep -c ADR-010` → 1 · `build_dashboard.py --root docs/roadmap --strict` exit 0 · `test_export_skills: 13 passed` (las dos skills nuevas viajan en el paquete portable; sus tests no, por eso el SKILL.md no cita `test_*.py`); TDD n/a: prosa/config): `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts skills/code-health/scripts skills/dependency-upgrade/scripts evals` → passed (321 + nuevos) · `python3 tests/test_manifests.py` → `4/4 OK (15 skills · 11 comandos · 10 agentes …)` · `python3 tests/test_readme_badges.py` → OK · `python3 tests/test_skill_size.py` → OK · `python3 scripts/lint_plugin.py` → `0 errores` · `python3 evals/check.py` → `36 piezas … 0 errores` exit 0 · `python3 agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-03-memory-health/tasks.md` → exit 0 · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-memory-health --base 514abf2` → exit 0 · `python3 agent-kits/shared/skill-index.py --no-cache | python3 -c "import sys;t=sys.stdin.read();print(len(t.splitlines()), len(t))"` → ≤ 45 y ≤ 3500 · `diff ci.yml.MANUAL-COPY .github/workflows/ci.yml` → vacío · `ls docs/knowledge/adr | grep -c ADR-010` → 1.

**Criterios de aceptación**
- [x] Badges `skills-15` en ambos README; filas nuevas en «What you get»/«Qué obtienes» y en la lista de skills; `test_readme_badges` verde.
- [x] `plugin.json` y `marketplace.json` listan `code-health` y `dependency-upgrade` (descriptions idénticas); `test_manifests` 4/4 con 15 skills.
- [x] `docs/README.md` + EN, `CLAUDE.md`, `agent-kits/shared/README.md`, `docs/FLOWS.md` + EN actualizados; fila `memory-health` en `docs/roadmap/README.md`.
- [x] Evals: `skill-code-health.json` y `skill-dependency-upgrade.json` con 2 positivos + negativo vecino; negativo «guarda esto en la memoria técnica» (→ ADR/knowledge-write, no journal) en `command-retro.json`; `evals/check.py` 36 piezas, 0 errores.
- [x] ADR-010 con la plantilla (`estado: propuesta`, `iniciativa: memory-health`) y fila en `docs/knowledge/README.md`.
- [x] CI y `CONTRIBUTING.md` corren las dos carpetas nuevas; copia manual sincronizada.
- [x] **Cierre medido:** suites verdes, `lint_plugin` 0 errores, `evals/check` exit 0, `ledger-lint` exit 0, `scope-check --base 514abf2` exit 0, `skill-index` dentro de límites.

---

**Verificación de cierre (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts skills/code-health/scripts skills/dependency-upgrade/scripts evals` → **365 passed** (321 + 44) · 22 suites-script OK · `lint_plugin` 10 agentes · 0 errores · 3 avisos (los 3 previos) · `evals/check` 36 piezas · 117 casos · 0 errores · `ledger-lint` 0/0 · `scope-check --base 514abf2` 53 ficheros en alcance, 0 fuera · `skill-index` 42 líneas / 3.498 caracteres (36 piezas) · hooks simulados: `SessionEnd` con stdin realista → entrada escrita; 2.ª vez mismo `session_id` → actualiza, no duplica; `SessionStart resume` → bloque `Journal de sesión` de 3 líneas (≤ 25); `compact` → sin journal · `code-health.py .` y `deps-inventory.py .` ejecutados sobre este repo (salida resumida en T-02/T-03). Pendiente de la **revisión de dos lentes** (la lanza el orquestador). `CHANGELOG*.md` sin tocar (lo hace el orquestador). **Versionado del journal (decidido en la revisión, M4):** las entradas SÍ se versionan (memoria del proyecto, como ADR/lecciones); quien no quiera, `docs/knowledge/journal/*.md` (no el README) en su `.gitignore` — documentado en `journal/README.md` y regla 10 (+EN). En este repo la carpeta nace solo con el `README.md`; las entradas las escribirá el hook en las próximas sesiones.

**Contradicciones/dudas con la doc oficial (2026-09-03, `hooks.md` + `hooks-guide.md`):**
- `SessionEnd`: la tabla de exit codes dice «Can block? No — Output and exit code are ignored, except `terminalSequence`», pero la sección del evento añade que un exit 2 «stops cleanup but leaves the session in place to resume». Contradicción menor dentro de la propia doc; nuestro hook siempre exit 0 y no emite nada, así que no nos afecta.
- Presupuesto de tiempo de `SessionEnd`: **1,5 s compartidos** entre todos sus hooks, ampliable hasta 60 s con `timeout` por hook — no estaba registrado en `claude-code-contracts.md` (solo los 600 s de `command`); `hooks.json` declara `timeout: 20` y la fila se ha añadido.
- Hooks `prompt`/`agent`: `hooks-guide.md` (leída completa) fija el contrato `{"ok", "reason"[, "impossible"]}` y describe el efecto de `ok: false` solo en `Stop`/`SubagentStop`/`PreToolUse`/`PostToolUse`/`PostToolBatch`/`UserPromptSubmit`/`UserPromptExpansion`; la sección `#prompt-based-hooks` de la referencia `hooks.md` llegó **truncada** por WebFetch (página larga) → la lista exacta de eventos que admiten `prompt` queda «a re-verificar». La decisión (no implementar el resumen por IA) no depende de ella: en `SessionEnd` la salida se ignora en cualquier caso.
- El ejemplo de stdin de `SessionEnd` en la doc no muestra `permission_mode`/`effort`; el hook no los usa.
- El formato JSONL de la transcripción (`transcript_path`) no está documentado oficialmente: `journal.py` lo lee best-effort (primer mensaje `type: user`) y degrada a «Sesión sobre <iniciativa>» si no puede.
- Duda de alcance: `deps-inventory.py` detectó en ESTE repo dos `package.json` sin lockfile con dos majors pendientes (`markdown-it 14→15`, `puppeteer 23→25`) — deuda real fuera del alcance de esta iniciativa; candidata a una spec «upgrade-to-pdf» con la propia skill.

## Revisión de dos lentes — intento 1: 0 Critical · 1 Important · 3 Minor (revisión del orquestador con fixtures; el revisor externo cayó por un 429) — todos corregidos en `T-fix1`

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| I1 | Important | El hook `SessionEnd` creaba `docs/knowledge/journal/` en CUALQUIER repo con el plugin instalado (sin `docs/roadmap/`, `docs/knowledge/` ni `.claude/dev.json`) — spam de carpetas | T-01 | `journal.proyecto_con_plugin(root)`: `write` devuelve `None` y no toca disco si no existe ninguno de los tres rastros; el CLI sale exit 0 sin stdout; comentario del hook y regla 10 (+EN) punto (6) | `test_write_sin_rastro_del_plugin_no_escribe_nada` (unit: repo con solo `a.txt` → `['a.txt']`; cada rastro por separado → escribe) · `test_session_journal_repo_ajeno_sin_rastro_del_plugin_no_siembra_nada` (shell) · repro manual del hook real: `rc 0 '' ['a.txt']` |
| M2 | Minor | Slug sin iniciativa activa → `<fecha>-n-a.md` (feo y ambiguo) | T-01 | `slugify("n/a"\|""\|None)` → `sesion`; criterio documentado en el docstring, en `journal/README.md` («`AAAA-MM-DD-<iniciativa activa \| sesion>.md`») y en la regla 10 (+EN); el frontmatter conserva `iniciativa: n/a` | `test_slug_sin_iniciativa_es_sesion_no_n_a` OK |
| M3 | Minor | `requirements.txt`: la `declarada` arrastraba marcador de entorno y comentario; `-e .` y `git+https://…` desaparecían en silencio | T-03 | Parser de requirements reescrito: comentario de fin de línea fuera, `marcador` en su propio campo, `-e .`/`git+…`/`nombre @ url` como dependencias `tipo: local\|vcs\|url` (sin versión ni `latest`, `salto: desconocido`), opciones de pip (`-r`, `-c`, `--index-url`…) no son dependencias; columna «Tipo / marcador» en el MD y campos `tipo`/`marcador`/`origen` en el JSON (`registro` por defecto) | `test_requirements_marcadores_comentarios_local_y_vcs` (8 líneas → 6 deps, antes 2) · `RED: falló con '| **local** `.` |' not in md (el render recortaba el backtick final) · 2026-09-03` → corregido · `11 passed` |
| M4 | Decisión | ¿Versionar las entradas del journal? | T-01 | SÍ se versionan (memoria del proyecto, como ADR/lecciones); opt-out documentado: `docs/knowledge/journal/*.md` (no el README) en `.gitignore` — en `journal/README.md` (cabecera generada por `journal.py index`) y regla 10 (+EN) punto (7) | `grep -c gitignore docs/knowledge/journal/README.md docs/CONVENTIONS.md docs/en/CONVENTIONS.md` → 1 · 1 · 1 |

**Verificación tras `T-fix1` (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts skills/code-health/scripts skills/dependency-upgrade/scripts evals` → **369 passed** (365 + 4: 2 journal, 1 hooks-shell, 1 deps-inventory) · 22 suites-script OK · `lint_plugin: 10 agentes · 0 errores · 3 avisos` · `evals/check: 36 piezas · 117 casos · 0 errores` · `ledger-lint` 0/0 · `scope-check --base 514abf2` 0 fuera · repro I1 con el hook real sobre repo ajeno: `rc 0 '' ['a.txt']`.

## Revisión de dos lentes — intento 2: sin gaps (re-verificación del orquestador: hook mudo sin rastro del plugin, slug `sesion`, requirements con marcador/vcs/local; 369 tests). ADR-010 promovida a `aceptada (validada: revisión de dos lentes, 2026-09-03, intento 2)`.
