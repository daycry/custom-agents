---
tasks: parity-core
descripcion: Cerrar la paridad de núcleo con la vía rápida — (1) tiering de modelos CONFIGURABLE en dos capas (frontmatter `model` + `effort` en todos los agentes, `dev.json` `modelos.<agente>` por agente, resolutor determinista `model-tier.py` que leen los orquestadores, linter valida `effort`, `/setup` 5-quater); (2) agente `architect` (opciones de diseño con trade-offs → `design.md` + ADR, validación por trozos, cadena pm-cycle/dev-cycle/planner); (3) skill `tdd` como fuente única de RED-GREEN-REFACTOR (ley dura, evidencia del rojo, anti-patrones, comandos por stack); (4) agente `reviewer` de solo lectura para las lentes de `adversarial-review` con fallback a subagente genérico; (5) referencias `docs-style` (ES+EN) y `claude-code-contracts` (contratos oficiales verificados con fecha); (6) integración, doc bilingüe, evals, ADR-009.
estado: completado        # borrador | en-progreso | completado | cancelado
creado: 2026-09-03
actualizado: 2026-09-03
via: rapida               # vía rápida de /dev-cycle: sin spec/evaluación/plan; conserva revisión de dos lentes + suites
verificacion: obligatoria # cada `### T-XX` lleva `- **Verificación**:`; ledger-lint lo exige (exit ≠ 0 si falta)
generacion:
  fuente: estimado        # usage-meter no disponible en este entorno (sandbox cloud)
---

# Checklist de Tareas — parity-core (vía rápida)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-03 |
| **Plan** | n/a — **vía rápida** (sin spec/evaluación/plan; ledger ligero + revisión de dos lentes + verificación) |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del cambio. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Decisión del usuario (2026-09-03).** Seis huecos de paridad con la práctica de los mejores plugins de agentes: el tiering de modelos era fijo en el frontmatter (sin `effort`, sin override por proyecto); no había agente de **diseño** entre la spec aprobada y el plan (el planner decidía la arquitectura de pasada); el método TDD vivía duplicado en prosa (implementer P3, dev-cycle, setup); las lentes de la revisión iban a subagentes genéricos con `tools` sin restringir (un revisor que puede escribir); no había guía de redacción técnica ni un registro único de los contratos oficiales de Claude Code que el plugin usa (cada iniciativa re-verificaba por su cuenta). Se cierra en una iniciativa de vía rápida: T-01 tiering configurable, T-02 `architect` + `design.md`, T-03 skill `tdd`, T-04 `reviewer`, T-05 referencias `docs-style` + `claude-code-contracts`, T-06 integración/doc/memoria. `CHANGELOG*.md` lo toca el orquestador.

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase única — paridad de núcleo | 6 | 6 | 100% | 10,1 / 11,0h | 0,50 / 0,55h | 0,14 / 0,14h | 250k / 260k |
| **TOTAL** | **6** | **6** | **100%** | **10,1 / 11,0h** | **0,50 / 0,55h** | **0,14 / 0,14h** | **250k / 260k** |

---

## Fase única — paridad de núcleo

**Estado**: completado · **Estimado**: 11,0h · **Real**: 10,1h (estimado) · **Coste est.**: ≈575 € · **Tokens est.**: 260k

### T-01 — Tiering de modelos CONFIGURABLE (dos capas)

- **Descripción**: (a) Todo agente declara en el frontmatter `model` (ya) y `effort` (valores oficiales `low|medium|high|xhigh|max` — sub-agents.md, verificado 2026-09-03; el plugin usa `medium` por defecto y `high` para `architect`/`reviewer`/`evaluator`); `scripts/lint_plugin.py` exige `effort` presente y válido (`VALID_EFFORTS`) igual que `model`; fixtures de `tests/test_lint_plugin.py` al día + 2 casos nuevos (falta / inválido). (b) `docs/CONVENTIONS.md` (+EN) amplía la tabla de tiering (`haiku` mecánico · `sonnet` desarrollo · `opus` razonamiento crítico — architect, reviewer, evaluator —; `effort` por defecto `medium`, `high` en los tres de razonamiento) y la regla 9 documenta la clave `dev.json` `"modelos": {"<agente>": {"model": "...", "effort": "..."}}` (parcial, por agente). (c) Script determinista `agent-kits/shared/model-tier.py <agente> [--json] [--all]` que resuelve el tier efectivo: frontmatter (capa 1) + `dev.json` `modelos.<agente>` (capa 2, gana si el valor es válido; inválido → aviso y se ignora; fichero ausente/corrupto → aviso y capa 1); salida `fuente` por campo; exit 0 ok · 1 agente inexistente · 2 plugin no localizado; ≥ 6 tests en `agent-kits/shared/test_model_tier.py`. (d) Los orquestadores (`commands/dev-cycle.md`, `commands/pm-cycle.md`, `skills/adversarial-review/SKILL.md`, `skills/quick-implement/SKILL.md`) llaman a `model-tier.py` antes de despachar y pasan `model` en el parámetro `model` del Agent tool (contrato oficial: «per-invocation `model` parameter», prioridad 1 desde v2.1.251). **Honestidad:** el Agent tool NO documenta parámetro `effort` → el `effort` de `dev.json` se declara informativo (solo el del frontmatter es efectivo; el orquestador lo anuncia pero no puede aplicarlo); invocación manual `@agente` → frontmatter; vía cadena → config. (e) `commands/setup.md` paso 5-quater «Modelos por agente (opcional)»: muestra `model-tier.py --all` y permite cambiar, mergeando `modelos` en `dev.json`.
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,4h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,12h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `agent-kits/shared/model-tier.py` (nuevo; T-fix1: aviso con raíz no-objeto), `agent-kits/shared/test_model_tier.py` (nuevo; T-fix1: +1), `agent-kits/shared/README.md`, `scripts/lint_plugin.py`, `tests/test_lint_plugin.py`, `agents/analyst.md`, `agents/documenter.md`, `agents/evaluator.md`, `agents/implementer.md`, `agents/nemesis.md`, `agents/pdfy.md`, `agents/planner.md`, `agents/qa.md` (nuevo campo de frontmatter), `docs/CONVENTIONS.md`, `docs/en/CONVENTIONS.md`, `commands/setup.md`, `commands/dev-cycle.md`, `commands/pm-cycle.md`, `skills/adversarial-review/SKILL.md`, `skills/quick-implement/SKILL.md`, `skills/plugin-dev/SKILL.md`, `skills/plugin-dev/templates/agent.template.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `11 passed, 1 skipped` (el skip es el test de sanidad sobre `agents/architect.md`, que nace en T-02) · sin dev.json `{"model": "opus", "effort": "high", "fuente": {"model": "frontmatter", "effort": "frontmatter"}}` exit 0 (sobre `evaluator`; architect en T-02) · con dev.json `{"model": "sonnet", … "fuente": {"model": "dev.json", …}}` exit 0 · `fantasma` → `❌ agente fantasma no existe` exit 1 · `grep -L` vacío (8/8 con effort) · `lint_plugin: 8 agentes · 0 errores · 3 avisos` · `test_lint_plugin: 22/22 OK` · grep model-tier.py → dev-cycle 1 · pm-cycle 1 · quick-implement 1 · setup 3; adversarial-review en T-04): `python3 -m pytest -q agent-kits/shared/test_model_tier.py` → ≥ 6 passed · `python3 agent-kits/shared/model-tier.py architect --json` (sin `dev.json`) → `"model": "opus", "effort": "high"` con `fuente` frontmatter, exit 0 · con `.claude/dev.json` `{"modelos": {"architect": {"model": "sonnet"}}}` en un proyecto temporal → `"model": "sonnet"` con `fuente.model: dev.json`, exit 0 · `python3 agent-kits/shared/model-tier.py fantasma` → exit 1 · `grep -L "^effort:" agents/*.md` → vacío · `python3 scripts/lint_plugin.py` → `0 errores` · `python3 tests/test_lint_plugin.py` → OK (casos effort ausente/inválido) · `grep -c "model-tier.py" commands/dev-cycle.md commands/pm-cycle.md skills/adversarial-review/SKILL.md skills/quick-implement/SKILL.md commands/setup.md` → ≥ 1 cada uno.

**Criterios de aceptación**
- [x] Los 10 agentes llevan `effort` válido; `lint_plugin.py` falla con `effort` ausente o fuera de `{low, medium, high, xhigh, max}` (tests 2 casos nuevos) y sigue con 0 errores en el repo.
- [x] `model-tier.py`: sin `dev.json` devuelve el frontmatter; con `modelos.<agente>` válido lo aplica y lo etiqueta `dev.json`; valor inválido → aviso stderr + frontmatter; `dev.json` corrupto → aviso + frontmatter; agente inexistente → exit 1; `--all` tabla de todos los agentes; `--json` con `agente/model/effort/fuente/avisos`. ≥ 6 tests.
- [x] `docs/CONVENTIONS.md` + `docs/en/CONVENTIONS.md`: tabla de tiering ampliada (model + effort por agente) y fila de `dev.json` con la clave `modelos` en la regla 9.
- [x] Los 4 orquestadores citan `model-tier.py` antes de despachar y pasan `model` al Agent tool; los 4 (y CONVENTIONS) dicen explícitamente que `effort` de `dev.json` es informativo porque el Agent tool no documenta ese parámetro.
- [x] `commands/setup.md` paso 5-quater «Modelos por agente (opcional)» con tabla efectiva (`model-tier.py --all`) y merge en `dev.json`.

### T-02 — Agente `architect` + `design.md`

- **Descripción**: `agents/architect.md` (`model: opus`, `effort: high`, `tools: Read, Grep, Glob, Bash, Write, Edit` — Write/Edit SOLO sobre `docs/roadmap/<…>/design.md`, el frontmatter `design:` de `spec.md`/`improvement-plan.md` y `docs/knowledge/adr/` + índice; alcance en prosa + tabla de racionalización, sin hook de guardia). A partir de `spec.md` aprobada (y `evaluation.md` si existe) explora el repo y produce `design.md` con la plantilla `agent-kits/architect/templates/design.md`: contexto y restricciones · **2-3 opciones** con trade-offs (complejidad, riesgo, coste relativo, reversibilidad) · criterios de decisión · opción elegida y por qué · impacto en módulos/ficheros · riesgos y mitigaciones · preguntas abiertas. Presenta las opciones al usuario **en trozos digestibles** (una opción por mensaje, resumen comparativo, pregunta de validación) antes de fijar la elegida; escribe el ADR de la decisión (umbral de `knowledge-write.md`, `estado: propuesta`) y enlaza spec ↔ design ↔ plan (`design:` en spec e `improvement-plan.md`). Estados de `design.md`: `borrador · aprobado · obsoleto`. No estima, no planifica, no implementa. Cadena: `/pm-cycle` lo ofrece tras el go (opt-in, recomendado si complejidad Alta / riesgo arquitectónico); `/dev-cycle` Fase 2 lo invoca antes de `planner` si existe `design.md` o si el usuario lo pide; `planner` lee `design.md` si existe y respeta la opción elegida (P1). Tabla de transiciones de dev-cycle con la columna/fila de `design.md`. Doc `docs/agents/architect.md`; `docs/FLOWS.md` (+EN) con `architect` en la cadena.
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,12h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `agents/architect.md` (nuevo), `agent-kits/architect/templates/design.md` (nuevo), `docs/agents/architect.md` (nuevo), `agents/planner.md`, `commands/pm-cycle.md`, `commands/dev-cycle.md`, `docs/FLOWS.md`, `docs/en/FLOWS.md`, `docs/CONVENTIONS.md`, `docs/en/CONVENTIONS.md`, `tests/test_rationalization_tables.py`, `hooks/architect-guardrail.sh` (nuevo, T-fix1), `hooks/implementer-guardrail.sh` (T-fix1: variable de agente), `agent-kits/shared/guardrail-check.py` (T-fix1: `--agent architect`), `agent-kits/shared/test_guardrail_check.py` (T-fix1), `tests/test_hooks_shell.py` (T-fix1), `docs/knowledge/adr/ADR-007-deny-solo-con-alcance-de-agente.md` (T-fix1: enmienda), `agent-kits/evaluator/templates/spec.md` (T-fix1: `design:`), `agent-kits/planner/templates/improvement-plan.md` (T-fix1), `agent-kits/planner/templates/tasks.md` (T-fix1), `skills/roadmap-dashboard/scripts/build_dashboard.py` (T-fix1: columna diseño), `tests/test_dashboard.py` (T-fix1), `tests/fixtures/roadmap/2026-01-10-alpha/design.md` (nuevo, T-fix1), `skills/confluence-publish/assets/confluence.example.json` (T-fix1: D1 con design.md), `skills/confluence-publish/references/config-and-policy.md` (T-fix1), `skills/confluence-publish/SKILL.md` (T-fix1), `tests/test_confluence_scope.py` (T-fix1), `tests/fixtures/confluence-scope/docs/roadmap/init1/design.md` (nuevo, T-fix1), `agent-kits/shared/knowledge-write.md` (T-fix1: architect escribe ADR), `agent-kits/shared/README.md` (T-fix1), `evals/cases/agent-architect.json` (nuevo — adelantado desde T-06 para que el check estático de evals siga en exit 0 en este commit)
- **Verificación** (ejecutada 2026-09-03 — salida: `lint_plugin: 9 agentes · 0 errores · 3 avisos` · `grep -c "{{"` → 44 · `grep architect … | wc -l` → 25 · `grep -c design.md commands/dev-cycle.md` → 5 · `test_rationalization_tables: 7 tests OK` · `test_mermaid_blocks: 30 diagrama(s) OK` · `evals/check: 32 ficheros · 100 casos · 0 errores` · `model-tier.py architect --json` → `{"model": "opus", "effort": "high", "fuente": frontmatter}`): `python3 scripts/lint_plugin.py` → `0 errores` (architect con `model`, `effort`, `dependencies` válidos) · `grep -c "{{" agent-kits/architect/templates/design.md` → ≥ 6 (plantilla con placeholders) · `grep -n "architect" commands/pm-cycle.md commands/dev-cycle.md agents/planner.md docs/FLOWS.md docs/en/FLOWS.md | wc -l` → ≥ 6 · `grep -c "design.md" commands/dev-cycle.md` → ≥ 2 (Fase 2 + tabla de transiciones) · `python3 tests/test_rationalization_tables.py` → OK con `architect` en `PIEZAS`.

**Criterios de aceptación**
- [x] `agents/architect.md` con frontmatter válido, alcance de escritura en prosa, proceso (contexto → opciones por trozos → validación → design.md → ADR → enlaces), tabla de racionalización (6-8 filas) antes del DoD y DoD con evidencia.
- [x] Plantilla `design.md` con las 7 secciones y frontmatter (`estado: borrador`, `spec:`, `plan:`, `adr:`, `generacion:`).
- [x] `/pm-cycle` ofrece `architect` tras el go (opt-in, con la recomendación por complejidad/riesgo); `/dev-cycle` Fase 2 lo invoca antes de `planner` cuando hay `design.md` o el usuario lo pide; `planner` P1 lee `design.md` y respeta la opción elegida; tabla de transiciones de dev-cycle con `design.md` (`borrador → aprobado`; `obsoleto` en cancelación/re-diseño).
- [x] `docs/agents/architect.md` escrito; `docs/FLOWS.md` + EN muestran `architect` entre `evaluator` y `planner` (opcional) en §1 y §3.
- [x] CONVENTIONS regla 7 (+EN): `design.md` en el árbol de la carpeta de iniciativa, vocabulario de estados y enlace `design:`.

### T-03 — Skill `tdd` (fuente única del método RED-GREEN-REFACTOR)

- **Descripción**: `skills/tdd/SKILL.md` (≤ 200 líneas): ley dura («el código escrito antes de su test se borra y se reescribe tras el rojo»), ciclo RED-GREEN-REFACTOR con evidencia obligatoria del rojo (`RED: <test> falló con <error> · <fecha>` en el ledger), qué NO es TDD (tests después, tests vacíos, asserts triviales, test que ya pasaba), excepciones declaradas (prosa/config → «TDD n/a: <motivo>»), tabla de racionalización de 8 filas, `references/anti-patterns.md` (catálogo con ejemplos) y `references/by-stack.md` (cómo correr UN solo test rojo en pytest/jest/vitest/phpunit/go test — comandos exactos). `agents/implementer.md` P3 sustituye la prosa del método por una referencia de 2 líneas a la skill (token-diet; la excepción «TDD n/a» y la fila de la tabla siguen citando la evidencia del rojo); `task-brief.py` añade al brief «sigue la skill `tdd`» cuando `dev.json` tiene `tdd: true` (autodetección de `.claude/dev.json` en la raíz derivada de la carpeta, o `--tdd`/`--dev-json`; +3 tests). Invocable a demanda («hazlo con TDD», «escribe primero el test»). `evals/cases/skill-tdd.json` (2 positivos + negativo vecino «corre los tests»).
- **Estado**: completado
- **Tiempo humano**: est. 1,5h · real 1,4h (estimado)
- **Tiempo IA (ejec.)**: est. 0,08h · real 0,07h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `skills/tdd/SKILL.md` (nuevo), `skills/tdd/references/anti-patterns.md` (nuevo), `skills/tdd/references/by-stack.md` (nuevo), `agents/implementer.md`, `agent-kits/shared/task-brief.py` (T-fix1: sección «Diseño» desde `design.md` aprobado), `agent-kits/shared/test_task_brief.py` (T-fix1: +3), `commands/dev-cycle.md`, `commands/setup.md`, `evals/cases/skill-tdd.json` (nuevo), `tests/test_rationalization_tables.py`
- **Verificación** (ejecutada 2026-09-03 — salida: `119 skills/tdd/SKILL.md` · `23 passed` (20 + 3 tdd) · `RED: test_tdd_true_en_dev_json_inyecta_la_skill y test_flag_tdd_fuerza… fallaron con AssertionError ("## TDD" not in out) · 2026-09-03` · grep `skill \`tdd\`` en brief de proyecto tmp con `{"tdd": true}` → 1 · `grep -c RED-GREEN-REFACTOR agents/implementer.md` → 0 · `test_rationalization_tables: 8 tests OK` · `evals/check: 33 ficheros · 104 casos · 0 errores` · `test_skill_size: 13/13 OK`): `wc -l skills/tdd/SKILL.md` → ≤ 200 · `python3 -m pytest -q agent-kits/shared/test_task_brief.py` → passed (+3 casos tdd) · `python3 agent-kits/shared/task-brief.py <carpeta-tmp-con-dev.json-tdd> T-01 --sin-lint | grep -c "skill \`tdd\`"` → ≥ 1 · `grep -c "RED-GREEN-REFACTOR" agents/implementer.md` → ≤ 1 (la prosa del método ya no vive ahí) · `python3 tests/test_rationalization_tables.py` → OK con `tdd` en `PIEZAS` · `python3 evals/check.py` → exit 0.

**Criterios de aceptación**
- [x] `SKILL.md` ≤ 200 líneas con ley dura, ciclo con evidencia, «qué NO es TDD», excepciones, tabla de racionalización (8 filas, formato compartido) y tabla de referencias; `references/anti-patterns.md` y `references/by-stack.md` existen y se citan «lee X solo al llegar al paso Y».
- [x] `implementer.md` P3 con `tdd: true` remite a la skill en ≤ 2 líneas (sin repetir el ciclo); `task-brief.py` inyecta «sigue la skill `tdd`» con `tdd: true` (+3 tests: con dev.json true, con false/ausente, con `--tdd`).
- [x] `evals/cases/skill-tdd.json` con literal + paráfrasis + negativo «corre los tests» (`redirect` a `agent:qa`); `evals/check.py` exit 0.

### T-04 — Agente `reviewer` (solo lectura) para las lentes

- **Descripción**: `agents/reviewer.md` (`model: opus`, `effort: high`, `tools: Read, Grep, Glob, Bash` — Bash SOLO para ejecutar tests/scripts; **Write/Edit no están en `tools`**, y se documenta): revisor adversarial de contexto fresco que recibe la lente (A conformidad / B robustez / C seguridad), el diff/rango y los artefactos, y devuelve la salida ESTRUCTURADA que `adversarial-review` espera (tabla por criterio ✓/✗ con `fichero:línea`, gaps graduados `Critical/Important/Minor` con escenario, evidencia ejecutada — «sin defectos» es salida válida). La tabla de racionalización del revisor vive en la skill (§ «Racionalizaciones del REVISOR») → el agente la referencia, no la duplica. `skills/adversarial-review/SKILL.md` §2: las lentes se despachan al agente `reviewer` por nombre (Agent tool `subagent_type: reviewer`, `model` de `model-tier.py`), fallback a subagente genérico si el agente no está disponible (degradación con aviso). El SKILL.md se mantiene ≤ 200 líneas moviendo los prompts literales de las lentes a `references/lens-prompts.md` (cero pérdida: `--diet-check`). Doc `docs/agents/reviewer.md`; `evals/cases/agent-reviewer.json` (negativo vecino «revisa este diff» → `skill:adversarial-review`).
- **Estado**: completado
- **Tiempo humano**: est. 1,5h · real 1,4h (estimado)
- **Tiempo IA (ejec.)**: est. 0,08h · real 0,07h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `agents/reviewer.md` (nuevo; T-fix1: rango unificado, columna de tarea), `docs/agents/reviewer.md` (nuevo), `skills/adversarial-review/SKILL.md`, `skills/adversarial-review/references/lens-prompts.md` (nuevo), `skills/adversarial-review/references/lens-c-heuristics.md` (nuevo — detalle de la heurística de la Lente C movido desde §1 para volver a ≤ 200 líneas), `evals/cases/agent-reviewer.json` (nuevo), `docs/FLOWS.md`, `docs/en/FLOWS.md` (ya en T-02)
- **Verificación** (ejecutada 2026-09-03 — salida: `lint_plugin: 10 agentes · 0 errores · 3 avisos` · `tools: Read, Grep, Glob, Bash` · `197 skills/adversarial-review/SKILL.md` · `diet-check adversarial-review vs b8add37: 116 párrafos · 0 párrafos perdidos · SKILL.md 197 líneas` · `grep -c reviewer` → 9 · `test_rationalization_tables: 8 tests OK` · `evals/check: 34 ficheros · 108 casos · 0 errores`; TDD n/a: prosa): `python3 scripts/lint_plugin.py` → `0 errores` · `grep -E "^tools:" agents/reviewer.md` → `tools: Read, Grep, Glob, Bash` (sin Write/Edit) · `wc -l skills/adversarial-review/SKILL.md` → ≤ 200 · `python3 tests/test_skill_size.py --diet-check adversarial-review b8add37` → `0 párrafos perdidos` · `grep -c "reviewer" skills/adversarial-review/SKILL.md` → ≥ 2 · `python3 tests/test_rationalization_tables.py` → OK · `python3 evals/check.py` → exit 0.

**Criterios de aceptación**
- [x] `agents/reviewer.md` con frontmatter válido, `tools` sin Write/Edit (explicado en prosa), contrato de entrada (lente + diff + artefactos + tabla del intento anterior) y de salida (estructura que espera la skill), referencia a la tabla de racionalización de la skill (no duplicada).
- [x] `adversarial-review` §2 despacha a `reviewer` por nombre con el tier de `model-tier.py` y declara el fallback a subagente genérico; prompts literales en `references/lens-prompts.md`; `SKILL.md` ≤ 200 líneas y `--diet-check` 0 párrafos perdidos.
- [x] `docs/agents/reviewer.md`; FLOWS (+EN) nombran a `reviewer` en las lentes; evals con 2 positivos + negativo «revisa este diff» → `skill:adversarial-review`.

### T-05 — Referencias `docs-style` y `claude-code-contracts`

- **Descripción**: (a) `agent-kits/shared/docs-style.md` (ES) + `docs-style.en.md` (EN): reglas de redacción técnica — frases cortas, voz activa, un concepto por párrafo, tablas para comparar y prosa para explicar, ejemplos reales del código, sin adjetivos vacíos, títulos que responden preguntas, bilingüe sin traducción literal (misma sustancia, redacción nativa) —; `documenter`, `analyst`, `planner` y `architect` la cargan (una línea cada uno, con fallback) y `adversarial-review` Lente A la usa como criterio SOLO cuando la iniciativa es de prosa (docs/skills/agentes). Fila en `agent-kits/shared/README.md`. (b) `skills/plugin-dev/references/claude-code-contracts.md`: los contratos verificados hoy (URL + fecha 2026-09-03) — eventos de hooks y JSON de salida (`systemMessage`, `hookSpecificOutput.additionalContext`, PreToolUse `permissionDecision`), campos del stdin por evento, matchers de SessionStart (`startup|resume|clear|compact|fork`), tope de 10.000 caracteres (NO localizado hoy en hooks.md: marcado «a re-verificar»), frontmatter de agente (`model`, `effort`, `tools`, `disallowedTools`, `skills`, `hooks`, `isolation`, `memory`, `permissionMode`, `maxTurns`, `background` — cuál usamos y cuál no y por qué), prioridad del modelo del subagente (parámetro por invocación > frontmatter > `CLAUDE_CODE_SUBAGENT_MODEL` > sesión), frontmatter de skill (`disable-model-invocation`, `user-invocable`, `allowed-tools`, `model`, `effort`, `context: fork`, `paths`; descripción truncada a 1.536 caracteres en el listado), CLI headless (`-p`, `--plugin-dir`, `--bare`, `--output-format stream-json --verbose`, `--allowedTools`, `--max-turns`, `--permission-mode`), `statusLine` en settings.json (`type`/`command`/`padding`; `~` documentado; `settings` de `plugin.json` solo admite `agent` y `subagentStatusLine`), `plugin.json` (`displayName`, `keywords`, `settings`, `dependencies`) y marketplace (`strict`, `tags`, `category`). **Contradicción registrada:** plugins-reference dice hoy que `${CLAUDE_PLUGIN_ROOT}` se sustituye en «Skill and agent content — anywhere the placeholder appears», contra la regla 5 de CONVENTIONS (que asume que NO se expande en markdown); se mantiene `find` porque cubre también la instalación como `.claude/` sin plugin, y se anota como candidato a simplificación. Sección «cómo re-verificar» y regla en `plugin-dev` SKILL.md: «consulta esta referencia antes de hacer WebFetch; si dudas o han pasado > 90 días, re-verifica y actualiza la fecha».
- **Estado**: completado
- **Tiempo humano**: est. 1,5h · real 1,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,08h · real 0,07h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `agent-kits/shared/docs-style.md` (nuevo), `agent-kits/shared/docs-style.en.md` (nuevo), `agent-kits/shared/README.md`, `agents/documenter.md`, `agents/analyst.md`, `agents/planner.md`, `agents/architect.md`, `skills/adversarial-review/SKILL.md`, `skills/adversarial-review/references/lens-prompts.md`, `skills/plugin-dev/references/claude-code-contracts.md` (nuevo), `skills/plugin-dev/SKILL.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `ls … | wc -l` → 3 · `grep -l docs-style … | wc -l` → 5 · `grep -c 2026-09-03 claude-code-contracts.md` → 8 · `grep -c claude-code-contracts SKILL.md` → 1 · `grep -c "90 días"` → 2 · `148 skills/plugin-dev/SKILL.md` · `lint_plugin: 10 agentes · 0 errores · 3 avisos`; TDD n/a: prosa): `ls agent-kits/shared/docs-style.md agent-kits/shared/docs-style.en.md skills/plugin-dev/references/claude-code-contracts.md` → 3 ficheros · `grep -l "docs-style" agents/documenter.md agents/analyst.md agents/planner.md agents/architect.md skills/adversarial-review/references/lens-prompts.md | wc -l` → 5 · `grep -c "2026-09-03" skills/plugin-dev/references/claude-code-contracts.md` → ≥ 8 · `grep -c "claude-code-contracts" skills/plugin-dev/SKILL.md` → ≥ 1 · `grep -c "90 días" skills/plugin-dev/SKILL.md` → ≥ 1 · `wc -l skills/plugin-dev/SKILL.md` → ≤ 200.

**Criterios de aceptación**
- [x] `docs-style.md` + `docs-style.en.md` con las 8 reglas y un ejemplo antes/después por regla; fila en `agent-kits/shared/README.md`; cargada por `documenter`, `analyst`, `planner`, `architect` (una línea + fallback) y por la Lente A solo en iniciativas de prosa.
- [x] `claude-code-contracts.md` con URL + fecha por contrato, tabla «cuál usamos / cuál no / por qué» para el frontmatter de agente, la contradicción `${CLAUDE_PLUGIN_ROOT}` registrada, el tope 10.000 marcado «a re-verificar» y la sección «cómo re-verificar»; `plugin-dev` SKILL.md con la regla de los 90 días.

### T-06 — Integración, doc, memoria y cierre

- **Descripción**: `docs/README.md` + `docs/en/README.md` (filas de `architect`, `reviewer`, skill `tdd`), `README.md`/`README.es.md` (badges `agents-10`, `skills-13`; filas en «What you get»/«Qué obtienes»: tiering configurable, arquitecto, TDD como skill, revisor de solo lectura; tabla desplegable con los 10 agentes; lista de skills), `.claude-plugin/plugin.json` + `marketplace.json` descriptions (agentes + skill `tdd`, sincronizadas), `CLAUDE.md` (tabla de agentes + skills + regla de tiering con `effort`), `agent-kits/shared/README.md` (`model-tier.py`, `docs-style`), `docs/FLOWS.md` + EN (ya en T-02/T-04), fila en `docs/roadmap/README.md`; `evals/cases/agent-architect.json` («diseña la arquitectura» vs negativo «planifica las tareas» → `agent:planner`), `agent-reviewer.json` (T-04), `skill-tdd.json` (T-03); ADR-009 «tiering configurable en dos capas» (`estado: propuesta`) + fila en `docs/knowledge/README.md`. Cierre: DoD global, ledger a `completado`.
- **Estado**: completado
- **Tiempo humano**: est. 1,5h · real 1,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,05h · real 0,05h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `docs/README.md`, `docs/en/README.md`, `README.md`, `README.es.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CLAUDE.md`, `agent-kits/shared/README.md`, `docs/roadmap/README.md`, `evals/cases/agent-architect.json` (nuevo en T-02), `evals/cases/agent-reviewer.json` (T-04), `evals/cases/skill-tdd.json` (T-03), `docs/knowledge/adr/ADR-009-tiering-configurable-en-dos-capas.md` (nuevo), `docs/knowledge/README.md`, `docs/roadmap/2026-09-03-parity-core/tasks.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `308 passed` (290 + 18: 12 model-tier, 3 task-brief tdd, 2 rationalization, 1 skill-size) · 17 suites-script sin fallo · `test_manifests: 4/4 OK (13 skills · 11 comandos · 10 agentes …)` · `test_readme_badges: 6 badge(s) verificados OK` · `test_skill_size: 13/13 OK` · `lint_plugin: 10 agentes · 0 errores · 3 avisos` (los 3 previos: `retro`/`roadmap-status`/`setup` genéricos) · `evals/check: 34 ficheros · 108 casos (68 positivos, 40 negativos) · 34 piezas · 0 errores` · `ledger-lint: 0 incoherencias · 0 avisos` · `scope-check … 54 fichero(s) · fuera de alcance (0)` exit 0 · `skill-index` → `40 3465` (≤ 45 líneas / ≤ 3.500 caracteres con 34 piezas, sin tocar el recorte) · `ls … | grep -c ADR-009` → 1; TDD n/a: prosa/config): `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → passed (290 + nuevos) · `python3 tests/test_manifests.py` → `4/4 OK (13 skills · 11 comandos · 10 agentes …)` · `python3 tests/test_readme_badges.py` → OK · `python3 tests/test_skill_size.py` → OK · `python3 scripts/lint_plugin.py` → `10 agentes · 0 errores` · `python3 evals/check.py` → `34 piezas … 0 errores` exit 0 · `python3 agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-03-parity-core/tasks.md` → exit 0 · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-parity-core --base b8add37` → exit 0 · `python3 agent-kits/shared/skill-index.py --no-cache | python3 -c "import sys;t=sys.stdin.read();print(len(t.splitlines()), len(t))"` → ≤ 45 líneas y ≤ 3.500 caracteres · `ls docs/knowledge/adr | grep -c ADR-009` → 1.

**Criterios de aceptación**
- [x] Badges `agents-10` / `agentes-10` y `skills-13` en ambos README; filas nuevas en «What you get»/«Qué obtienes» y en la tabla desplegable de agentes; `test_readme_badges` verde.
- [x] `plugin.json` y `marketplace.json` listan `architect`, `reviewer` y la skill `tdd` (descriptions idénticas); `test_manifests` 4/4 con 13 skills · 11 comandos · 10 agentes.
- [x] `docs/README.md` + EN con filas de `architect`, `reviewer`, `tdd`; `CLAUDE.md` tabla de agentes (10) y skills (13) + regla de tiering con `effort`; `agent-kits/shared/README.md` con `model-tier.py` y `docs-style`; fila `parity-core` en `docs/roadmap/README.md`.
- [x] `evals/cases/agent-architect.json`, `agent-reviewer.json`, `skill-tdd.json` con 2 positivos + 1 negativo vecino cada uno; `evals/check.py` 34 piezas, 0 errores.
- [x] ADR-009 con la plantilla (`estado: propuesta`, `iniciativa: parity-core`) y fila en `docs/knowledge/README.md`.
- [x] **Cierre medido:** suites verdes, `lint_plugin` 0 errores (avisos listados), `evals/check` exit 0, `ledger-lint` exit 0, `scope-check --base b8add37` exit 0, `skill-index` dentro de límites con 34 piezas.

---

**Verificación de cierre (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → 308 passed · 17 suites-script OK · `lint_plugin` 10 agentes · 0 errores · 3 avisos (los 3 previos) · `evals/check` 34 piezas · 0 errores · `ledger-lint` 0/0 · `scope-check --base b8add37` 54 ficheros en alcance, 0 fuera · `skill-index` 40 líneas / 3.465 caracteres · `model-tier.py architect --json` → `opus/high` (frontmatter) y `sonnet` con `dev.json` de prueba. Pendiente de la **revisión de dos lentes** (la lanza el orquestador; primera revisión con el agente `reviewer`). `CHANGELOG*.md` sin tocar (lo hace el orquestador).

**Contradicciones/dudas con la doc oficial (2026-09-03):**
- `effort` oficial admite `low|medium|high|xhigh|max` (no solo tres): el linter acepta los cinco; la tabla de tiering usa `medium`/`high`.
- El Agent tool documenta parámetro `model` por invocación (prioridad 1 desde v2.1.251) pero **no** `effort` → el `effort` de `dev.json` se declara informativo en CONVENTIONS, setup, orquestadores y `model-tier.py`.
- `plugins-reference.md` dice que `${CLAUDE_PLUGIN_ROOT}` se sustituye en «Skill and agent content — anywhere the placeholder appears», lo que contradice la premisa de la regla 5 de CONVENTIONS («no se expande en markdown»); se mantiene `find` (cubre la instalación como `.claude/` copiado y el paquete portable) y queda registrado como candidato a simplificación en `claude-code-contracts.md`.
- El tope de 10.000 caracteres del hook que cita CONVENTIONS regla 8 **no se localizó** en la lectura de hoy de `hooks.md`: marcado «a re-verificar».
- `model` oficial admite además `fable` e ids completos `claude-…`: el linter sigue restringiendo el frontmatter a `haiku|sonnet|opus|inherit` (tiering); `dev.json` `modelos` sí admite ids completos (los valida `model-tier.py`).

## Revisión de dos lentes — intento 1: 0 Critical · 2 Important · 6 Minor + 2 decisiones (todos verificados y corregidos en `T-fix1`)

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| I1 | Important | La «validación por trozos» del `architect` era imposible como subagente (devuelve UN mensaje): promesa contradictoria en el prompt | T-02 | Contrato en **dos pasadas** (patrón go/no-go): pasada 1 → `design.md` `borrador` + `opcion_elegida: pendiente` + resumen estructurado (una línea por opción + recomendada + pregunta); el **orquestador** presenta por trozos (AskUserQuestion en Cowork / lista numerada en CLI), recoge la elección y re-invoca con `elegida: O<n>`; pasada 2 → fija opción, ADR, enlaces, `aprobado`. Modo manual `@architect` sí dialoga. Reescritos `agents/architect.md` P3/P6/DoD/tabla/description, plantilla (sección 4 con «Recomendación»), `commands/pm-cycle.md` 2-bis, `commands/dev-cycle.md` 2-a, `docs/agents/architect.md`, FLOWS ES/EN (etiquetas «2 pasadas · elección del usuario · ADR») | `grep -c "elegida: O" agents/architect.md commands/pm-cycle.md commands/dev-cycle.md` → 3 · 2 · 2; eval `agent-architect.json` sin cambios necesarios (los prompts no prometen diálogo), `evals/check` 0 errores |
| I2 | Important | Regla 7 (`design:`) no estaba en las plantillas; `roadmap-dashboard` ignoraba `design.md` | T-02 | `design: n/a` en `agent-kits/evaluator/templates/spec.md` e `improvement-plan.md` (+ fila **Diseño**), fila **Diseño** en la cabecera de `tasks.md`; `build_dashboard.py` lee `design.md` (`has_design`, `design_estado`, `generacion` del diseño), columna «diseño» en HTML y Markdown | `tests/test_dashboard.py` (+4 asertos, fixture `2026-01-10-alpha/design.md`) OK · `build_dashboard.py --root docs/roadmap --strict` exit 0 · sobre la fixture, exit 1 solo por el aviso preexistente de `beta` |
| D1 | Decisión | Hook de guardia para `architect` | T-02 | `guardrail-check.py pre-tool --agent implementer\|architect` (o `CLAUDE_AGENT_NAME`): architect permite solo `docs/roadmap/<inic>/design.md`, `docs/knowledge/adr/**` + `README.md`, y `spec.md`/`improvement-plan.md` SOLO con `Edit` cuyo `old_string`/`new_string` contenga `design:`/`design.md` (Write completo → deny; riesgo documentado: un Edit con `design:` + otros cambios pasa — lo caza la Lente A); deny del resto (código, `tasks.md`, `evaluation.md`, gotchas); git igual; sin regla de rama. Wrapper `hooks/architect-guardrail.sh` (`GUARDRAIL_AGENT=architect` → `implementer-guardrail.sh`), frontmatter `hooks:` del architect igual al del implementer, enmienda en ADR-007 | `test_guardrail_check.py` 31 passed (+7 architect, incl. CLI `--agent` y env) · `test_hooks_shell.py` 26 passed (+2 architect-guardrail) · `lint_plugin` 0 errores (hook existe y es ejecutable) |
| D2 | Decisión | Política Confluence: `design.md` se publica (decisión de arquitectura, como spec y ADR) | T-02 | Nota en `_comment_exclude` de `confluence.example.json`, fila D1 de `config-and-policy.md`, «Dentro» de `confluence-publish/SKILL.md`, fila `architect` en la matriz de FLOWS ES/EN (11 disparadores); fixture `init1/design.md` + asertos en `test_confluence_scope.py` (en alcance, no excluido; staging lo copia) | `test_confluence_scope.py` 25 passed |
| M1 | Minor | CONVENTIONS regla 10 (+EN) y `knowledge-write.md` no listaban a `architect` como escritor de ADR | T-05 | Añadido en ambos + fila `templates/adr.md` y `knowledge-write.md` del README de shared | `grep -c architect docs/CONVENTIONS.md docs/en/CONVENTIONS.md agent-kits/shared/knowledge-write.md` → ≥ 1 cada uno |
| M2 | Minor | `guardrail-check.py` razón del deny nombraba a `planner` para `design.md` | T-02 | Razón específica «el diseño lo cambia architect» cuando el fichero es `design.md` | `test_implementer_razon_design_nombra_a_architect` OK |
| M3 | Minor | `model-tier.py`: `dev.json` con raíz no-objeto callaba | T-01 | Aviso «la raíz no es un objeto JSON» como el resto de casos | `test_dev_json_raiz_no_objeto_avisa` OK (13 passed) |
| M4 | Minor | `agents/reviewer.md`: `<base>..HEAD` vs `<base>...HEAD`; tabla de gaps sin columna `Tarea` | T-04 | Unificado a `git diff <base>...HEAD`; columna `Tarea (T-XX u objetivo)` + regla «la fusión no la infiere» | `grep -c "<base>..HEAD" agents/reviewer.md` → 0 |
| M5 | Minor | `docs/en/FLOWS.md` etiqueta mermaid en español («design aprobado») | T-02 | «design approved» (+ etiquetas «2 passes: options → user choice → ADR») | `test_mermaid_blocks: 30 diagrama(s) OK` |
| M6 | Minor | `task-brief.py` no inyectaba la opción elegida de `design.md` | T-03 | Sección «Diseño (design.md · opción elegida O<n>)» con SOLO la sección «opción elegida y por qué» (token-diet); `borrador`/`pendiente` → aviso stderr y nada; dev-cycle y README de shared lo documentan | `test_task_brief.py` 26 passed (+3: inyecta solo la sección, sin design nada, borrador avisa) |

**Verificación tras `T-fix1` (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → **321 passed** (308 + 13) · 17 suites-script OK · `lint_plugin: 10 agentes · 0 errores · 3 avisos` · `evals/check: 34 piezas · 108 casos · 0 errores` · `diet-check adversarial-review` 0 perdidos (197 líneas) · `ledger-lint` 0/0 · `scope-check --base b8add37` exit 0 · `build_dashboard.py --root docs/roadmap --strict` exit 0.

## Revisión de dos lentes — intento 2: sin gaps (re-verificación del orquestador: guardia del architect allow design.md/ADR · deny código y spec salvo Edit con `design:`; dev-cycle 2-a con validación por trozos a cargo del orquestador; 321 tests). ADR-009 promovida a `aceptada (validada: revisión de dos lentes, 2026-09-03, intento 2)`.
