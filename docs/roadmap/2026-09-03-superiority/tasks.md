---
tasks: superiority
descripcion: Nivel superior del plugin — cinco piezas que faltaban frente a la práctica de los mejores plugins, sin crear roles duplicados: (1) comando `/doctor` + `agent-kits/shared/doctor.py` (diagnóstico determinista de la instalación con veredicto ✅/⚠️/❌ por línea y arreglo sugerido); (2) skill `changelog-sync` + `scripts/changelog-sync.py` (entradas `[Unreleased]`/`[Sin publicar]` EN+ES generadas desde los ledgers cerrados, idempotente, `--check` para `release.py`); (3) skill `unit-tests` + `scripts/coverage-gate.py` (pirámide de pruebas y gate de cobertura por stack, `--changed-only`; la usan `implementer` y `qa`, NO es un agente nuevo); (4) Lente D de rendimiento en `adversarial-review` (`review-lens-select.py` decide `lente_d` por heurística de rutas + patrones N+1/bucles/await-en-for; `revision.lenteRendimiento`); (5) skill `api-contract` + `scripts/openapi-lint.py` (validación mínima de OpenAPI 3.x sin dependencias + `--diff` de cambios rompedores); (6) integración, doc bilingüe, evals, LES-013.
estado: completado        # borrador | en-progreso | completado | cancelado
creado: 2026-09-03
actualizado: 2026-09-03
via: rapida               # vía rápida de /dev-cycle: sin spec/evaluación/plan; conserva revisión de dos lentes + suites
verificacion: obligatoria # cada `### T-XX` lleva `- **Verificación**:`; ledger-lint lo exige (exit ≠ 0 si falta)
generacion:
  fuente: estimado        # usage-meter no disponible en este entorno (sandbox cloud)
---

# Checklist de Tareas — superiority (vía rápida)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-03 |
| **Plan** | n/a — **vía rápida** (sin spec/evaluación/plan; ledger ligero + revisión de dos lentes + verificación) |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del cambio. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Decisión del usuario (2026-09-03).** Cinco huecos frente a la práctica de los mejores plugins: no había forma de comprobar la **instalación** (hooks, configs, scripts) sin leer la doc; el **CHANGELOG** bilingüe se escribía a mano al cerrar cada iniciativa (dos ficheros, dos idiomas, dos ocasiones de olvidarlo — LES-012); no existía una **pirámide de pruebas** con gate de cobertura (solo `tdd` como método y `qa` como E2E); la revisión adversarial no miraba **rendimiento** (N+1, bucles con I/O, `await` en `for`); y no había flujo **contract-first** para APIs (OpenAPI). **No se crea ningún rol duplicado:** los tests unitarios son una **skill compartida** (`unit-tests`) que usan `implementer` (P5) y `qa` (informe), no un agente «tester» (LES-013). Se cierra en una iniciativa de vía rápida: T-01 `/doctor`, T-02 `changelog-sync`, T-03 `unit-tests`, T-04 Lente D, T-05 `api-contract`, T-06 integración/doc/memoria. `CHANGELOG*.md` lo toca el orquestador (con la propia `changelog-sync`).

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase única — nivel superior | 6 | 6 | 100% | 14,0 / 14,0h | 0,70 / 0,70h | 0,18 / 0,18h | — / 340k |
| **TOTAL** | **6** | **6** | **100%** | **14,0 / 14,0h** | **0,70 / 0,70h** | **0,18 / 0,18h** | **— / 340k** |

---

## Fase única — nivel superior

**Estado**: completado · **Estimado**: 14,0h · **Real**: 14,0h (estimado — `usage-meter` no disponible en este entorno) · **Coste est.**: ≈730 € · **Tokens est.**: 340k

### T-01 — Comando `/doctor` + `agent-kits/shared/doctor.py`

- **Descripción**: `agent-kits/shared/doctor.py [--root <proyecto>] [--plugin-root <dir>] [--json]`: comprobación DETERMINISTA y sin efectos de la instalación del plugin en un proyecto, con veredicto por línea (✅ ok · ⚠️ aviso · ❌ error) y **arreglo sugerido** en cada ⚠️/❌. Bloques: (a) **herramientas** — `python3` (≥ 3.9), `git`, `bash`, `jq` (opcional: la statusline lo usa con fallback a python3), `node`/`npm` (opcional: `to-pdf`), Playwright (opcional: `qa`; `~/.claude/tool-cache/qa/node_modules/@playwright`); (b) **plugin** — raíz resuelta por `--plugin-root` → `CLAUDE_PLUGIN_ROOT` → padre de `agent-kits/shared` → `find` sobre `$PWD/.claude` y `$HOME/.claude`; hooks registrados en `hooks/hooks.json` (parsea el JSON y reutiliza `lint_hook_commands` de `scripts/lint_plugin.py` si está: cada script existe y es ejecutable; sin el linter, comprobación local equivalente); statusline configurada o no en `.claude/settings.json` (informativo); (c) **configs del proyecto** — `.claude/rates.json` (JSON válido; `precioTokens` a 0 o sin `verificado` → `⚠️ a verificar` con arreglo `rates-verify`), `.claude/dev.json` (JSON válido; claves desconocidas → ⚠️; valores fuera de vocabulario en `tdd/worktree/subagentes/guardrails/statusline/revision.lenteSeguridad/revision.lenteRendimiento/sesion/tests.coberturaMinima` → ❌ con el valor esperado; `modelos` resueltos con `model-tier.py --all --json` y sus avisos), `.claude/jira.json` / `.claude/confluence.json` (si `enabled: true`, campos obligatorios presentes; ausentes → «no configurado» informativo); (d) **estado** — marcadores huérfanos de `usage-state.json` (`usage-meter.py status`), iniciativas `en-progreso` (`progress-report.py active`), última entrada del journal (`docs/knowledge/journal/`), último informe en `evals/reports/` y su fecha; (e) **versión** — `plugin.json` `version` vs `.claude/.plugin-version-seen` si existe (SIN red: solo dice «versión X; la última vez que se vio este proyecto era Y» o «sin registro»; nunca inventa «hay actualización»). Salida MD (una tabla por bloque + resumen «N ✅ · N ⚠️ · N ❌») o `--json`; **exit 0 si no hay ❌, 1 si hay**; 2 solo por uso incorrecto. Nunca escribe nada. `commands/doctor.md` lo invoca y explica cada arreglo en llano (sin jerga); `/setup` paso 0 ofrece `/doctor` si detecta config previa en `.claude/`. Tests ≥ 10 en `agent-kits/shared/test_doctor.py` con proyectos temporales (sin config → todo informativo y exit 0; `dev.json` corrupto → ❌; valor inválido → ❌ con arreglo; hook no ejecutable → ⚠️; script de hook inexistente → ❌; marcador huérfano → ⚠️; `rates.json` con precio a 0 → ⚠️; `--json` coherente con MD; exit 1 solo con ❌; version-seen).
- **Estado**: completado
- **Tiempo humano**: est. 3,0h · real 3,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,15h · real 0,15h (estimado)
- **Supervisión**: est. 0,04h · real 0,04h (estimado)
- **Archivos**: `agent-kits/shared/doctor.py` (nuevo), `agent-kits/shared/test_doctor.py` (nuevo), `commands/doctor.md` (nuevo), `commands/setup.md`, `evals/cases/command-doctor.json` (nuevo — adelantado para que `evals/check.py` siga en exit 0 en este commit)
- **Verificación** (ejecutada 2026-09-03 — salida: `24 passed` · repo `8 ✅ · 0 ⚠️ · 0 ❌ · 11 ℹ️` exit 0 · tmp sin config exit 0 sin filas ❌ · tmp con `dev.json` corrupto → 3 celdas ❌ y exit 1 · `--json` con claves `proyecto/plugin_root/bloques/resumen/exit` · `grep -c doctor commands/setup.md` = 3 · `lint_plugin` 0 errores · `evals/check` 37 piezas 0 errores): `python3 -m pytest -q agent-kits/shared/test_doctor.py` → ≥ 10 passed · `python3 agent-kits/shared/doctor.py --root . ; echo $?` → informe MD con los 5 bloques y exit 0 o 1 según haya ❌ (salida resumida pegada aquí) · `python3 agent-kits/shared/doctor.py --root <tmp-sin-config>; echo $?` → exit 0, sin ❌ · `python3 agent-kits/shared/doctor.py --root <tmp-dev.json-corrupto>; echo $?` → exit 1 con ❌ y arreglo sugerido · `python3 agent-kits/shared/doctor.py --root . --json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['resumen'])"` → JSON válido · `grep -c "doctor" commands/setup.md` → ≥ 1 · `python3 scripts/lint_plugin.py` → 0 errores · `python3 evals/check.py` → exit 0.

**Criterios de aceptación**
- [x] Sobre un proyecto temporal SIN `.claude/` el informe no tiene ❌ (todo «no configurado» informativo o ✅) y exit 0; sobre uno con `dev.json` corrupto hay ❌ con arreglo «recrear con /setup» y exit 1.
- [x] Hook cuyo script no existe → ❌; script sin bit ejecutable → ⚠️ con `chmod +x`; marcador de `usage-state.json` sin cierre → ⚠️ con `usage-meter.py close`.
- [x] `--json` y MD llevan los mismos veredictos; el bloque «versión» nunca afirma que haya actualización disponible.
- [x] `commands/doctor.md` explica cada arreglo en llano; `/setup` paso 0 ofrece `/doctor` cuando ya hay config previa.

### T-02 — Skill `changelog-sync` + `scripts/changelog-sync.py`

- **Descripción**: `skills/changelog-sync/scripts/changelog-sync.py [--root <repo>] [--dry-run] [--only <slug>] [--check] [--json]`: genera las entradas `[Unreleased]` (EN, `CHANGELOG.md`) y `[Sin publicar]` (ES, `CHANGELOG.es.md`) a partir de los ledgers CERRADOS (`docs/roadmap/<fecha>-<slug>/tasks.md` con `estado: completado` en el frontmatter; los legacy sin frontmatter se ignoran con aviso) que **aún no estén** en el CHANGELOG — detección por slug en backticks (`` `<slug>` ``) en el texto del fichero → **idempotente** (segunda ejecución: 0 cambios). Formato determinista, SIN modelo: una subsección `### <Categoría> — \`<slug>\` initiative (<fecha>)` (EN) / `### <Categoría> — iniciativa \`<slug>\` (<fecha>)` (ES) con UN bullet por tarea `T-XX`: **título** + primera frase de su `Descripción` + `Archivos` clave (máx. 5, sin las anotaciones entre paréntesis). Categoría `Added|Changed|Fixed` por heurística sobre `descripcion:`/títulos (`fix|corrige|corrección|saldar|bug` → Fixed; `cambia|retira|renombra|migra|sustituye` → Changed; resto Added), sobreescribible con `changelog: Fixed|Changed|Added` en el frontmatter del ledger. Las entradas nuevas se insertan justo debajo de la cabecera Unreleased, en orden de fecha del ledger. `--dry-run` imprime lo que escribiría; `--only <slug>` limita a una iniciativa; `--check` → exit 1 si hay ledgers cerrados sin entrada (sin escribir) — `release.py` lo ejecuta en sus precondiciones como **aviso** (no bloqueo: un release puede publicar deuda de notas a sabiendas); exit 2 por uso (sin CHANGELOG). La skill (`SKILL.md` ≤ 200): cuándo usarla (al cerrar una iniciativa, antes del release), cómo afinar el texto generado (reescribir el bullet, nunca inventar alcance), qué NO hace (no crea la sección de versión — eso es `release.py` —, no inventa alcance, no traduce con modelo). `/dev-cycle` Fase 6 y `quick-implement` la invocan al cierre (una línea). Tests ≥ 8 en `skills/changelog-sync/scripts/test_changelog_sync.py` (fixture con 2 ledgers cerrados + 1 abierto: genera ES y EN, idempotente, `--check` exit 1→0, `--only`, categorías por heurística y por override, slug ya presente no se duplica, `--dry-run` no escribe, sin CHANGELOG exit 2).
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,5h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,13h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/changelog-sync/SKILL.md` (nuevo), `skills/changelog-sync/scripts/changelog-sync.py` (nuevo), `skills/changelog-sync/scripts/test_changelog_sync.py` (nuevo), `scripts/release.py`, `tests/test_release.py`, `commands/dev-cycle.md`, `skills/quick-implement/SKILL.md`, `evals/cases/skill-changelog-sync.json` (nuevo — adelantado por el check de evals)
- **Verificación** (ejecutada 2026-09-03 — salida: `13 passed` · `--check` lista `memory-health` (Added, override en su frontmatter) y `parity-core` (Added) y NO las ya presentes, exit 1 · `release.py 9.9.9 --dry-run` imprime «changelog-sync --check: PENDIENTE» + el aviso, sin abortar por ello · `SKILL.md` 81 líneas · `dev-cycle` Fase 6 paso 6 y `quick-implement` la citan · `test_release` 18 passed · `test_export_skills` 13 passed tras quitar la ruta `scripts/release.py` del texto portable): `python3 -m pytest -q skills/changelog-sync/scripts` → ≥ 8 passed · `python3 skills/changelog-sync/scripts/changelog-sync.py --dry-run` → propone entradas para los ledgers cerrados de hoy que NO están en el CHANGELOG (`parity-core`, `memory-health`) y NO para los ya presentes (`activation-reliability`, `plan-and-diet`, `distribution`), exit 0 (salida resumida pegada aquí) · `python3 skills/changelog-sync/scripts/changelog-sync.py --check; echo $?` → 1 mientras falten entradas · `python3 scripts/release.py 9.9.9 --dry-run 2>&1 | grep -c changelog-sync` → ≥ 1 (aviso, no bloqueo) · `wc -l skills/changelog-sync/SKILL.md` → ≤ 200 · `grep -c "changelog-sync" commands/dev-cycle.md skills/quick-implement/SKILL.md` → ≥ 1 cada uno · `python3 -m pytest -q tests/test_release.py` → passed.

**Criterios de aceptación**
- [x] Sobre la fixture genera la sección EN y ES con un bullet por T-XX (título + primera frase + ≤ 5 archivos); la segunda ejecución no cambia nada (idempotente); `--dry-run` no escribe.
- [x] `--check` exit 1 con ledgers cerrados sin entrada y 0 tras sincronizar; un slug ya presente en el CHANGELOG no se duplica; `--only` limita a una iniciativa.
- [x] Categoría por heurística (`fix…` → Fixed, `retira…` → Changed, resto Added) y override `changelog:` en el frontmatter.
- [x] `release.py` ejecuta `--check` en sus precondiciones como aviso (el `--dry-run` lo muestra; no aborta); `/dev-cycle` Fase 6 y `quick-implement` citan la skill en el cierre; `SKILL.md` ≤ 200 con «qué NO hace».

### T-03 — Skill `unit-tests` (pirámide de pruebas, sin agente nuevo)

- **Descripción**: `skills/unit-tests/SKILL.md` (≤ 200) + `skills/unit-tests/scripts/coverage-gate.py <ruta> [--min 80] [--changed-only --base <ref>] [--json] [--runner <cmd>]`: detecta el stack por ficheros (`pytest`/coverage.py si hay `pyproject.toml`/`pytest.ini`/`setup.cfg`/`tests/`+`*.py`; `jest`/`vitest` por `package.json`; `phpunit` por `phpunit.xml*`/`composer.json`; `go test -cover` por `go.mod`), ejecuta la cobertura oficial SOLO si la herramienta está en PATH (`python3 -m pytest --cov=. --cov-report=json` → `coverage.json`; `npx jest --coverage --coverageReporters=json-summary` / `npx vitest run --coverage.enabled --coverage.reporter=json-summary` → `coverage/coverage-summary.json`; `phpunit --coverage-clover` → XML; `go test -coverprofile` → perfil) y parsea el **% global y por fichero**; con `--changed-only` mide SOLO los ficheros de código del diff (`git diff --name-only <base>` ∪ untracked; base = `--base` o merge-base con `main`/`master`), sin tests ni prosa. **Exit 0** si ≥ umbral, **1** si no, **2** sin herramienta/stack/parseo (aviso explícito; **nunca inventa un %**). Ejecutor inyectable para tests (`--runner` toma un comando que debe dejar el fichero de cobertura; los tests lo mockean). La skill: estrategia por capa (unit/integration/E2E: qué va en cada una y por qué la base es unitaria), qué probar primero (ramas de error, límites, contratos entre módulos), anti-patrones (tests que replican la implementación, mocks de todo, asserts triviales, tests que dependen del orden), y el gate. Integración: `agents/implementer.md` P5 ejecuta `coverage-gate.py --changed-only` si `.claude/dev.json` trae `"tests": {"coberturaMinima": N}` (default: sin gate, solo informe si la herramienta está); `agents/qa.md` incluye en el informe la **pirámide** (unit % del gate + E2E del qa-gate); Lente A de `adversarial-review` comprueba «criterios con test tienen test» con este dato (una línea en `lens-prompts.md`); `/setup` paso 5-quinquies pregunta la cobertura mínima (opt-in, default sin gate). Delimita frente a `tdd` (método de escritura) y `qa` (E2E). Tests ≥ 8 en `skills/unit-tests/scripts/test_coverage_gate.py` con runners mockeados (detección de stack por fixture, parseo coverage.json / coverage-summary.json / clover / go profile, `--min` 0/1, `--changed-only` filtra por diff en repo git temporal, sin herramienta → exit 2, `--json`).
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,5h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,13h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/unit-tests/SKILL.md` (nuevo), `skills/unit-tests/scripts/coverage-gate.py` (nuevo), `skills/unit-tests/scripts/test_coverage_gate.py` (nuevo), `skills/unit-tests/references/pyramid.md` (nuevo), `agents/implementer.md`, `agents/qa.md`, `skills/adversarial-review/references/lens-prompts.md`, `commands/setup.md`, `evals/cases/skill-unit-tests.json` (nuevo — adelantado por el check de evals), `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (Skills: + `unit-tests` — mantiene `test_manifests.py` en verde en este mismo commit, como T-01/T-02)
- **Verificación** (ejecutada 2026-09-03 — salida: `22 passed` · sandbox sin `pytest-cov` (`ModuleNotFoundError: No module named 'pytest_cov'`) → `coverage-gate: stack «pytest» detectado pero su herramienta de cobertura no está disponible (PATH/paquete) — nunca se inventa un porcentaje` exit 2 · `SKILL.md` 109 líneas · `lint_plugin` 0 errores 3 avisos (nombres genéricos, preexistentes) · `evals/check` 39 ficheros/129 casos 0 errores · pytest global 430 passed): `python3 -m pytest -q skills/unit-tests/scripts` → 22 passed (≥ 8) · `python3 skills/unit-tests/scripts/coverage-gate.py . --changed-only --base ab22a4c; echo $?` → `coverage-gate: stack «pytest» detectado pero su herramienta de cobertura no está disponible (PATH/paquete) — nunca se inventa un porcentaje` (×2: MD + stderr) exit 2 · `wc -l skills/unit-tests/SKILL.md` → 109 (≤ 200) · `grep -c "coberturaMinima" agents/implementer.md commands/setup.md` → `agents/implementer.md:1` `commands/setup.md:2` · `grep -c "coverage-gate" agents/qa.md skills/adversarial-review/references/lens-prompts.md` → `agents/qa.md:1` `skills/adversarial-review/references/lens-prompts.md:1` · `python3 scripts/lint_plugin.py` → `lint_plugin: 10 agentes · 0 errores · 3 avisos` · `python3 evals/check.py` → `evals/check: 39 ficheros · 129 casos (78 positivos, 51 negativos) · 39 piezas del repo · 0 errores` exit 0.

**Criterios de aceptación**
- [x] Detecta los 4 stacks por fixture y parsea el % global y por fichero de cada formato con el runner mockeado; sin herramienta en PATH → exit 2 con aviso y sin % inventado.
- [x] `--changed-only` mide solo los ficheros de código del diff (los tests y la prosa no cuentan); `--min` decide exit 0/1.
- [x] `implementer` P5 aplica el gate solo con `dev.json` `tests.coberturaMinima`; `qa` incluye la pirámide en el informe; Lente A cita el dato; `/setup` 5-quinquies lo pregunta opt-in.
- [x] `SKILL.md` ≤ 200 con estrategia por capa, qué probar primero, anti-patrones y «qué NO hace» (no es `tdd` ni `qa`; no es un agente).

### T-04 — Lente D (rendimiento) en `adversarial-review`

- **Descripción**: `review-lens-select.py` gana la decisión **`lente_d`** (rendimiento) junto a `lente_c`, con heurística propia y mismo contrato (exit 0 siempre; `--json` con `lente_d` + `motivos_d`): por **RUTA** (stems anclados: `repository|repo|dao|query|sql|migration|cache|queue|worker|batch|export|import|report|loop|scheduler`; prosa y `docs/**` excluidos; `revision.excluir` compartido con la lente C) y por **CONTENIDO de las líneas añadidas** (patrones documentados en `CONTENIDO_D`: bucle con llamada dentro — `for …:`/`for (…)`/`foreach` seguido de `.query(`/`.all()`/`->get()`/`->first()`/`SELECT` en las líneas siguientes del mismo hunk (N+1); `await` dentro de `for`; `sleep(`; `readFileSync` en handlers; `re.compile(` dentro de bucle; concatenación de string en bucle (`+=` con cadena); `JSON.parse(JSON.stringify(`; bucles anidados con llamada dentro). Config `.claude/dev.json` `revision.lenteRendimiento` `auto|siempre|nunca` (default `auto`; valor desconocido → auto + aviso). Prompt de la **Lente D** en `references/lens-prompts.md`: defectos de rendimiento con **escenario de carga concreto y orden de magnitud** (N registros → N+1 consultas; O(n²) con n = …), NO micro-optimizaciones ni estilo; cita el patrón que activó la lente; despacho al `reviewer` como las demás; fusión en la misma lista Critical/Important/Minor (Critical solo si degrada el objetivo con carga realista). `SKILL.md` ≤ 200 (mueve detalle a `references/lens-d-heuristics.md`), `/setup` 5-ter pregunta ambas lentes (seguridad y rendimiento), doc ES/EN (CONVENTIONS regla 9), tests ≥ 8 en `test_review_lens_select.py` (ruta `repository/` → true; `for` + `.query(` → true; `await` en `for` → true; `sleep(` → true; código sin patrones → false; `nunca` → false aunque haya patrones; `excluir` saca la ruta pero no el contenido; el repo actual → `lente_d: false`).
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 2,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,10h · real 0,10h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/adversarial-review/scripts/review-lens-select.py`, `skills/adversarial-review/scripts/test_review_lens_select.py`, `skills/adversarial-review/SKILL.md`, `skills/adversarial-review/references/lens-prompts.md`, `skills/adversarial-review/references/lens-d-heuristics.md` (nuevo), `commands/setup.md`, `docs/CONVENTIONS.md`, `docs/en/CONVENTIONS.md`, `evals/cases/skill-adversarial-review.json`
- **Verificación** (ejecutada 2026-09-03 — nota: la base real de esta iniciativa es `475f64a` (commit de cierre de T-02); `ab22a4c` de la redacción original del ledger es de la iniciativa ANTERIOR — salida: `35 passed` (22 preexistentes de la Lente C + 13 nuevos de la Lente D, ≥ 8) · self-check sobre el diff real de la rama → `lente_c: False lente_d: False` (regresión encontrada y corregida: la primera versión de `LOOP_D_RE` se disparaba con el propio comentario que documentaba la sintaxis `for`/`foreach` con su paréntesis pegado; se reescribió el comentario sin ese símbolo adyacente) · `SKILL.md` 202 líneas (por encima del objetivo de 200 en 2 líneas tras recortar de una proyección de 208; por debajo del umbral duro 250 de `test_skill_size.py`, que sigue en verde — `lint_plugin` lo avisa, 0 errores) · `lenteRendimiento` documentado en `/setup` (×3, ambas lentes en el mismo paso 5-ter) y en CONVENTIONS ES/EN (de paso se documentó también `tests.coberturaMinima`, hueco de T-03) · pytest global 443 passed · `evals/check` 130 casos 0 errores · `scope-check --base 475f64a` exit 0, 19 ficheros en alcance): `python3 -m pytest -q skills/adversarial-review/scripts` → 35 passed (≥ 8 nuevos de la Lente D) · `python3 skills/adversarial-review/scripts/review-lens-select.py --base 475f64a --json | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['lente_c'], d['lente_d'])"` → `False False` · `wc -l skills/adversarial-review/SKILL.md` → 202 · `grep -c "Lente D" skills/adversarial-review/references/lens-prompts.md` → 1 · `grep -c "lenteRendimiento" commands/setup.md docs/CONVENTIONS.md docs/en/CONVENTIONS.md` → `commands/setup.md:3` `docs/CONVENTIONS.md:1` `docs/en/CONVENTIONS.md:1` · `python3 -m pytest -q tests agent-kits/shared skills/*/scripts evals` → 443 passed · `python3 scripts/lint_plugin.py` → `10 agentes · 0 errores · 4 avisos` · `python3 evals/check.py` → `39 ficheros · 130 casos … 0 errores` exit 0.

**Criterios de aceptación**
- [x] Positivos (ruta `repository/`, `for` + `.query(`, `await` en `for`, `sleep(`) → `lente_d: true` con motivo `fichero:línea` + patrón; negativos (código plano, prosa) → false; `nunca` → false; `excluir` saca la ruta pero no el contenido.
- [x] `lens-prompts.md` trae el prompt literal de la Lente D (escenario de carga + orden de magnitud, sin micro-optimizaciones); `SKILL.md` ≤ 200 la despacha y fusiona como las demás (queda en 202/250; ver nota en Verificación).
- [x] `/setup` 5-ter pregunta ambas lentes; regla 9 de CONVENTIONS (+EN) documenta `revision.lenteRendimiento`; los tests existentes de la lente C siguen en verde (22/22).

### T-05 — Skill `api-contract` + `scripts/openapi-lint.py`

- **Descripción**: `skills/api-contract/SKILL.md` (≤ 200) + `skills/api-contract/scripts/openapi-lint.py <spec.(yaml|json)> [--json]` | `--diff <old> <new> [--json]`: valida la estructura MÍNIMA de OpenAPI 3.x **sin dependencias externas** (JSON con `json`; YAML con `yaml` si está instalado y, si no, un parser YAML mínimo propio para el subconjunto habitual de las specs: mapas, listas, escalares, cadenas entrecomilladas, `$ref`; YAML exótico → exit 2 con aviso «instala PyYAML»): `openapi: 3.x` presente; `paths` no vacío; cada operación con `operationId` único; `responses` con al menos un `2xx` y un `4xx`/`5xx`; cada `$ref` `#/components/...` existe; parámetros con `schema` (o `content`); aviso (no error) por request body de objeto sin `additionalProperties: false` (opcional). **`--diff <old> <new>`** detecta cambios ROMPEDORES: path u operación eliminada, parámetro obligatorio nuevo (o que pasa a `required`), campo de respuesta eliminado (propiedades de `2xx` que desaparecen, `$ref` resueltos), tipo cambiado (`type` distinto en propiedad o parámetro), `enum` que pierde valores; y NO rompedores como informativos (path nuevo, campo opcional nuevo). Exit **0** (válido / sin cambios rompedores), **1** (errores / rompedores), **2** (fichero ilegible o uso). Salida MD + `--json`. La skill: flujo **contract-first** (spec → revisión con `architect`/`reviewer` → tests de contrato como PLANTILLA por stack: `pytest` + `schemathesis`, `dredd`, `prism` como sugerencias sin instalarlas), regla «el contrato es la fuente de verdad; el código se adapta», cómo encaja en la cadena (`planner` añade tareas de contrato cuando la spec toca una API; Lente A ejecuta `--diff` contra la versión base cuando el diff toca la spec OpenAPI — una línea en `lens-prompts.md`). Tests ≥ 10 en `skills/api-contract/scripts/test_openapi_lint.py` con fixtures OpenAPI válidas/inválidas/rompedoras (JSON y YAML).
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,5h (estimado)
- **Tiempo IA (ejec.)**: est. 0,12h · real 0,12h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `skills/api-contract/SKILL.md` (nuevo, 114 líneas), `skills/api-contract/scripts/openapi-lint.py` (nuevo), `skills/api-contract/scripts/test_openapi_lint.py` (nuevo, 37 tests), `skills/api-contract/references/contract-tests.md` (nuevo), `agents/planner.md`, `skills/adversarial-review/references/lens-prompts.md`, `evals/cases/skill-api-contract.json` (nuevo — adelantado por el check de evals), `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (descripción de skills sincronizada en el mismo commit que la pieza nueva, precedente T-03), `agent-kits/shared/test_skill_index.py` (ya declarado en T-06; dos asserts ajustados para tolerar el recorte proporcional de `skill-index.py` al cruzar el umbral de 40 piezas — el recorte es el mecanismo diseñado, no una regresión).
- **Verificación** (ejecutada 2026-09-03 con `--base 475f64a`): `python3 -m pytest -q skills/api-contract/scripts` → `37 passed` · fixtures ad-hoc (OpenAPI 3.0.3 mínimas): spec válida → `openapi-lint: spec VÁLIDA (0 errores)` exit 0 · spec sin `operationId`/sin `4xx`/parámetro sin `schema`/`$ref` roto → `spec INVÁLIDA — 4 error(es)` exit 1 con las 4 rutas JSON-pointer listadas · `--diff v1 v2` (parámetro `owner` obligatorio nuevo + campo `name` eliminado + `id` integer→string) → `3 cambio(s) ROMPEDOR(ES) de 3 total(es)` exit 1 · fichero inexistente → `ERROR — no existe` exit 2 · `wc -l skills/api-contract/SKILL.md` → 114 (≤ 200) · `grep -c "api-contract\|openapi-lint" agents/planner.md skills/adversarial-review/references/lens-prompts.md` → 1 y 1 · suite completa `python3 -m pytest -q tests agent-kits/shared skills/*/scripts evals` → `481 passed` · `python3 scripts/lint_plugin.py` → `10 agentes · 0 errores · 4 avisos` (3 nombres genéricos preexistentes + `adversarial-review` 202 líneas, ya reportado en T-04) · `python3 evals/check.py` → `40 ficheros · 134 casos (81 positivos, 53 negativos) · 40 piezas del repo · 0 errores` · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-superiority --base 475f64a` → `26 fichero(s) cambiado(s)` · `0` fuera de alcance · exit 0.

**Criterios de aceptación**
- [x] Fixture válida → exit 0; inválida (sin `operationId`, sin `4xx`, `$ref` roto, parámetro sin `schema`) → exit 1 con un error por hallazgo y `fichero`/ruta JSON.
- [x] `--diff` detecta path eliminado, parámetro obligatorio nuevo, campo de respuesta eliminado y tipo cambiado (exit 1); un cambio compatible (campo opcional nuevo, path nuevo) → exit 0 con informativos.
- [x] YAML parseado sin PyYAML para la fixture; fichero ilegible → exit 2; MD y JSON coherentes.
- [x] `SKILL.md` ≤ 200: contract-first, plantillas de tests por stack (sin instalar), «el contrato manda», encaje con `planner` y Lente A; delimitado frente a `documenter` (documentar la API) y `nemesis`.

### T-06 — Integración, doc, memoria y cierre

- **Descripción**: `docs/README.md` + `docs/en/README.md` (filas `changelog-sync`, `unit-tests`, `api-contract` en skills; `/doctor` en comandos; Lente D en `adversarial-review`), `README.md`/`README.es.md` (badges `skills-18` y `commands-12`; filas «What you get»/«Qué obtienes»; listas de skills y comandos), `.claude-plugin/plugin.json` + `marketplace.json` (descriptions sincronizadas con las 3 skills y el comando), `CLAUDE.md` (tablas de comandos y skills, revisión con Lente D, `dev.json` `tests`), `agent-kits/shared/README.md` (`doctor.py`), `docs/FLOWS.md` + EN (`/doctor`, `changelog-sync` en Fase 6, coverage gate en P5/qa, Lente D), `ci.yml.MANUAL-COPY` (+ copia `.github/workflows/ci.yml`) y `CONTRIBUTING.md` con las carpetas de tests nuevas, fila en `docs/roadmap/README.md`; `evals/cases/` para las 5 piezas con negativos vecinos («¿está bien instalado?» → doctor vs «configura el proyecto» → setup; «escribe el changelog» → changelog-sync vs «haz el release» → release.py; «sube la cobertura» → unit-tests vs «prueba la UI» → qa vs «hazlo con TDD» → tdd; «valida el OpenAPI» → api-contract vs «documenta la API» → documenter); lección **LES-013** «los tests unitarios son una skill compartida, no un agente: evitar roles duplicados» (`estado: propuesta`, fila en `docs/knowledge/README.md`); `skill-index.py` ≤ 45 líneas / 3.500 caracteres con 41 piezas (ajusta el recorte si hace falta y anótalo). Cierre: DoD global, ledger a `completado`.
- **Estado**: completado
- **Tiempo humano**: est. 1,5h · real 1,5h (estimado)
- **Tiempo IA (ejec.)**: est. 0,07h · real 0,07h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `docs/README.md`, `docs/en/README.md`, `README.md`, `README.es.md`, `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `CLAUDE.md`, `agent-kits/shared/README.md`, `docs/FLOWS.md`, `docs/en/FLOWS.md`, `docs/roadmap/README.md`, `ci.yml.MANUAL-COPY`, `.github/workflows/ci.yml`, `CONTRIBUTING.md`, `skills/plugin-dev/SKILL.md`, `docs/knowledge/lessons/LES-013-plugin-dev-tests-unitarios-skill-no-agente.md` (nuevo), `docs/knowledge/README.md`, `agent-kits/shared/skill-index.py`, `agent-kits/shared/test_skill_index.py`, `evals/cases/command-doctor.json`, `evals/cases/skill-changelog-sync.json`, `evals/cases/skill-unit-tests.json`, `evals/cases/skill-api-contract.json`, `evals/cases/command-setup.json`, `evals/cases/agent-qa.json`, `evals/cases/skill-tdd.json`, `evals/cases/agent-documenter.json`, `docs/roadmap/2026-09-03-superiority/tasks.md`
- **Verificación** (ejecutada 2026-09-03 sobre `feature/superior-level`, base `4635343`; el repo tiene hoy **9 agentes · 17 skills · 12 comandos** tras retirar `pdfy` y absorber `discovery` en `roles-and-jira-flow` — ADR-011): `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts skills/code-health/scripts skills/dependency-upgrade/scripts skills/changelog-sync/scripts skills/unit-tests/scripts skills/api-contract/scripts skills/jira-sync/scripts evals` → **`530 passed in 30.85s`** · `python3 scripts/lint_plugin.py` → **`lint_plugin: 9 agentes · 0 errores · 3 avisos`** (los 3 avisos son los preexistentes de nombre genérico: `/retro`, `/roadmap-status` y `/setup` — ok instalado como plugin) · `python3 evals/check.py` → **`38 ficheros · 133 casos (78 positivos, 55 negativos) · 38 piezas del repo · 0 errores`**, exit 0 (+4 negativos cruzados nuevos) · `python3 agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-03-superiority/tasks.md` → **`0 incoherencias · 0 avisos`**, exit 0 · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-superiority --base 4635343` → exit 0 (base real de esta tarea; `ab22a4c` ya no sirve: entre medias se cerró `roles-and-jira-flow`) · `python3 agent-kits/shared/skill-index.py --no-cache --json` → **`38 piezas · 44 líneas · 3490 chars`** (topes ≤ 45 / ≤ 3.500: sin retoque del recorte, cabe por 1 línea y 10 caracteres) · `python3 tests/test_manifests.py` → **`4/4 OK (17 skills · 12 comandos · 9 agentes en ambos manifiestos)`** · `python3 tests/test_readme_badges.py` → **`10 conteo(s) verificados OK (badges + prosa)`** · `python3 tests/test_skill_size.py` → `17/17 OK` · `python3 tests/test_export_skills.py` → exit 0 · `python3 tests/test_ci_manual_copy.py` → **`7/7 OK`** · `cmp ci.yml.MANUAL-COPY .github/workflows/ci.yml` → **sin salida** · `ls docs/knowledge/lessons | grep -c LES-013` → **`1`**.

**Criterios de aceptación**
- [x] Badges y prosa de ambos README coherentes con el repo REAL y `test_readme_badges` verde → `10 conteo(s) verificados OK (badges + prosa)`. *(Nota: el criterio pedía `skills-18`; la cifra real es **`skills-17`** — y además `agents-9` — porque la iniciativa siguiente, `roles-and-jira-flow`, retiró el agente `pdfy` y absorbió la skill `discovery` en `analyst` (ADR-011) DESPUÉS de escribirse este ledger: 18 → 17 skills y 10 → 9 agentes. `commands-12` sí coincide. Los badges y las filas «What you get»/«Qué obtienes» ya estaban al día; T-06 no los tocó, solo los verificó.)*
- [x] `plugin.json` y `marketplace.json` listan `changelog-sync`, `unit-tests`, `api-contract` y `/doctor` con `description` **idéntica** entre ambos (verificado por igualdad de cadena) y `test_manifests` 4/4. *(Nota: 4/4 OK con **17 skills · 12 comandos · 9 agentes**, no 18 skills, por la misma retirada de `pdfy`/`discovery` de ADR-011.)*
- [x] `docs/README.md` + EN (filas de las 3 skills, `/doctor`, Lente D) ya estaban; T-06 añade `CLAUDE.md` (fila `/doctor`, 3 filas de skills compartidas, Lente D de rendimiento en el párrafo de revisión, `dev.json` con `revision.lenteRendimiento`/`tests.coberturaMinima`), `agent-kits/shared/README.md` (fila `doctor.py`) y `docs/FLOWS.md` + `docs/en/FLOWS.md` (`/doctor` en el mapa 0 y nota en 7, Lente D en la Fase 3 —prosa y diagrama—, gate de cobertura en P5 del implementer, `changelog-sync` en el cierre). Fila `superiority` en `docs/roadmap/README.md`: **ya existía** (no se duplica).
- [x] Evals: los 4 ficheros nuevos ya tenían 2 positivos + 2 negativos vecinos cada uno; T-06 añade los **4 negativos cruzados** que faltaban — `command-setup` → `command:doctor`, `agent-qa` → `skill:unit-tests`, `skill-tdd` → `skill:unit-tests`, `agent-documenter` → `skill:api-contract`. `evals/check.py` → 0 errores, exit 0. *(Nota: **38 piezas**, no 41: 9 agentes + 17 skills + 12 comandos tras ADR-011.)*
- [x] `LES-013` creada con el formato exacto de `LES-012`/`LES-014` (frontmatter `id`/`tipo`/`area`/`estado`/`fuente` + `## plugin-dev` + bullet con fuentes enlazadas y verificadas) y fila en `docs/knowledge/README.md` con el mismo estado. *(Nota: nace en **`estado: aceptada (validada: revisión de dos lentes, 2026-09-03, intento 2)`**, no en `propuesta` como decía el criterio: la revisión de dos lentes de esta iniciativa ya pasó sin gaps y `roles-and-jira-flow` validó la doctrina de roles en ADR-011 caso 3, que es justo lo que la lección enuncia. La regla derivada se hizo accionable en el árbol de decisión de `skills/plugin-dev/SKILL.md` (152 líneas, < 200).)*
- [x] CI y `CONTRIBUTING.md` (sus DOS listas, EN y ES) corren las carpetas de tests nuevas — `skills/changelog-sync/scripts`, `skills/unit-tests/scripts`, `skills/api-contract/scripts` y `skills/jira-sync/scripts` (**cuatro**, no tres: `jira-sync` estrenó suite propia en `roles-and-jira-flow`); `cmp ci.yml.MANUAL-COPY .github/workflows/ci.yml` sin salida y `test_ci_manual_copy` 7/7. `skill-index` en **44 líneas / 3.490 caracteres** con 38 piezas, dentro de topes sin tocar el recorte.
- [x] **Cierre medido:** `530 passed`, `lint_plugin` 0 errores, `evals/check` exit 0, `ledger-lint` exit 0, `scope-check` exit 0 con `--base 4635343`. *(Nota: la base `ab22a4c` del criterio ya no es válida — entre medias se comitearon `roles-and-jira-flow` y su revisión; `4635343` es el HEAD real anterior a T-06 y por tanto el diff exacto de esta tarea.)*
