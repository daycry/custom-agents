# Roadmap — iniciativas (`docs/roadmap/`)

Una carpeta por iniciativa: `docs/roadmap/<fecha>-<slug>/` con `spec.md` + `evaluation.md` + `improvement-plan.md` + `tasks.md` (+ `testing/` cuando aplica). Una fila por iniciativa.

Estados: borrador · en-progreso · en-revision · completado · cancelado (spec: borrador · aprobada · implementada · obsoleta).

| Fecha | Iniciativa | Tiempo | Coste | Artefactos |
|-------|-----------|--------|-------|-----------|
| 2026-07-09 | qa-agent — agente qa (E2E Playwright + informe) | 21,6 h | ~1.087 € | [spec](2026-07-09-qa-agent/spec.md) · [eval](2026-07-09-qa-agent/evaluation.md) · [plan](2026-07-09-qa-agent/improvement-plan.md) · [tasks](2026-07-09-qa-agent/tasks.md) |
| 2026-07-09 | nemesis-sca-iac — trivy (SCA) + hadolint (IaC) | 12 h | ~605 € | [spec](2026-07-09-nemesis-sca-iac/spec.md) · [eval](2026-07-09-nemesis-sca-iac/evaluation.md) · [plan](2026-07-09-nemesis-sca-iac/improvement-plan.md) · [tasks](2026-07-09-nemesis-sca-iac/tasks.md) |
| 2026-08-10 | agent-best-practices — best practices de agentes top (tiering, tools mínimos, DoD, DRY, revisión adversarial, linter CI) | 20,7 h | ~1.069 € | [spec](2026-08-10-agent-best-practices/spec.md) · [eval](2026-08-10-agent-best-practices/evaluation.md) · [plan](2026-08-10-agent-best-practices/improvement-plan.md) · [tasks](2026-08-10-agent-best-practices/tasks.md) |
| 2026-08-10 | qa-strict — puertas deterministas (qa-gate, ledger-lint), Playwright estricto, cobertura criterios↔tests, bucle acotado, doble lente, bloques API/A11Y opt-in | 23,4 h | ~1.205 € | [spec](2026-08-10-qa-strict/spec.md) · [eval](2026-08-10-qa-strict/evaluation.md) · [plan](2026-08-10-qa-strict/improvement-plan.md) · [tasks](2026-08-10-qa-strict/tasks.md) |
| 2026-08-10 | jira-granularity — granularidad del volcado a Jira (modo fase: un issue por fase con tareas como checklist) además del modo tarea + publicación del resultado del revisor (comentario por criterio + worklog `[revisión]`); extensión de `jira-sync` | 24,3 h | ~1.261 € | [spec](2026-08-10-jira-granularity/spec.md) · [eval](2026-08-10-jira-granularity/evaluation.md) · [plan](2026-08-10-jira-granularity/improvement-plan.md) · [tasks](2026-08-10-jira-granularity/tasks.md) |
| 2026-08-10 | token-diet — dieta de tokens (read-discipline, filtrado Atlassian, progressive disclosure, output-discipline, mini-skill rates-verify) | 12,9 h | ~669 € | [spec](2026-08-10-token-diet/spec.md) · [eval](2026-08-10-token-diet/evaluation.md) · [plan](2026-08-10-token-diet/improvement-plan.md) · [tasks](2026-08-10-token-diet/tasks.md) |
| 2026-08-11 | coste-generacion — coste real de generación por artefacto y por tarea (usage-meter: tokens reales de la transcripción, frontmatter `generacion:`, overhead en /roadmap-metrics, calibración tokens→hora en /retro, formato humano `XhYm`) | 20,1 h | ~1.044 € | [spec](2026-08-11-coste-generacion/spec.md) · [eval](2026-08-11-coste-generacion/evaluation.md) · [plan](2026-08-11-coste-generacion/improvement-plan.md) · [tasks](2026-08-11-coste-generacion/tasks.md) |
| 2026-08-12 | sdd-hardening — autosuficiencia total (sin motores externos): constitución del consumidor + /spec-drift + criterios G/W/T + TDD/worktrees opt-in + debug-root-cause + cadena nativa SIEMPRE por defecto + subagentes de contexto fresco opt-in (4 mecánicas: task-brief.py, brief-only, estados ricos, revisor persistente) + doc observabilidad | 23,1 h | ~1.195 € | [spec](2026-08-12-sdd-hardening/spec.md) · [eval](2026-08-12-sdd-hardening/evaluation.md) · [plan](2026-08-12-sdd-hardening/improvement-plan.md) · [tasks](2026-08-12-sdd-hardening/tasks.md) · [retro](2026-08-12-sdd-hardening/retro.md) |
| 2026-08-12 | subagent-personas — **vía rápida**: perfiles de dominio para el subagente fresco — catálogo corto de 6 personas (`frontend/backend/db/devops/test/docs`) en el kit shared, campo opcional `Tipo` por tarea (lo asigna el planner) e inyección en el brief por `task-brief.py` (TDD, +6 tests) | 2,5 h | ~130 € | [spec](2026-08-12-subagent-personas/spec.md) · [tasks](2026-08-12-subagent-personas/tasks.md) (ledger ligero) · [retro](2026-08-12-subagent-personas/retro.md) |
| 2026-08-12 | workflow-polish — **vía rápida** (primera medida): las 3 disciplinas de flujo que faltaban — disciplina de RECIBIR la revisión (rebatir con evidencia), despacho PARALELO de tareas independientes (lotes máx. 3, medición honesta por lote) y ritual de CIERRE de rama (6 pasos) | 3,5 h | ~184 € | [tasks](2026-08-12-workflow-polish/tasks.md) (ledger ligero) · [retro](2026-08-12-workflow-polish/retro.md) |
| 2026-08-12 | plugin-dev — **vía rápida**: meta-skill de desarrollo del propio plugin (agentes/skills/comandos/kits/hooks): árbol de decisión, frontmatter+tiering+tools mínimos, validación TDD-ish, doc obligatoria, anti-patrones + plantillas rellenables | 2,5 h | ~130 € | [tasks](2026-08-12-plugin-dev/tasks.md) (ledger ligero) · [retro](2026-08-12-plugin-dev/retro.md) |
| 2026-08-13 | quick-implement — **vía rápida**: atajo en lenguaje natural a la vía rápida de `/dev-cycle` (los commands solo se disparan con la barra; las skills se auto-invocan por su descripción) — puerta de entrada delgada que delega en la fuente única, con disparadores negativos y filtro de idoneidad obligatorio | 1 h | ~52 € | [spec](2026-08-13-quick-implement/spec.md) · [tasks](2026-08-13-quick-implement/tasks.md) (ledger ligero) · [retro](2026-08-13-quick-implement/retro.md) |
| 2026-08-20 | confluence-policy — política explícita de qué documentación de `docs/` se publica en Confluence y qué no: **selección curada** (fuera `docs/en/`, `examples/`, `agents/`, planes, `tasks.md` y `testing/**`), disparadores de fin de ciclo `/retro` · `/spec-drift` · `/roadmap-brief` + ledger por fase, doc ES+EN y matriz de disparadores, **verificador + staging generado** (`confluence-scope.py --status`/`--stage` → carpeta derivada `docs/confluence/`, no editable a mano) — **spec `implementada` · evaluación `completado` · plan `completado` (4 fases, 13 tareas; revisión de dos lentes superada en 3 intentos; suites y linter en verde)** | 21,6 h | ~1.085 € | [spec](2026-08-20-confluence-policy/spec.md) · [eval](2026-08-20-confluence-policy/evaluation.md) · [plan](2026-08-20-confluence-policy/improvement-plan.md) · [tasks](2026-08-20-confluence-policy/tasks.md) |
| 2026-08-20 | knowledge-capture — **memoria técnica de los agentes**: ADR ligero con umbral, registro de gotchas en los puntos donde el conocimiento nace (`debug-root-cause` F4, flaky justificado de qa, retro), **bucle de lectura** (`knowledge-check.md`) en 5 agentes con las lecciones hardcodeadas del evaluator migradas a `LESSONS.md`, `/retro` de dos salidas, **backfill semilla** (5 `retro.md` + las 5 decisiones de confluence-policy) y doc ES+EN; índice+lint determinista **diferido** (>15 entradas o 1.ª colisión de IDs) — **spec `implementada` · evaluación `completado` · plan `completado` (5 fases, 16 tareas; revisión de dos lentes superada en 3 intentos; suites y linter en verde)** | 24 h | ~1.205 € | [spec](2026-08-20-knowledge-capture/spec.md) · [eval](2026-08-20-knowledge-capture/evaluation.md) · [plan](2026-08-20-knowledge-capture/improvement-plan.md) · [tasks](2026-08-20-knowledge-capture/tasks.md) |
| 2026-08-20 | knowledge-split — **vía rápida**: `docs/knowledge/` pasa a **un fichero por entrada** (`gotchas/GOT-NNN-<slug>.md`, `lessons/LES-NNN-<agente>-<slug>.md`) con lectura SELECTIVA por el índice en vez de cargar el registro entero, rutas actualizadas en escritores y lectores, espejo EN verificado y la decisión registrada como **ADR-006**; extensión `knowledge-ids`: IDs con la misma nomenclatura para gotchas y lecciones | 3,7 h | ~175 € | [tasks](2026-08-20-knowledge-split/tasks.md) (ledger ligero) |
| 2026-09-02 | deterministic-guardrails — **vía rápida**: los guardrails duros del `implementer` pasan de prosa a script — `guardrail-check.py` (alcance `docs/roadmap/` solo `tasks.md`, rama de trabajo, git destructivo) impuesto por un hook `PreToolUse` con alcance SOLO del agente (frontmatter `hooks:`, nunca global — ADR-007), `scope-check.py` (diff ⊆ `Archivos` del ledger) como puerta previa a la revisión, linter de los campos nativos `hooks`/`skills`, «Fase única» en `ledger-lint`; contratos oficiales verificados 2026-09-02 | 5 h | ~260 € | [tasks](2026-09-02-deterministic-guardrails/tasks.md) (ledger ligero) |
| 2026-09-02 | live-visibility — **vía rápida**: información en vivo mientras corren las tareas — `progress-report.py` (línea de progreso determinista del ledger, `active`, `session`, `--json`), hooks no bloqueantes `PostToolUse` (línea con debounce) · `SubagentStop` (iniciativas activas) · `SessionStart` (contexto de retoma, sobrevive a la compactación), statusline opt-in (modelo · coste · ctx · roadmap) en `/setup` 5-bis, linter de `hooks.json`; contratos oficiales verificados 2026-09-02 | 4 h | ~210 € | [tasks](2026-09-02-live-visibility/tasks.md) (ledger ligero) |
| 2026-09-02 | adversarial-review — **vía rápida**: la revisión adversarial de dos lentes sale de `commands/dev-cycle.md` a la skill compartida `adversarial-review` (fuente única del método: puerta `scope-check`, lentes A/B, **lente C de seguridad condicional** decidida por `review-lens-select.py` con tests, fusión y graduación, bucle acotado a 3, rebate con evidencia, traza «Revisión de dos lentes — intento N»), reutilizable desde /dev-cycle, quick-implement y a demanda; dev-cycle 181→156 líneas; lección LES-010 con las 5 evidencias | 4,5 h | ~235 € | [tasks](2026-09-02-adversarial-review/tasks.md) (ledger ligero) |
| 2026-09-02 | debt-cleanup — **vía rápida**: saldar TODAS las deudas anotadas por las tres iniciativas del día — debounce atómico con `flock` + rename (`progress-line.sh`), título sin negrita en `ledger-lint`, `hooks.json` 100644, guardrail case-insensitive + `docs/roadmap/README.md` permitido, lente C con stems con límite (`tokenizer`/`helmet` fuera) + `revision.excluir` + `/setup` 5-ter, copia manual de la CI vigilada (`test_ci_manual_copy`), linter «nombre genérico» afinado (3 avisos), **suite de shell de los hooks** (`tests/test_hooks_shell.py`, 20 casos) y lista de verificación manual en `docs/observability.md` | 5,5 h | ~285 € | [tasks](2026-09-02-debt-cleanup/tasks.md) (ledger ligero) |
| 2026-09-03 | activation-reliability — **vía rápida**: fiabilidad de ACTIVACIÓN y robustez del prompt (brecha frente a superpowers) — `evals/` (31 targets · 96 casos: positivos literal+paráfrasis y negativos vecinos con `redirect`; `check.py` en CI atado a las descriptions reales, `run.py` local con `claude -p`), **índice compacto de piezas** al arrancar/retomar/compactar (`skill-index.py`, ≤ 45 líneas / ≤ 3.500 caracteres, caché por hash, `dev.json` `sesion.indice`), **tablas de racionalización** («iron law») antes del DoD/veredicto de implementer · adversarial-review · qa, linter con avisos de cobertura en evals y description > 1.200 caracteres, lección LES-011 | 7,5 h | ~390 € | [tasks](2026-09-03-activation-reliability/tasks.md) (ledger ligero) |
| 2026-09-03 | plan-and-diet — **vía rápida**: tres patrones de superpowers que faltaban — **token-diet** de las 3 skills grandes (`jira-sync` 289→149 · `confluence-publish` 432→196 · `cybersecurity` 976→167 líneas) con progressive disclosure a `references/` y cero pérdida verificada (`tests/test_skill_size.py --diet-check`, umbral duro 250 + aviso del linter > 200, ADR-008), campo **`Verificación`** obligatorio por tarea (plantilla del planner, `ledger-lint` con `verificacion: obligatoria`, `task-brief`, implementer/Lente A) y **CI headless opcional** (`headless.yml`: evals reales baratos + hooks en sesión real con fichero-testigo, secret `ANTHROPIC_API_KEY`) | 8 h | ~415 € | [tasks](2026-09-03-plan-and-diet/tasks.md) (ledger ligero) |
| 2026-09-03 | distribution — **vía rápida**: distribución del plugin — **paquete portable «solo skills»** (`scripts/export-skills.py`: `claude` · `agents-md` · `cursor` · `all`; 10 skills + fragmentos shared citados; `find "$PWD/.claude"` → `find "${PORTABLE_ROOT:-.}"`; README ES+EN con qué viaja/qué no; determinista con hash; `--check`; zip en la Release), **`release.py` completo** (mueve `[Unreleased]`/`[Sin publicar]`, lint + evals + copias manuales + `.sh` en `100644`, `--dry-run`, `--check`; 14 tests sobre repo git temporal), **pulido** (`displayName` + keywords/tags sincronizados con test, quickstart de 5 pasos + «¿Qué verás?» + «Comparado con superpowers», `CONTRIBUTING.md`, issue forms + PR template con copia manual vigilada) y lección LES-012 | 9 h | ~470 € | [tasks](2026-09-03-distribution/tasks.md) (ledger ligero) |
| 2026-09-03 | memory-health — **vía rápida**: memoria de sesión y salud del código — **`session-journal`** (hook `SessionEnd` + `journal.py`: entrada determinista por sesión en `docs/knowledge/journal/` — iniciativa activa, ficheros tocados, tareas que cambiaron de estado, marcadores del meter —, idempotente por `session_id`, reinyectada al arrancar/retomar ≤ 25 líneas; excluida de Confluence; **sin resumen por IA** porque el contrato oficial de `SessionEnd` ignora la salida de los hooks — ADR-010), skill **`code-health`** (`code-health.py`: duplicados por shingles, funciones largas/anidamiento, hotspots, TODO envejecidos; MD/JSON/baseline; 14 tests) y skill **`dependency-upgrade`** (`deps-inventory.py`: 7 manifiestos + lockfiles, `outdated` oficial, salto semver → spec de upgrade; 10 tests); contratos `SessionEnd` y hooks `prompt`/`agent` verificados 2026-09-03 | 10 h | ~520 € | [tasks](2026-09-03-memory-health/tasks.md) (ledger ligero) |
| 2026-09-03 | parity-core — **vía rápida**: paridad de núcleo — **tiering configurable en dos capas** (`effort` obligatorio en el frontmatter + `dev.json` `modelos.<agente>`, `model-tier.py` con 11 tests, linter, `/setup` 5-quater, orquestadores pasan `model` al Agent tool; ADR-009), agente **`architect`** (2-3 opciones con trade-offs → `design.md` validado por trozos + ADR; pm-cycle 2-bis, dev-cycle 2-a, planner respeta la opción), skill **`tdd`** (fuente única de RED-GREEN-REFACTOR, `by-stack` + `anti-patterns`, `task-brief` la inyecta), agente **`reviewer`** de solo lectura para las lentes (fallback a subagente genérico), referencias **`docs-style`** ES/EN y **`claude-code-contracts`** (contratos oficiales con fecha) | 11 h | ~575 € | [tasks](2026-09-03-parity-core/tasks.md) (ledger ligero) |
| 2026-09-03 | superiority — **vía rápida**: cinco piezas de nivel superior sin roles duplicados — comando **`/doctor`** (diagnóstico determinista de la instalación: herramientas, plugin+hooks, statusline, configs, estado del trabajo; veredicto ✅/⚠️/❌ con arreglo por línea, 24 tests), skill **`changelog-sync`** (entradas `[Unreleased]`/`[Sin publicar]` EN+ES desde los ledgers cerrados, idempotente, `--check` en `release.py`), skill **`unit-tests`** (pirámide + `coverage-gate.py --changed-only` por stack; la usan implementer y qa — **no** es un agente, LES-013), **Lente D de rendimiento** en `adversarial-review` (N+1, bucles con I/O, `await` en `for`; `revision.lenteRendimiento`) y skill **`api-contract`** (`openapi-lint.py` sin dependencias + `--diff` de cambios rompedores) | 12 h | ~625 € | [tasks](2026-09-03-superiority/tasks.md) (ledger ligero) |
| 2026-09-03 | roles-and-jira-flow — **vía rápida**: un rol un dueño + ciclo Jira firmado — matriz **`docs/agents/ROLES.md`** (9 agentes + 3 orquestadores, DECIDE/ESCRIBE/LEE/no hace) con los solapes RESUELTOS (**`pdfy` retirado**, skill **`discovery` absorbida** por `analyst`, fronteras qa↔unit-tests y adversarial-review↔reviewer; ADR-011) + guardarraíl de disparadores duplicados en el linter; **`jira-flow.py`**: los 7 eventos del ciclo (`arrancar` · `implementado` · `revision` · `gaps` · `aprobado` · `qa-verde` · `qa-rojo`) como `ops` deterministas (etiqueta → transición → comentario → worklog) con el comentario renderizado desde `assets/comment-*.md` y **firmado por el agente** (`ca-implementer`/`ca-reviewer`/`ca-qa`), transición por `statusCategory`, worklog delegado a `worklog.py`, idempotente y agrupado (`--batch`); tabla única del ciclo en `/dev-cycle` Fase 3, gaps al implementer por su **brief** (no por Jira) y coste del ciclo medido en `docs/observability.md` (+EN); LES-014 | 9 h | ~470 € | [tasks](2026-09-03-roles-and-jira-flow/tasks.md) (ledger ligero) |
| 2026-09-03 | windows-console — **vía rápida** (bug reportado por el usuario con traceback real): salida UTF-8 segura en toda consola — los **27 scripts** versionados con símbolos (28 piezas tras T-05) reconfiguran `sys.stdout`/`sys.stderr` a UTF-8 con `errors="replace"` al arrancar (snippet replicado LITERAL: los scripts son standalone y viajan sueltos en el paquete portable), `tests/test_console_encoding.py` los descubre con el mismo grep del linter y los ejecuta bajo `PYTHONIOENCODING=cp1252` y `ascii`, y `lint_plugin.py` avisa del `.py` con símbolos sin snippet; **`release.py` deja de disfrazar el crash** — `clasificar()` distingue `FALLA` (exit 1 legítimo) de `ERROR al ejecutar` (traceback en stderr o exit ∉ {0,1}), que imprime las 3 últimas líneas de stderr y BLOQUEA también en `changelog-sync`; `GOT-005` con la causa raíz (revienta el HIJO del pipe, no la consola) y la decisión medida de **no** añadir modo ASCII; **T-04** (ronda de corrección de la revisión de dos lentes) cierra las dos mitades que faltaban del mismo bug — el snippet cubre también **`sys.stdin`** (sin él el guardrail del implementer dejaba de denegar EN SILENCIO ante cualquier contenido con emoji) y las **56 llamadas a `subprocess` en modo texto** del repo fijan `encoding="utf-8", errors="replace"` (el lado PADRE, que la propia T-01 había roto) —, más linter **estructural con `ast`** en vez de `grep` de subcadena, criterio único compartido literal con la suite, descubridor a prueba de rutas citadas por git y esta fila dentro de su tabla, con test que vigila la posición; **T-05** (cierre de los gaps del intento 2) amplía el criterio al lado que **LEE** —«no-ASCII en el fuente **o** `lee_stdin`», con `ast`— y con él entra la **28.ª pieza**, `pick_asset.py`, ASCII pura y lectora de `stdin`, cuyo `UnicodeDecodeError` se presentaba como «no hay binario para tu plataforma»; la regla escrita en `CONVENTIONS`/`plugin-dev`/`GOT-005` pasa a ser **exactamente** la vigilada; tres tests dejan de mentir (la comparación de veredictos solo mira lo versionado, el del `except` mide sobre un script real y trae su mutante, y la salida degradada a ASCII falla en vez de saltarse); y los `python3 -c` de `hooks/` y `statusline/` reconfiguran con `PYTHONIOENCODING=utf-8:replace` | 10,4 h | ~545 € | [tasks](2026-09-03-windows-console/tasks.md) (ledger ligero) |
| 2026-09-04 | changelog-brief — **vía rápida**: un bullet de CHANGELOG que se pueda leer — la skill `changelog-sync` ponía la **primera frase completa** de la `Descripción` del ledger (medido sobre los **13 ledgers cerrados**, **63 tareas**: mediana **447** y máximo **1.944** caracteres; el bullet entero, mediana 660 y máximo 2.163), y por eso las notas de la v1.16.0 se escribieron A MANO. Ahora el resumen sale de una **escalera determinista**: campo OPCIONAL `- **Changelog**: <una o dos frases>` en el bloque `### T-XX` —se usa **tal cual**; lo escribe quien CIERRA la tarea (`implementer` P3, plantilla del `planner`, `/dev-cycle`)— → primera frase si cabe en `RESUMEN_MAX = 200` (elegido midiendo 160/200/240: con 160 se pierde la única frase útil de 192 caracteres, con 240 entran enumeraciones de tres cláusulas de 237) → **oración principal** (primer `:`, `;`, `—` o `(` de nivel superior fuera de `código`/paréntesis/«comillas», con ≥ 5 palabras de prosa: sin esa puerta el corte devolvía listas de ficheros, porque las `Descripción` de este repo empiezan por la ruta) → **solo el título** + puntero al ledger. **Nunca trunca con `…`.** Ficheros clave de 5 a **3**, y **ninguno** si la tarea toca más de 6 (medido: mediana 8, máximo 62 ficheros por tarea). Empuje del campo: `--check` nombra las tareas sin él de las iniciativas realmente PENDIENTES, más una línea con el total, **sin tocar su exit code** (y el detalle por camino en `--check --json` → `degradacion.caminos`) y `ledger-lint.py` lo trata SIEMPRE como aviso y solo en **adopción parcial** (opcional por diseño: los 13 ledgers previos validan idéntico). Resultado medido: el bullet pasa de **mediana 660 / máximo 2.163** a **mediana 128 / máximo 325**, y de 100 de 126 bullets por encima de 400 caracteres a **ninguno** — con la deuda escrita y el desglose POR CAMINO, que dice lo incómodo: **42 de 63 tareas (67 %) degradan al título** (mediana 115) y el camino que se promueve es el que produce los bullets más LARGOS (`changelog` mediana 350, techo real del bullet completo **376**, no 325). `ADR-012` (**aceptada**) con las seis alternativas descartadas (truncar con `…`, resumir con modelo, seguir igual, solo el corte, campo obligatorio, un puntero por subsección); este ledger es el **primero que usa el campo**. **T-06 cierra los gaps del intento 1 de la revisión**: un `- **Changelog**:` VACÍO publicaba la línea siguiente del ledger (`\s*` se come el `\n` con `re.M`; el mismo fallo estaba en `Descripción` desde antes), la cola del ledger tras la última `### T-XX` se atribuía a esa tarea, los dos parsers del campo no reconocían lo mismo (ahora `CHANGELOG_FIELD_PATTERN` es uno, replicado literal y comparado byte a byte), el recorte a dos frases se comía la segunda cuando la primera llevaba una abreviatura (guarda `ABREVIATURAS`), y los tests se parametrizaban con las constantes que debían fijar (**14 de 25 mutantes sobrevivían; 0 de 40 ahora**) | 11,3 h | ~590 € | [tasks](2026-09-04-changelog-brief/tasks.md) (ledger ligero) |

**Calibración:** las iniciativas con `retro.md` alimentan [`CALIBRATION.md`](CALIBRATION.md), de donde `evaluator` toma el histórico y `usage-meter.py` la mediana `tokens/hora` para convertir tokens medidos en horas-IA.
