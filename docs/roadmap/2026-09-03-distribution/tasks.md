---
tasks: distribution
descripcion: Cerrar la distribución del plugin con la vía rápida — (1) exportación PORTABLE «solo skills» (`scripts/export-skills.py`: paquete agnóstico de Claude Code en formatos `claude`/`agents-md`/`cursor`, determinista, con `--check`, zip adjunto a la Release); (2) `release.py` completo (mueve `[Unreleased]`/`[Sin publicar]` a la sección de la versión en los dos CHANGELOG, corre lint+evals, comprueba copias manuales y modo 100644 de los `.sh`, `--dry-run`, tests sobre repo git temporal); (3) pulido de distribución (`displayName`/`keywords`/`tags` sincronizados con test, quickstart de 5 pasos + «¿Qué verás?» + «Comparado con superpowers» en README ES/EN, `CONTRIBUTING.md`, issue forms + PR template con copia manual vigilada); (4) doc, lección LES-012 y cierre.
estado: completado        # borrador | en-progreso | completado | cancelado
creado: 2026-09-03
actualizado: 2026-09-03
via: rapida               # vía rápida de /dev-cycle: sin spec/evaluación/plan; conserva revisión de dos lentes + suites
verificacion: obligatoria # cada `### T-XX` lleva `- **Verificación**:`; ledger-lint lo exige (exit ≠ 0 si falta)
generacion:
  fuente: estimado        # usage-meter no disponible en este entorno (sandbox cloud)
---

# Checklist de Tareas — distribution (vía rápida)

| | |
|---|---|
| **Estado** | completado |
| **Fecha** | 2026-09-03 |
| **Plan** | n/a — **vía rápida** (sin spec/evaluación/plan; ledger ligero + revisión de dos lentes + verificación) |

> **⚠️ Ledger canónico de progreso.** Este fichero es la **fuente única de verdad** del avance del cambio. **Cualquier** implementador —el agente `implementer`, el chat principal, o un orquestador externo— **debe** marcar aquí cada tarea (checkbox + estado) al completarla y actualizar el resumen. Los ledgers propios de otras herramientas son **espejo**, no fuente.

> **Decisión del usuario (2026-09-03).** El release de hoy (v1.15.0) tropezó dos veces: `[Unreleased]` se quedó sin mover a mano en los dos CHANGELOG y varios `.sh` llegaron con modo `100644` desde Windows. Además, superpowers distribuye sus skills también fuera de Claude Code (Codex, Cursor, Copilot vía `AGENTS.md`/rules) y este plugin no. Se cierra en una iniciativa de vía rápida: T-01 exportación portable, T-02 `release.py` completo, T-03 pulido de distribución (manifiestos, quickstart, CONTRIBUTING, plantillas de GitHub), T-04 doc/lección. `CHANGELOG*.md` lo toca el orquestador (T-02 solo cambia la mecánica, no el contenido de `[Unreleased]`).

---

## Resumen de progreso

| Fase | Completadas | Total | Progreso | H. humanas (real/est) | H. IA ejec. (real/est) | Supervisión (real/est) | Tokens (real/est) |
|------|------------|-------|----------|-----------------------|------------------------|------------------------|-------------------|
| Fase única — distribución | 4 | 4 | 100% | 8,6 / 9,0h | 0,45 / 0,47h | 0,10 / 0,10h | 185k / 190k |
| **TOTAL** | **4** | **4** | **100%** | **8,6 / 9,0h** | **0,45 / 0,47h** | **0,10 / 0,10h** | **185k / 190k** |

---

## Fase única — distribución

**Estado**: completado · **Estimado**: 9,0h · **Real**: 8,6h (estimado) · **Coste est.**: ≈470 € · **Tokens est.**: 190k

### T-01 — Exportación portable «solo skills» (patrón multi-entorno de superpowers)

- **Descripción**: `scripts/export-skills.py [--out dist/portable] [--format claude|agents-md|cursor|all]` genera un paquete AGNÓSTICO de Claude Code con lo que viaja: `skills/*/SKILL.md` + sus `references/`, `scripts/` y `assets/` (sin `__pycache__` ni tests) y los fragmentos `agent-kits/shared/*` que las skills citan (`agent-kits/shared/<x>` o `$SHAREDKIT/<x>` en el texto de las skills copiadas, incl. la carpeta `personas/`). EXCLUYE agentes, comandos, hooks, statusline y las skills `quick-implement` (puntero a `commands/dev-cycle.md`, que no viaja) y `plugin-dev` (desarrolla ESTE plugin: agentes/comandos/hooks; necesita el repo) y lo documenta en el `README.md` del paquete (ES+EN) con la tabla «qué viaja / qué no / por qué». Formatos: (a) `agents-md` → `AGENTS.md` raíz (estándar abierto que leen Codex, Copilot, Cursor, Jules… — agents.md, verificado 2026-09-03: markdown plano, sin frontmatter, en la raíz del repo) con el índice compacto de skills (reutiliza `skill-index.py`: `piezas()` + `resumen()`, solo grupo `skill`) y la instrucción «lee `skills/<n>/SKILL.md` cuando aplique»; (b) `cursor` → `.cursor/rules/custom-agents-skills.mdc` con frontmatter de Cursor (`description` string, `globs` string separado por comas, `alwaysApply` boolean — cursor.com/docs/context/rules, verificado 2026-09-03; modo *Agent-Selected*: `alwaysApply: false` + `description`) apuntando al mismo índice; (c) `claude` → `skills/` tal cual, instalable como carpeta `skills/` de un proyecto (+ `agent-kits/shared/` con los fragmentos). Reescritura en la copia: `find "$PWD/.claude" [otras rutas] "$HOME/.claude"` → `find "${PORTABLE_ROOT:-.}"` (documentado en el README del paquete; el resto del comando `-type … -path '*agent-kits/shared/…'` no cambia porque el paquete conserva la misma estructura de carpetas). Determinista (misma entrada → mismo árbol; hash sha256 del contenido en el README y en el fichero-marcador `.custom-agents-portable`; el zip de la Release es reproducible: `touch -d @SOURCE_DATE_EPOCH` (fecha del commit del tag) + `TZ=UTC zip -X`). La carpeta de salida solo se vacía si tiene README con marca Y el fichero-marcador (nunca se pisa una carpeta ajena). `--check <dir>` valida un paquete generado: toda referencia `references/<x>.md`, `scripts/<x>`, `assets/<x>` y `agent-kits/shared/<x>` citada en un `SKILL.md`/`references/*.md` copiado existe dentro del paquete; ningún `find "$PWD/.claude"` colgando; hash del README coincide. Tests `tests/test_export_skills.py` (≥ 8). `dist/` en `.gitignore`. `release.yml.MANUAL-COPY` (+ `.github/workflows/release.yml` byte-idéntica, vigilada por `tests/test_ci_manual_copy.py`) adjunta también `custom-agents-skills-portable-<ver>.zip`. Doc: `docs/INSTALL.md` + EN sección «Usar las skills fuera de Claude Code»; README/README.es una frase + badge «portable skills».
- **Estado**: completado
- **Tiempo humano**: est. 3,5h · real 3,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,18h · real 0,17h (estimado)
- **Supervisión**: est. 0,04h · real 0,04h (estimado)
- **Archivos**: `scripts/export-skills.py` (nuevo), `tests/test_export_skills.py` (nuevo), `.gitignore`, `release.yml.MANUAL-COPY`, `.github/workflows/release.yml`, `docs/INSTALL.md`, `docs/en/INSTALL.md`, `README.md`, `README.es.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `export-skills --check: 73 ficheros · 0 problema(s)` exit 0 · `diff -r` sin salida · `grep` de `find "$PWD/.claude"` → 0 líneas · `12 passed` · `cmp` sin salida · `grep -c skills-portable` → 4 · `test_ci_manual_copy: 3/3 OK`): `python3 scripts/export-skills.py --out /tmp/portable --format all && python3 scripts/export-skills.py --check /tmp/portable` → exit 0 y árbol listado · `python3 scripts/export-skills.py --out /tmp/p2 --format all && diff -r /tmp/portable /tmp/p2` → sin diferencias (determinismo) · `grep -rn 'find "\$PWD/.claude"' /tmp/portable` → vacío · `python3 -m pytest -q tests/test_export_skills.py` → ≥ 8 passed · `cmp release.yml.MANUAL-COPY .github/workflows/release.yml` → sin salida · `grep -c "skills-portable" release.yml.MANUAL-COPY` → ≥ 1.

**Criterios de aceptación**
- [x] **Medido:** 10 skills · 13 fragmentos shared (incl. `personas/` y `templates/` por cierre desde `knowledge-write.md`; `ledger-lint.py` arrastrado por `scope-check.py`) · 24 `find` reescritos · 73 ficheros. `export-skills.py --format all` produce `README.md` (ES+EN, tabla qué viaja/qué no/por qué, hash, instrucciones por entorno), `AGENTS.md`, `.cursor/rules/custom-agents-skills.mdc`, `skills/<n>/…` (10 skills: las 12 menos `quick-implement` y `plugin-dev`) y `agent-kits/shared/<fragmentos citados>`; `--format claude` solo `skills/` + `agent-kits/shared/` + `README.md`.
- [x] Ningún fichero del paquete contiene `find "$PWD/.claude"`; los `find` reescritos usan `"${PORTABLE_ROOT:-.}"` y el README lo documenta.
- [x] Dos ejecuciones consecutivas producen árboles idénticos (`diff -r`) y el mismo hash en el README.
- [x] `--check` devuelve exit 0 sobre un paquete íntegro y exit 1 (con el fichero que falta) si se borra una `references/*.md` o un script citado, o si queda un `find "$PWD/.claude"`.
- [x] `tests/test_export_skills.py` **12 tests** (mini-plugin sintético + repo real) (formatos, exclusiones, reescritura, determinismo, `--check` positivo/negativo, hash, frontmatter Cursor, AGENTS.md lista todas las skills).
- [x] `dist/` en `.gitignore`; `release.yml.MANUAL-COPY` genera y adjunta `custom-agents-skills-portable-<ver>.zip`; copia `.github/workflows/release.yml` byte-idéntica.
- [x] `docs/INSTALL.md` + `docs/en/INSTALL.md` sección «Usar las skills fuera de Claude Code»; README/README.es frase + badge «portable skills».

### T-02 — `release.py` completo (las dos trampas del release de hoy)

- **Descripción**: `release.py X.Y.Z` pasa a hacer TODO el release mecánico sin cambiar su comportamiento por defecto (bump en los 3 sitios + commit + tag). Añade: (1) CHANGELOG — mueve el contenido de `## [Unreleased]` (EN) / `## [Sin publicar]` (ES) a una sección nueva `## [X.Y.Z] - AAAA-MM-DD` justo debajo, deja la sección Unreleased vacía y añade `[X.Y.Z]: https://github.com/daycry/custom-agents/releases/tag/vX.Y.Z` encima de los enlaces existentes; si `[Unreleased]` está vacío aborta con mensaje claro (un release sin notas es un error) salvo `--allow-empty-notes`; si la sección `[X.Y.Z]` ya existe no duplica. `--check` además comprueba que la versión actual tiene su sección en AMBOS CHANGELOG. (2) Checks previos al commit: `python3 scripts/lint_plugin.py` y `python3 evals/check.py` (aborta si fallan; `--skip-checks` los salta), `*.MANUAL-COPY` vs sus copias (aborta si difieren), y `.sh` versionados con modo `100644` (`git ls-files -s`) → los corrige con `git update-index --chmod=+x` avisando y los incluye en el commit (la trampa de Windows de hoy). (3) `--dry-run`: imprime el plan completo (bump, secciones que movería, checks, chmod, commit, tag) sin tocar nada; con `[Unreleased]` vacío NO se corta: marca «el release real ABORTARÍA aquí» (así el dry-run sirve también antes de escribir las notas). Si tras el `git add` no hay nada que comitear (release repetido), avisa y solo crea el tag. Al aplicar el chmod corrige también el modo en disco (en Linux/macOS `status` marcaría el fichero). Los ficheros del CHANGELOG entran en el `git add` del commit de release. Tests `tests/test_release.py` sobre un repo git temporal (≥ 8: bump, changelog movido EN+ES, enlace añadido, unreleased vacío aborta, dry-run no toca, check detecta sección ausente, modo 100644 corregido, no-git). Este cambio toca solo la mecánica: el contenido de `[Unreleased]` no se edita aquí.
- **Estado**: completado
- **Tiempo humano**: est. 2,5h · real 2,3h (estimado)
- **Tiempo IA (ejec.)**: est. 0,13h · real 0,12h (estimado)
- **Supervisión**: est. 0,03h · real 0,03h (estimado)
- **Archivos**: `scripts/release.py`, `tests/test_release.py` (nuevo), `docs/INSTALL.md`, `docs/en/INSTALL.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `14 passed` · `--check` → `OK: todas coinciden en 1.15.0` + `sección [1.15.0] presente` ×2 exit 0 · `--dry-run` → plan con checks OK, 2 avisos «ABORTARÍA» (Unreleased vacío hoy), 7 `.sh` en 100644 listados, commit/tag, exit 0 y `git status --porcelain` sin cambios salvo los ficheros de esta tarea · `git diff --stat -- CHANGELOG*.md` vacío): `python3 -m pytest -q tests/test_release.py` → ≥ 8 passed · `python3 scripts/release.py --check` → `OK` (versiones coinciden y `[1.15.0]` en ambos CHANGELOG) · `python3 scripts/release.py 9.9.9 --dry-run && git status --porcelain` → plan impreso y git limpio (sin cambios) · `git diff --stat -- CHANGELOG.md CHANGELOG.es.md` → vacío (el contenido no se toca aquí).

**Criterios de aceptación**
- [x] En un repo temporal con `[Unreleased]` con contenido: `release.py 1.2.3` crea `## [1.2.3] - <hoy>` en EN y `## [1.2.3] - <hoy>` bajo `## [Sin publicar]` en ES con el contenido movido, deja las dos secciones Unreleased vacías y añade la línea de enlace `[1.2.3]: …/releases/tag/v1.2.3` antes de `[1.1.0]: …`.
- [x] `[Unreleased]` vacío → exit ≠ 0 con mensaje que cita el fichero, y con `--allow-empty-notes` continúa (crea la sección con `_Sin notas._`/`_No notes._`).
- [x] `--check` falla si la versión de `plugin.json` no tiene `## [X.Y.Z]` en alguno de los dos CHANGELOG, y pasa en este repo.
- [x] `lint_plugin.py`/`evals/check.py` fallando → aborta antes de tocar nada (test con checks falsos vía monkeypatch); `--skip-checks` los salta. Copia manual divergente → aborta.
- [x] `.sh` en modo 100644 → `git update-index --chmod=+x` aplicado y avisado; el commit lo incluye; `--dry-run` solo lo lista.
- [x] **Medido:** `tests/test_release.py` 14 tests (bump+commit+tag, changelog EN+ES, enlace, repetido sin duplicar, vacío aborta, `--allow-empty-notes`, dry-run, `--check` ausente/incoherente, 100644 corregido y en el commit, lint fallando + `--skip-checks`, copia manual divergente, árbol `github-templates.MANUAL-COPY`, sin git / `--no-git`, semver). Nota: el repo tiene HOY 7 `.sh` en 100644 (`agent-kits/nemesis/tools/*.sh`, `agent-kits/qa/lib-guardrail.sh`) que el próximo `release.py` corregirá y avisará. `--dry-run` deja `git status --porcelain` vacío y los ficheros byte-idénticos; sin repo git → mensaje claro y exit ≠ 0 (salvo `--no-git`).
- [x] Comportamiento por defecto intacto: commit `chore: release vX.Y.Z` + tag `vX.Y.Z`; `--no-git` sigue existiendo.

### T-03 — Pulido de distribución

- **Descripción**: (a) `plugin.json`: `displayName` («Custom Agents — budgeted SDD lifecycle»; clave verificada en code.claude.com/docs/en/plugins-reference.md, 2026-09-03: «Human-readable name shown in UI; falls back to `name`»), `keywords` ampliados (jira, agile, sdd, budgeting, tdd, code-review, evals — y `product`, que ya era tag del marketplace, para que `tags ⊆ keywords`); `marketplace.json` `tags` en línea con las keywords y `description` de la entrada sincronizada con la de `plugin.json`. Test nuevo `tests/test_manifests.py`: ambas descriptions listan TODAS las skills y comandos que existen en el repo (mismo conteo que `tests/test_readme_badges.py`: carpetas de `skills/`, ficheros de `commands/`), `displayName` presente, `tags ⊆ keywords`. (b) README + README.es: «Get started in 2 minutes» reescrito como quickstart de 5 pasos copiables (marketplace, instalar, `/setup`, primera iniciativa con `/pm-cycle`, dónde mirar el resultado) + bloque «¿Qué verás?» en texto (sin GIF: `<!-- TODO(gif): … -->` comentado HTML; pendiente del USUARIO, no deuda técnica) + sección final «Comparado con superpowers» honesta (qué aporta, qué toma prestado, conviven con `--superpowers`). (c) `CONTRIBUTING.md` (EN + sección ES corta): proponer una pieza con `plugin-dev`, checklist antes del PR (`lint_plugin`, suites, `evals/check`, evals positivo+negativo, doc ES+EN, `[Unreleased]`), convención de commits `T-XX:`/`feat:`/`chore:`, sincronización de `.MANUAL-COPY`. Issue forms YAML en `.github/ISSUE_TEMPLATE/` (bug, mejora, pieza nueva; sintaxis verificada en docs.github.com «Syntax for issue forms» 2026-09-03: `name`/`description`/`body` obligatorios, tipos `markdown`/`input`/`textarea`/`dropdown`/`checkboxes`, `validations.required`) + `PULL_REQUEST_TEMPLATE.md`; como `.github/` está protegida para herramientas remotas, se genera también `github-templates.MANUAL-COPY/` con el mismo árbol y `tests/test_ci_manual_copy.py` compara ambos árboles (mismo mecanismo que los yml). `docs/README.md` + EN enlazan CONTRIBUTING.
- **Estado**: completado
- **Tiempo humano**: est. 2,0h · real 2,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,10h · real 0,10h (estimado)
- **Supervisión**: est. 0,02h · real 0,02h (estimado)
- **Archivos**: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json`, `tests/test_manifests.py` (nuevo), `README.md`, `README.es.md`, `CONTRIBUTING.md` (nuevo), `github-templates.MANUAL-COPY/` (nuevo), `.github/ISSUE_TEMPLATE/` (nuevo), `.github/PULL_REQUEST_TEMPLATE.md` (nuevo), `tests/test_ci_manual_copy.py`, `scripts/lint_plugin.py`, `docs/README.md`, `docs/en/README.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `14 passed` · `Custom Agents — budgeted SDD lifecycle 18` · `diff -r`/`cmp` sin salida · `1` y `1` · línea 240 «An honest note… superpowers» bajo `## Compared with superpowers` · `1` y `1` · `lint_plugin: 0 errores · 4 avisos` (los mismos)): `python3 -m pytest -q tests/test_manifests.py tests/test_ci_manual_copy.py tests/test_readme_badges.py` → passed · `python3 -c "import json;d=json.load(open('.claude-plugin/plugin.json'));print(d['displayName'], len(d['keywords']))"` → nombre y ≥ 17 keywords · `diff -r github-templates.MANUAL-COPY .github/ISSUE_TEMPLATE` (y `cmp` del PR template) → sin diferencias · `grep -c "TODO(gif)" README.md README.es.md` → 1 y 1 · `grep -n "superpowers" README.md | tail -1` → sección «Compared with superpowers» · `grep -c CONTRIBUTING docs/README.md docs/en/README.md` → ≥ 1 cada uno.

**Criterios de aceptación**
- [x] `plugin.json` tiene `displayName` y las 7 keywords nuevas; `marketplace.json` `tags` ⊆ `keywords` e incluye las 7; `tests/test_manifests.py` falla si una skill/comando del repo no aparece en alguna de las dos descriptions.
- [x] README/README.es: 5 pasos numerados con bloques copiables, «¿Qué verás?»/«What you'll see» con el árbol `docs/roadmap/<fecha>-<slug>/` y la línea de progreso del hook, `<!-- TODO(gif) -->` en ambos, sección «Comparado con superpowers»/«Compared with superpowers» de 3-4 líneas; `tests/test_readme_badges.py` sigue verde.
- [x] `CONTRIBUTING.md` con las cuatro partes (proponer pieza, checklist pre-PR, commits, `.MANUAL-COPY`) y sección «En español (resumen)».
- [x] `github-templates.MANUAL-COPY/ISSUE_TEMPLATE/{bug,mejora,pieza-nueva}.yml` + `PULL_REQUEST_TEMPLATE.md` byte-idénticos a los de `.github/`; `tests/test_ci_manual_copy.py` compara el árbol (falla si divergen, skip si falta la copia) y `lint_plugin.py` no rompe.
- [x] `docs/README.md` y `docs/en/README.md` enlazan `CONTRIBUTING.md` (fila `scripts/ · CONTRIBUTING.md · github-templates.MANUAL-COPY/` en la tabla de carpetas). **Medido:** `test_manifests` 4/4 (12 skills · 11 comandos · 8 agentes en ambas descriptions); `test_ci_manual_copy` 7/7 (3 yml + 3 issue forms + PR template); `lint_manual_copies` cubre también el árbol (caso nuevo en pytest); 3 issue forms validados con PyYAML (`name`/`description`/`body`, tipos `markdown`/`input`/`textarea`/`dropdown`/`checkboxes`, `validations.required`).

### T-04 — Doc, memoria y cierre

- **Descripción**: `CLAUDE.md` (árbol: `dist/` ignorado, `scripts/export-skills.py`, `CONTRIBUTING.md`, `github-templates.MANUAL-COPY/`; fila «Linter + tests» cita `release.py --check`), `docs/README.md` + `docs/en/README.md` (fila `scripts/`: export-skills y release completo), `agent-kits/shared/README.md` si aplica (los fragmentos viajan en el paquete portable), fila `2026-09-03 distribution` en `docs/roadmap/README.md`; lección `docs/knowledge/lessons/LES-012-plugin-dev-release-mecanico.md` (siguiente NNN libre = 012, comprobado; `estado: propuesta`): «`release.py` debe hacer TODO el release (changelog, checks, modos) — las dos trampas del 2026-09-02» + fila en `docs/knowledge/README.md`. Cierre: DoD global y ledger a `completado`.
- **Estado**: completado
- **Tiempo humano**: est. 1,0h · real 1,0h (estimado)
- **Tiempo IA (ejec.)**: est. 0,06h · real 0,06h (estimado)
- **Supervisión**: est. 0,01h · real 0,01h (estimado)
- **Archivos**: `CLAUDE.md`, `docs/README.md`, `docs/en/README.md`, `agent-kits/shared/README.md`, `docs/roadmap/README.md`, `docs/knowledge/lessons/LES-012-plugin-dev-release-mecanico.md` (nuevo), `docs/knowledge/README.md`, `docs/roadmap/2026-09-03-distribution/tasks.md`
- **Verificación** (ejecutada 2026-09-03 — salida: `1` · `1` · `1` · `8` · `285 passed` (249 + 36: 12 export-skills, 14 release, 4 manifests, 6 manual-copy) y las 13 suites-script OK · `lint_plugin: 8 agentes · 0 errores · 4 avisos` (los mismos 4 previos: `retro`/`roadmap-status`/`setup` genéricos + `adversarial-review` 203 líneas) · `evals/check: 31 ficheros · 96 casos · 0 errores` exit 0 · `ledger-lint: 0 incoherencias · 0 avisos` · `scope-check … fuera de alcance (0)` exit 0): `ls docs/knowledge/lessons | grep -c LES-012` → 1 · `grep -c LES-012 docs/knowledge/README.md` → ≥ 1 · `grep -c distribution docs/roadmap/README.md` → ≥ 1 · `grep -n "export-skills\|CONTRIBUTING" CLAUDE.md docs/README.md docs/en/README.md | wc -l` → ≥ 4 · `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → passed (249 + nuevos) · `python3 scripts/lint_plugin.py` → `0 errores` · `python3 evals/check.py` → exit 0 · `python3 agent-kits/shared/ledger-lint.py docs/roadmap/2026-09-03-distribution/tasks.md` → exit 0 · `python3 agent-kits/shared/scope-check.py docs/roadmap/2026-09-03-distribution --base 5a8c081` → exit 0.

**Criterios de aceptación**
- [x] `LES-012-plugin-dev-release-mecanico.md` con el frontmatter de la familia (`id`, `tipo: leccion`, `area`, `estado: propuesta`, `fuente`) y fila en `docs/knowledge/README.md`.
- [x] `CLAUDE.md`, `docs/README.md`, `docs/en/README.md` mencionan `export-skills.py`, `CONTRIBUTING.md` y `dist/`; `docs/roadmap/README.md` tiene la fila `distribution`.
- [x] **Medido (cierre):** `285 passed` · suites-script OK · `lint_plugin` 0 errores · 4 avisos · `evals/check` 0 errores · `ledger-lint` 0/0 · `scope-check --base 5a8c081` 0 fuera · `export-skills --check /tmp/portable` → `73 ficheros · 0 problema(s)` · `release.py --check` → `OK: todas coinciden en 1.15.0` + sección `[1.15.0]` en ambos CHANGELOG · `release.py 9.9.9 --dry-run` → plan completo, exit 0 y `git status --porcelain` vacío (ejecutado sobre árbol limpio con `git stash`) · `cmp` de `ci`/`release`/`headless.yml` y `diff -r` del árbol de plantillas sin salida. DoD global: suites verdes, `lint_plugin` 0 errores (avisos listados), `evals/check` exit 0, `ledger-lint` exit 0, `scope-check --base 5a8c081` exit 0, `export-skills --check` exit 0, `release.py --check` OK, `release.py 9.9.9 --dry-run` sin tocar nada, `cmp` de todas las copias manuales.

---

**Verificación de cierre (2026-09-03):** `python3 -m pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → 285 passed · suites-script OK · `lint_plugin` 0 errores · 4 avisos (los 4 previos) · `evals/check` exit 0 · `ledger-lint` 0/0 · `scope-check --base 5a8c081` exit 0 · `export-skills --check` 0 problemas (73 ficheros, determinista) · `release.py --check` OK · `release.py 9.9.9 --dry-run` sin tocar nada · copias manuales idénticas (3 yml + 3 issue forms + PR template). Pendiente de la **revisión de dos lentes** (la lanza el orquestador). `CHANGELOG*.md` sin tocar (lo hace el orquestador).

**Contradicciones/dudas con la doc oficial:** ninguna — `displayName` existe en la referencia de `plugin.json` (code.claude.com/docs/en/plugins-reference.md); `AGENTS.md` es markdown plano sin frontmatter en la raíz (agents.md); regla de Cursor con `description` + `alwaysApply: false` = modo *Agent-Selected* (cursor.com/docs/context/rules; `globs` omitido a propósito: no hay ficheros que disparen las skills); issue forms con `name`/`description`/`body` obligatorios y `validations.required` (docs.github.com). Decisión propia: `plugin-dev` tampoco viaja en el paquete portable (además de `quick-implement`), porque sirve para desarrollar este repo y cita `scripts/lint_plugin.py`, que no viaja.

**Pendiente del usuario (no es deuda técnica):** grabar el GIF del quickstart y sustituir el `<!-- TODO(gif) -->` de `README.md`/`README.es.md`. **Aviso:** el repo tiene hoy 7 `.sh` en modo `100644` (`agent-kits/nemesis/tools/*.sh`, `agent-kits/qa/lib-guardrail.sh`); el próximo `release.py X.Y.Z` los corregirá y avisará (T-02 no los toca para no salirse del alcance).

## Revisión de dos lentes — intento 1: 0 Critical · 3 Important · 5 Minor (todos verificados y corregidos en `T-fix1`)

| # | Grado | Gap | Tarea | Corrección | Evidencia |
|---|---|---|---|---|---|
| 1 | Important | `release.py` no comprobaba árbol limpio: un `CHANGELOG.md` modificado sin comitear se arrastraba al commit `chore: release` | T-02 | **Reproducido** (repo temporal con `CHANGELOG.md` editado → el commit lo incluía). `arbol_sucio()` (`git status --porcelain`) → aborta ANTES de escribir listando los ficheros, salvo `--allow-dirty`; en `--dry-run` lo marca «ABORTARÍA» y sigue | `test_arbol_sucio_aborta_antes_de_escribir` (snapshot idéntico, sin tag; dry-run marca; `--allow-dirty` sigue) |
| 2 | Important | Versión menor o igual que la actual no abortaba (`0.9.0` sobre `1.1.0`) | T-02 | **Reproducido**. `_tupla(new) <= _tupla(cur)` → aborta con «no es mayor que la actual», salvo `--force-version` (aviso). El test «release repetido» pasa a usar `--force-version` (misma versión) | `test_version_menor_o_igual_aborta_salvo_force` (`0.9.0`, `1.1.0`, `1.0.99` → exit 1 sin tocar; con flag → bump) |
| 3 | Important | `test_manifests.py` unidireccional: una skill listada pero inexistente (fantasma/borrada) pasaba | T-03 | **Reproducido** (`Skills: ghost-skill, …` pasaba). `listados()` extrae el conjunto de skills (segmento «Skills: … .») y comandos (`/x` fuera de rutas) y `discrepancias()` exige **igualdad de conjuntos** en ambos sentidos | `test_discrepancias_detecta_ausente_y_fantasma` (`ghost-skill` y `/ghost-cmd` → 1 discrepancia cada uno; `.claude/dev.json` no cuenta como comando); `test_manifests: 4/4 OK` |
| 4 | Minor | Tag `vX.Y.Z` existente se detectaba tras el commit | T-02 | `tag_existe()` (`git rev-parse -q --verify refs/tags/vX.Y.Z`) en las precondiciones, antes de escribir | `test_tag_existente_aborta_en_los_checks_previos` (HEAD intacto, snapshot idéntico, sin «Aplicando») |
| 5 | Minor | `release.py` normalizaba CRLF→LF en todo el CHANGELOG | T-02 | `read_raw()`/`write_raw()` con `newline=""`: detecta `\r\n`, procesa en LF y reescribe con el final de línea original («CRLF conservado» en la salida) | `test_crlf_se_conserva` (`count(\n) == count(\r\n)`, sección y enlace nuevos con `\r\n`; fichero LF sigue LF) |
| 6 | Minor | `export-skills.py`: la marca propia era solo la 1.ª línea del README → un README falsificado bastaba para `rmtree` | T-01 | Fichero-marcador dedicado `.custom-agents-portable` (marca + versión + formato + hash; excluido del hash), exigido junto al README por `es_paquete_nuestro()` antes de borrar; `--check` lo exige y compara su hash | `test_readme_falsificado_sin_marcador_no_basta_para_borrar` (README falso → se niega; marcador sin hash → se niega; paquete real → regenera; sin marcador → `--check` falla); 74 ficheros, `--check` 0 problemas |
| 7 | Minor | `cybersecurity` («Spawn ALL 8 agents using the Agent tool») depende del runtime y no estaba en la tabla del README del paquete | T-01 | Tabla `DEGRADA` (⚠️ degrades · degrada) con `cybersecurity` («en otros entornos, ejecuta las 8 dimensiones en secuencia con las referencias») y `discovery` (plantilla del evaluator), ES+EN; párrafo de degradación actualizado | `test_readme_del_paquete_tabla_y_bilingue` (`Spawn ALL 8 agents`, `degrada`, `en secuencia`) |
| 8 | Minor | Ledger T-01 afirmaba «mtimes fijas en el zip lo hace la Release» pero `release.yml` hacía `zip -qr` sin normalizar | T-01 | **Elegido: hacer cierta la afirmación** (un zip reproducible es lo que permite comparar hashes entre releases; retirar la frase dejaría la Release con mtimes del runner): `SOURCE_DATE_EPOCH=$(git log -1 --pretty=%ct)` + `find … -exec touch -d "@$SOURCE_DATE_EPOCH" {} +` + `TZ=UTC zip -X -qr` en `release.yml.MANUAL-COPY` y copia; ledger T-01 corregido con el mecanismo | simulación local: dos exportaciones + zip con 1 s de separación → `cmp` idéntico (sha256 `3a7548e582da…`); YAML parseado; `cmp` de la copia sin salida |

**Rebatidos:** ninguno (los 8 reproducidos o confirmados). **Verificación tras `T-fix1`:** `pytest -q tests agent-kits/shared skills/adversarial-review/scripts evals` → **290 passed** (285 + 4 `test_release` + 1 `test_export_skills`) · suites-script OK (`test_manifests 4/4`, `test_ci_manual_copy 7/7`, `test_lint_plugin 22/22`, …) · `lint_plugin` 0 errores · 4 avisos (los mismos) · `evals/check` 0 errores · `export-skills --check /tmp/portable` → 74 ficheros · 0 problemas · `release.py --check` OK · `release.py 9.9.9 --dry-run` sobre árbol limpio (stash) → exit 0, plan con «ABORTARÍA» (Unreleased vacío) y los 7 `.sh` en 100644, `git status --porcelain` vacío · `cmp` de las 3 copias yml y `diff -r` del árbol de plantillas sin salida · este ledger 0/0.

## Revisión de dos lentes — intento 2: sin gaps (re-verificación del orquestador: versión ≤ actual aborta, árbol sucio aborta antes de escribir, 290 tests). LES-012 promovida a `aceptada (validada: revisión de dos lentes, 2026-09-03, intento 2)`.
